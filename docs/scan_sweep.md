# `scan`:滑台蛇形扫描 + 逐点功率采集

控制滑台在一个矩形区域里**蛇形(逐行往返)扫描**,每到一个网格点停下来,读一次功率计,
把 `(X, Y, 功率, 时间戳)` 追加进 CSV,最后得到一张功率分布图。

它与 [hmi](hmi_click_position.md) 是**并存的两个程序**,不是一个程序的两种模式:

| | `bin/hmi.exe` | `bin/scan.exe` |
|---|---|---|
| 干什么 | 手动调试台:点哪里走哪里 | 自动采集:自己走完一个区域 |
| 面板 | 两根一维轨道 | 一块二维面板 + 热力图 |
| 会自己动吗 | 不会 | 会,而且一次一两小时 |
| 共用 | **同一个 `hmi/ecatworker.cpp` 编进来的** —— 线程模型 / 坐标零点 / CSP 插补 / 全套护栏只有一份实现 | |

**功率计是 PD300R + Juno+**(走 Ophir 的 COM,见 §7.1),另有三个模拟源。
接真机时**状态机、CSV、热力图一个字都没改** —— 改到的只有界面 (多三行设备选项 +
一个「读一次」按钮) 与一个专属工作线程。

> ### 读本文前先知道一件事(2026-09-21)
>
> **仓库里原先有一批命令行工具,现在没有了。** 它们是 `aliasinfo` / `slide_verify` /
> `slide_motion`(含 `sm_state` / `sm_pdo` / `test2`)/ `motor_test`,外加参数基线
> `baseline_ykd2205pe.ini` 与 `docs/slide_motion_verify.md`。`scan` 一个都不依赖,
> 所以连源码带 CMake 目标一起删了 —— **现在只有 `scan` / `hmi` / `scan_selftest`
> 三个目标**。
>
> 本文是**逐次改动的时间流水**,里面的 CLI 名字是当时的记录 —— 那些**结论、实测值、
> 风险等级、安全口径全都仍然有效**(它们约束的是驱动器和总线,不是那个程序),
> 但**照着做的指令已经不可执行了**。凡涉及"现在该跑什么、现在谁是调用方"的地方,
> 已经就地改写;纯历史条目保留原样。
>
> 那些 CLI 留下的**实测值**一条没丢,它们都还在本文里 —— 只是现在**没有任何程序会
> 自动把它们读出来**,要复核得自己临时加几行 `em_rd_*`。

---

## 1. 目录与文件

```
scan/
  CMakeLists.txt          两个目标: scan (界面) 与 scan_selftest (自检)
  main.cpp                em_console_init + QApplication + 字体/夜间配色 + setStyleSheet
  scanstyle.h/.cpp        界面样式表 (exe 旁边的 scan.qss) 的读盘 + 出厂那一份, 见 §31.7
  scanwindow.h/.cpp       主窗口: 顶栏 + 二维面板 + 参数面板 + 状态栏
  mapcanvas.h/.cpp        二维画布: 区域框 / 热力图 / 滑台位置 / 点击手动定位
  busview.h               控制器对总线的**全部**需求, 只有四个方法 (见 §2)
  scanplan.h/.cpp         ★纯逻辑, 不依赖 Qt★ 网格 / 蛇形 / 换算 / CSV 格式
  scanarrive.h/.cpp       到位判据 (插补到位 + 实测在容差内 + 连续稳定)
  scancontroller.h/.cpp   扫描状态机 (QObject, GUI 线程, 30Hz 外部喂时钟)
  powermeter.h/.cpp       PowerMeter 抽象 + 三个模拟实现
  scanlog.h/.cpp          CSV 追加写 + 读回
  scanprefs.h/.cpp        参数的记忆 (exe 旁边的 scan.ini)。**只链 Qt6::Core**, 见 §16
  selftest.cpp            **不需要硬件也不需要界面**的自检 (833 条断言, 见 §13)
```

`scanplan` **不 include 任何 Qt**(`std::vector` + POD + `std::string`)。

---

## 2. 线程模型:总线线程一个字不改

现有边界是 **`EcatThread` 独占总线,GUI 侧只经 `post*` / `set*` / `telemetry()` 与它交互**。
扫描完全落在这条边界的 GUI 一侧:

```
GUI 线程                                  工作线程 (2ms 循环, 不动)
─────────                                 ──────────────────────────
ScanController (30Hz, 时钟由外面喂)
  ├─ BusView::setTarget(axis, want)  ───▶  m_want[]  ─┐
  ├─ BusView::telemetry()            ◀──  BusTelem   │ interpolate(dt)
  └─ 状态机: Moving→Dwelling→Reading→Log             └─▶ em_csp_set_target
```

中间隔着一层 `BusView`(只有四个方法):
[hmi_click_position.md](hmi_click_position.md) 的「尚未验证」一节写明
**hmi 的「连接」那条路一次都没在真机上跑过**。也就是说这个仓库目前**没有可用的真机回路**
来验扫描状态机。而状态机里真正会出错的是暂停/继续/中止、到位窗口、外部改目标、
续扫从哪接上这些**时序**问题,它们跟总线没关系,只跟状态机有关。
换成 `FakeBus`(自己按速度推 `pos`,能注入掉帧/故障/失能/限位),整个状态机就能
**在没有硬件的情况下跑通** —— 这就是 `scan_selftest` 干的事。

**节拍是外部喂的**(`tick(now_ms)`),不是状态机自己起定时器。
时间一律用**单调钟**(`QElapsedTimer`)—— 墙上时间会被 NTP 和对时往回拨,
那样"停留了 200ms"可能永远不成立,扫描会卡在那一点上。

30Hz 对到位判据够用:换行一次移动要几百 ms,停留最少也有 100ms 量级。

### 唯一的例外:GUI 线程直呼了一次 `em_request_stop()`

上面那条边界在此之前是**无例外**的(除了 `main()` 里那句只设控制台代码页的
`em_console_init()`),现在有两个例外,都写在
[hmi/ecatworker.h](../hmi/ecatworker.h) 顶部。

非开不可的原因是**结构性的**:回零是 `em_home()` 在**工作线程里阻塞着**跑完的
(它自己泵帧、自己每 2ms 轮询 `6041h`),而命令队列是在 `EcatThread::run()` 顶部
**由同一个线程**排空的 —— 回零期间投进去的 `CMD_STOP` 要等 `em_home()` 退出来才被看见,
也就是最长 30 秒之后。
> **划改(2026-09-22)**:那个 30 秒是当时的常量,现在是可改的回零超时(缺省 120 s,上限 600 s),
> 见 §27。本节与 §18.2 的论证一字未变,**读的时候把「30 秒」换成「整个回零超时」** —— 它变长了,
> 所以这条例外比写的时候更有必要。

它安全的理由**同样是结构性的**:`em_request_stop()` 只往一个
`static volatile sig_atomic_t` 里存 1 —— 不碰 `em_bus_t`、不碰网卡、不做任何 I/O。
motor_api 自己就是按「信号处理器里也能调」来设计它的(见
[ec_motor.c](../motor_api/ec_motor.c) 里 `em_request_stop` 那一段)。所以它跟
「GUI 线程不许碰总线状态」这条规矩不冲突 —— 它碰的不是总线,是一个原子量。

**用它的地方只有两处**,都在 `scan/scanwindow.cpp`:
- 「停止」按钮在回零进行中走的那一支(`onStopClicked`);
- 断开 / 关窗收尾的第一步(`disconnectAndStop`)—— 那条路径是同步等 12 秒的,
  而回零单个动作就能阻塞 30 秒,不先掐掉就会在 `QThread` 被拆时还在泵帧。

`hmi` 那两个按钮**一个字都没改**(它连回零功能都没有),所以这条例外对它不存在。

---

## 3. 长度单位 (mm)、坐标与量程

**界面上那个长度单位就是 mm(毫米)** —— 区域 X/Y、分辨率、画布的两根标尺与悬停读数都写
`mm`。代码内部与 CSV 表头里仍旧叫 `unit`(`area_x_unit` / `res_unit` / `pulses_per_unit`),
那是**已经落在文件里的格式**: 改名会让以前采的 CSV 全都接不上(续扫靠表头做几何校验),
所以一个字都不动。改的只是**给人看的那些字**, 全部清单见 §24。

```
1 mm = 50000 脉冲          (2400h 实测的一圈; 界面上那一行「1 mm = N 脉冲」是可调参数,
                            缺省按"丝杠一圈走 1 mm"标定 —— 导程不同的机器要自己改)
显示坐标 = 6064h 原始值 − 软件零点
零点     = 「连接」那一刻读到的位置, 或「设为区域中心」按下的位置
           (**scan 从 2026-09-22 起: 连接不再重设零点, 详见 §29**)
区域     = 27×27 mm, **以原点为中心** → ±13.5 mm = ±675000 脉冲
```

> 本文件里 **2026-09-22 之前**写下的段落, 凡是说"N 单位"的, 说的就是现在的 N mm ——
> 那天只改了界面上与文档里的**字**, 参数含义、脉冲换算、CSV 格式一个都没动。

### 量程必须跟着区域走

`HMI_RANGE` 原来是编译期常量 `500000`,扫描区域要 ±675000。它在 `ecatworker.cpp` 里
夹了 **4 次**(`setTarget()` 两次、`interpolate()` 两次)。直接复用会把区域边缘
**悄悄削掉**。

改法(**纯增量,`hmi.exe` 行为逐字节不变**):

- `m_range` 从常量变成 `std::atomic<int32_t>`,**默认仍是 `HMI_RANGE`**
- 新增 `postRange(int32_t)`,走 `m_cmds` 队列(与 `postCenter` 同一个模式)
- `hmi` 自己从不调 `postRange`,于是它拿到的永远是 500000

```
量程脉冲 = ceil(max(max(area_x, area_y) / 2 + 1.0, 画布半宽) * pulses_per_unit)
         = ceil(max(13.5 + 1.0, 16) * 50000) = 800000   (默认参数, 2026-09-22 起)
```

> **划改(2026-09-22)**:上面那个公式原先是 `ceil((half + 1.0) * ppu)`(默认 725000),
> 现在多了"画布半宽"这条下限 —— 少了它, 一个 3 mm 的区域只放行 ±2.5 mm 的手动定位,
> 而画布仍然画到 ±16, 点面板边缘会被**悄悄夹回来**。全在 §28。

`+1.0` 的余量是给**手动对位**用的:扫描区域之外还能手动走一点。
`range_pul` **不暴露给操作员**,永远按区域自动算。

> **`postRange` 有个必须绕开的陷阱。** `interpolate()` 是**先夹 `m_tgt`、再用 `m_tgt`
> 算 CSP 目标**的。所以如果把量程**改小**、而滑台此刻正停在旧量程的边缘外,那一夹就会
> 把 `m_tgt` 拽回来 —— **等于凭空产生一次运动,而且是没人按过任何按钮的运动**。
> 所以 `doRange()` 的规矩是:**放大无条件允许;缩小只在新范围装得下所有轴的当前
> `m_tgt` 时才允许,装不下就拒绝**并提示"先把滑台走回区域内"。绝不为了迁就新量程
> 去改 `want`/`tgt`。

---

## 4. 网格与蛇形

```
n     = floor(area / res) + 1        // 27 / 0.5 + 1 = 55
span  = (n - 1) * res                // 27.0
坐标[i] = -span/2 + i * res          // -13.5 .. +13.5, 共 55 个
```

`area / res` 除不尽时取 `floor` 并**把网格居中**(此时 `span < area`),
于是永远**不会越出区域**。

轴 0 = **X**(行方向,快轴),轴 1 = **Y**(列方向,慢轴)。默认蛇形(逐行往返),
也可以改成每行同向。第一行的方向由「起始方向」定。

> **蛇形里每一步只动一根轴。** 行内走 X、Y 不动;到行尾时下一个点
> 就在下一行的同一列上,于是 X 不动、只有 Y 进一格。稳态下**不会出现两轴同时走的斜线** ——
> 唯一一次斜着走是「从当前位置到第一个网格点」那一步(以及续扫/重测时)。
> 每步要么是 `res` 的距离、要么是行尾一格 `res`。

### 时间估算

`estimatePerPointMs()` 是**线性**的:

```
移动   = res * pulses_per_unit / speed * 1000 = 25000 / 20000 * 1000 = 1250 ms
每点   = 移动 * 1.30 + settle + dwell + 50 * samples
       = 1625 + 100 + 200 + 50 = 1975 ms
全程   = 3025 * 1975 ms ≈ 100 分钟
```

`* 1.30` 是给**进近段减速**留的粗估,`50 * samples` 是采样本身占位
(模拟源是 0,真机未知)。界面上会明说这是线性估计、**实际更长**。

---

## 5. 到位判据

`AxisTelem::at_target` 只是 `want == tgt`(`ecatworker.cpp`),那是**插补器自己走完了**,
**不等于实际位置到了**。所以判据是**两个都要**:

```
到位(i) = telem.ax[i].at_target                     // 插补目标已到终点
       && |telem.ax[i].pos - want_i| <= pos_tol     // 6064h 实测也在容差内
```

并且要**连续 `settle_ms`(默认 100ms)成立**才算稳定 —— 单帧瞬时满足可能在高速
掠过时误判。窗口一旦被打破就**重新计时**。

`pos_tol` **由步长推出来,不暴露给操作员**:固定 50 pul 在 25000 pul 的步长下是 0.2%
(合适),但有人把步长调到 500 pul 时就变成"允许偏 10% 个格子"(不合适)。
所以按步长取 1/20,并夹在 `[50, 步长/4]` —— 默认参数下是 **1250 pul**。

**没有单独的 `Settling` 状态**:稳定窗口就长在 `Moving` 里,界面想问"现在是在走
还是在等稳定",用 `settlingNow()` 读。

---

## 6. 状态机

```
Idle ──开始──▶ Moving ──到位且稳──▶ Dwelling ──停够──▶ Reading ──采到──┐
                 ▲                                                      │
                 └──────────────── 下一个点 ◀──────────────────────────┘
                 全部走完 ▶ Done

Paused / Aborted 可以从上面任意一个状态进来, 且**立刻生效**。
```

- **Preflight**(缺一不可,全过才动):在 OP · 已使能 · 两轴 `mirror_ok` · 功率计已开 ·
  CSV 可写 · 参数合法 · 区域半宽 + 余量 ≤ 量程 · 点数 ≥ 1 且 ≤ 200000
- **Moving**:`setTarget(0, x_pul)` + `setTarget(1, y_pul)`(显示坐标),等两个轴都到位
- **Dwelling**:停 `dwell_ms`
- **Reading**:发一次 `requestReading()`,等信号回来(异步)。超时(默认 2s)
  或 `readingFailed` → 该点标为失败,CSV 里功率列写空、带一列错误原因,
  **扫描继续**(一个点读不到不该毁掉整轮)
- **Log**:追加一行 CSV + `flush` + 刷新热力图 → 下一个点

**`samples_per_point > 1`** = 到点后连采 n 次取平均。代价是每点多 n 倍**读数时间**
(真机串口一次几百 ms 时这项很贵),不是塞在停留期里白拿 —— 停留期是"等机械稳定",
两件事不该混。

### 暂停 / 继续 / 中止

- **暂停**:立即 `postStop()` 冻在当前位置(**保持保持力矩,不卸力**),
  当前点标记为未完成。**响应是立即的**。
- **继续**:**重新走向该点并重新采样**,所以不会写进半点数据的半成品。
- **中止**:同样冻住,状态转 `Aborted`,**已完成进度留在 CSV 里**。

> 暂停/中止走的是 hmi 的「停止」语义:**故意不写 `6040h=0x0000`**,那是卸力,
> 滑台会因自重下滑。

### 自动中止(每个 tick 都查)

扫描是**一次一两小时、没人看着**的长动作,所以下面这些在**每个 tick** 都查,
命中就立刻自动中止:

| 条件 | 备注 |
|---|---|
| 掉出 OP / 连接断了 | |
| 任一轴 `6041h` bit3 故障 | `EcatThread` 已经会冻结目标,但扫描必须停下来 |
| 任一轴 `6041h` bit11 撞硬件限位 | 判据读 `AxisTelem::limit_active`(只有 `ecatcmd::limit_hit()` 一处算出)。中止文案会把 `60FDh` 那一行一并写出来(哪个开关压着 / 状态未知) |
| WKC 连续 10 帧不足 | 拔网线就是这个表现 |
| 任一轴丢帧(`mirror_ok` 掉)或掉使能 | |
| 有人从**旁路**改了目标 | 见下 |
| 走到位的最后期限 | `max(5000, 距离/速度*3 + 3000)` 封顶 120s —— 卡住/被夹住时的兜底 |
| CSV 写失败 | 宁可停,也不要默默少采几千点 |

中止时弹**红色横幅 + 模态框**。

> **`externalWantChanged()` 的两个坑。**
>
> 1. `setTarget()` → `publish()` 之间差一个 2ms 周期,所以下发之后不能立刻比 ——
>    那样会把自己当成外部干预。于是有一道"等它出现"的确认门(`m_want_ok`)。
>    但这道门**可能永远开不了**:如果在我们自己的值出现**之前**就有别人改了目标,
>    扫描会跟着别人的目标一路走下去,直到 120s 的移动期限才停。
>    所以加了 `WANT_CONFIRM_MS = 200`:下发 200ms 内没看到自己的值,就判定为外部干预。
> 2. **暂停时目标本来就"不是我们下发的那个了"** —— `postStop()` 把它冻在当前位置
>    (`want := tgt`),而那正是我们要的。不在暂停时放行的话,**一按暂停就会被自己
>    判定成"有人从别处改了目标"然后自动中止。**

---

## 7. 功率计接口(异步)

真机很可能是串口/SCPI,一次往返几百毫秒。所以是**请求 + 信号**:

```cpp
class PowerMeter : public QObject {
   Q_OBJECT
public:
   virtual QString kind() const = 0;
   virtual bool    open(QString *err) = 0;
   virtual void    close()            = 0;
   virtual void    requestReading()   = 0;   // 非阻塞; 恰好回一次 ready 或 failed
   bool isOpen() const;                      // Preflight 要问这一句
signals:
   void readingReady (double watts);
   void readingFailed(const QString &err);
};
```

四个实现(三个模拟 + 一个真机):

| 实现 | 用途 |
|---|---|
| `ManualMeter` | 界面上现填一个数,每次采回来都是它。用来手对流程 |
| `RandomMeter` | 基值 + 噪声,可调延迟。用来压热力图的配色与刷新 |
| `ScriptMeter` | 读一个每行一个数的文本文件,**每次请求取下一行**。可以配延迟 |
| `OphirMeter` | **真机**: PD300R 探头 → Juno+ 表头 → PC,走 `OphirLMMeasurement` COM |

`ScriptMeter` 是没有功率计时最有用的一个:**它能让整条流水线(状态机 → CSV → 热力图 →
断点续扫)完整跑一遍,而且数据是可预测、可断言的**。

三个模拟实现都用 `QTimer::singleShot(...)` 把 `emit` **挪出调用栈**
(`ManualMeter` 是 `0`,另两个是那个可调的 `delay_ms`,默认 20ms)。
如果 `requestReading()` 直接 emit,状态机的 `readingReady` 槽会在
`requestReading()` 内部**嵌套执行** —— 状态机正在 Reading 里发请求,槽又把它推到下一步,
于是所有"进入某状态后紧接着做的事"都会踩到自己。延时投递之后,模拟源与真机
(串口回来了才 emit)的行为**完全一致**,状态机只需要考虑一种时序。

**一个例外**:没打开 (`!m_open`) 时三个模拟实现都是**同步** emit
`readingFailed` 然后 return。也就是说"`emit` 一定在调用栈之外"这句话**只在打开之后成立**。
扫描里走不到这一支 (Preflight 已经要求 `isOpen()`),「读一次」那个按钮也挡了没打开的情形,
但任何新的调用方都不能假设"发完请求就已经把状态摆好了才轮到回话" —— 见 §17.3。

**不做工厂 / 注册表 / 配置文件**:一个基类 + 四个子类 + 界面上一个下拉框的 `switch`。
也不支持并发请求 —— 一个时刻只允许一个未决请求,状态机才能有确定的 Reading。

### 7.1 真机那一路(`OphirMeter`,PD300R + Juno+)

硬件链路是 **PD300R (光电二极管探头) → Juno+ (USB 表头) → PC**,软件走 Ophir 官方的
`OphirLMMeasurement` COM 对象。四件事把它和其它三个实现区分开:

1. **它有自己的线程。** 那个 COM 服务是 Apartment 线程模型 (手册 "Threading issues"):
   一个对象只能在**创建它的线程**里用。而界面线程不能去做 USB 往返。两条加起来只剩一条路
   —— 所有 COM 调用集中在专属线程里,界面靠原子标志 (请求读数) 和信号 (结果) 跟它说话,
   **一次直接调用都没有**。
2. **`open()` 会阻塞**(有上限,超时就把线程收掉并报错)。接口就是这么定的
   (`bool open(QString *err)`),真机却做不到"设一下就好":枚举 USB、开设备、读探头、配置、
   开流要几百毫秒到一两秒。
3. **读数的语义**:设备一直在推数,而状态机是"到位了给我一个数"。所以每次返回**缓冲区里
   最新的那个**,并且按设备时间戳记水位线 —— **同一个采样不返回两次**(否则
   `samples_per_point > 1` 求的平均就是个假数)。`status != 0` 的项**不能当测量值**
   (手册:"The status code must always be checked"),过量程就明确报错。
4. **信息由设备给,不按型号推断。** 表头型号/序列号、探头型号/序列号,以及波长/量程/模式
   **三个下拉框的选项表**,全部原样来自设备 —— 手册明确说了不要按型号推断规格。换探头就是
   换一张选项表。

打开时界面会把这四样摆出来 ("功率计已接上: ...")。

**踩过的一个坑**:这台机器注册表里那个 typelib 的版本号是字面量 `a.a`,于是
`IDispatch::GetIDsOfNames` / `GetTypeInfo` / `Invoke` 全返回 `0x8002801D`。所以
`ophircom` **绕开注册表**,直接从 dll 资源里 `LoadTypeLibEx` 再走 `ITypeInfo::Invoke`。
自检里那条 "GetVersion 拿得到数" 证明的不是"设备在",而是**这套绕法成立、名字解析和派发都通**。

---

## 8. CSV:它本身就是进度文件

**不另存 sidecar 状态文件** —— 边采边写的 CSV 里已经有全部信息。

```csv
# scan v1
# area_x_unit=27.000000 area_y_unit=27.000000 res_unit=0.500000 pulses_per_unit=50000.000000
# speed_pul_s=20000 dwell_ms=200 settle_ms=100 samples=1 serp=1 start_pos=1
# range_pul=800000 pos_tol_pul=1250 zero_epoch=1
# started=2026-09-17T10:00:00
index,ix,iy,x_unit,y_unit,x_pul,y_pul,watts,ok,flags,pos_x_pul,pos_y_pul,spread_x_pul,spread_y_pul,unix_ms,elapsed_ms
0,0,0,-13.5,-13.5,-675000,-675000,0.001234,1,,−675000,−675000,60,45,1758096000123,412
```

- **每采一点追加一行 + `flush`** → 中途崩了/断电了,已采的数据一行不丢
- 列名一律 ASCII、数值用 C locale 格式化(`%f`/`%lld` 本来就是)—— 中文列名在中文
  Windows 上会被 Excel 按 GBK 打开成乱码,而某些区域设置会把小数点变成逗号、
  把整列解析搞崩
- `watts` 用 `%.9g`,**采不到时留空而不是写 0** —— 0 是一个合法的读数,
  拿它当"没采到"的哨兵,以后就没法区分"这儿真的没有功率"和"这儿没读上"了
- **`ok` 与 `flags` 是一对**:`ok=0` 时 `flags` 写原因(如 `timeout`),`ok=1` 时留空。
  单点重测写 `flags=retest` —— 重测**不是一个单独的列**,而是与失败原因共用这一列
  (两者可以同时成立,那时用 `|` 连起来,如 `timeout|retest`)。热力图与统计一律取
  **该格最后一行**
- `pos_*_pul` 是**采样那一刻的实测位置**,和网格点的理论值放一起就能看出这一点的
  定位误差;`spread_*_pul` 是稳定窗口内 `pos` 的极差

### 断点续扫

打开一个已有 CSV → 读表头**校验几何参数一致**(区域 X/Y、分辨率、每 mm 脉冲数;
不一致就拒绝并**指出哪个变了**)→ 读所有 `(ix,iy)` → 剩下没采的补扫 →
**继续往同一个文件追加**。

速度/停留/采样次数这些**不影响几何**的随便改,不该拦。

### 零点世代(`zero_epoch`)

每次「连接」或每次「设为区域中心」,**零点世代 +1**,写进 CSV 表头。

连接时 `tryInitOrigin()` 会把零点重设为"当时所在的位置"。
所以断线重连之后,**同一个显示坐标指的可能已经是另一个物理位置**。

> **划改(2026-09-22)**:上面这两段对 **`hmi`** 仍然一字不差地成立(它没改)。
> **`scan` 不再是这样**:连接改为**沿用**上一次那个零点, 于是连接本身不再推世代 ——
> 推世代的变成工作线程(第一次取零点 / 回零 / 「设为区域中心」/ 沿用不了只好重取这四处),
> 界面只在 `refresh()` 里从遥测跟着往前同步。§29。

续扫时世代对不上**不是错误**:界面**不弹确认框**,直接按"允许世代
不同"接上去,并把那句差异留在**红色横幅**上(接错了「中止」随时可按,已经采到的点都在
CSV 里)。

### 单点重测

在画布上 **左键**选中一格 → 点「重测选中点」→ 走回那一点再采一次,
**追加一行**(带 `flags=retest`),不改动别的格子。热力图与统计取**该格最后一行**。

> **`ScanLog::beginAppend()` 会先把没写完的那半行切掉。** 追加写是掉电留下的残行是
> **正常情况**;而 `QIODevice::Append` 会把下一行**粘在那半行后面**,
> 于是产生一行字段错位的垃圾,并且**它之后的所有行都跟着错位**。
> 切回最后一个 `'\n'` 是唯一不丢东西的做法 —— 那半行按定义就是没写完的。

> **跑完之后日志不关。** 中止也一样不关。

---

## 9. 二维面板与配色

`MapCanvas` 是 hmi 那两条一维轨道的二维版本,语义照搬:
**实心点 = `6064h` 实测位置,橙色 = `want`(Shift+点击给的),绿色虚线 = `tgt`(本周期下发的)**。
多出来的一层是热力图 —— 以及**棋盘上那两种左键**:见下面的「两个左键」。

- **可视量程固定 30×30 mm**(每边 ±15 再各留 1 mm 余量,`MapCanvas::kViewHalfUnits = 16`),
  **不跟着区域缩放**;网格 + 每 5 mm 一条淡线 + 零点十字
  > **划改(2026-09-22)**:原句留作记录,但那个常量已经不叫这个名字也不住在这里了 ——
  > 它现在是 `scan::kCanvasHalfUnits`(`scan/scanplan.h`),因为**软量程的下限也是它**,
  > 一份定义两处用。上面「每边 ±15」说的是标尺画到哪,接受点击的边界一直是 ±16。见 §28。
- **坐标标尺**:下边 X、左边 Y,每 1 mm 一个小刻度、每 5 mm 一个带数字的,
  位置与网格线**同一组**(都走 `pxOf(u)`,u 为 5 的倍数);零点用亮色,别的用灰。
  **`mm` 写在两根标尺交会的那只角上**
- **区域框**:超出量程时**标红并写明"边缘会被静默夹掉"**
- **扫描路径预览**:开始前画出将要走的蛇形折线
- **当前点高亮** + 剩余点数 / 预计剩余 / 悬停读数(**mm** + 脉冲)
- **左键 = 查看那一格**(选中 + 读出数值 / 索引 / 坐标);**Shift + 左键 = 手动定位**;
  手动定位在扫描进行中一律吞掉,查看随便点
- **撞硬件限位时**,实测位置那个点外面套一圈红环 + 写下是哪根轴(见下)

### 视野固定为 30×30

视野固定成 ±16 mm 之后:

- 1 mm 恒等于固定的一格,热力图可以直接叠着比;
- 区域框成了**图里的一个量** —— 默认 27×27 时每边还剩 2.5 mm 余量;
- 每 5 mm 一条的网格线落在 ±5/±10/±15 上,正好与 30×30 的边界对齐。

窗口变大变小,变的是这块方形的**像素边长**,不是它的量程。正方形画布**居中**,
区域尺寸写在左上角第三行。

> **画布上的东西一律"按单位存,画的时候才换成像素"。** 热力图、蛇形轨迹、滑台位置
> 都是这么画的,所以窗口一拉伸是**整张图一起缩放**。
>
> 蛇形轨迹把 `pxOf()` 的结果(像素)缓存进 `m_path` 时,缓存只在"网格数/点数变了"时失效 ——
> 窗口缩放这两样都没变,于是热力图跟着长了,折线却停在旧的像素位置上。
> 现在 `m_path` 存的是**单位**,像素由 `drawPath()` 用 `QTransform` 现算。
>
> 那个变换里 `QPen::setCosmetic(true)` **不能省**:世界变换会把线宽一起放大 k 倍
> (k ≈ 20),不写的话那条 1px 的预览线会变成一条 20px 的灰带,把整张热力图盖掉。

> **手动定位被夹在"当前生效量程"之内。** 视野固定之后,画布上会有一圈既在扫描区
> 外面、也超出量程的地方;不夹的话在那儿点一下就是一次走到量程尽头的长动作,而界面上
> 完全看不出"我要的是 16 单位、实际只走到 14.5"。夹掉之后橙色三角停在量程边上。

### 两个左键:查看给左键,动滑台给 Shift + 左键

| 动作 | 干什么 | 扫描中 |
|---|---|---|
| **左键** | 查看那一格:选中 + 显示数值 / `[ix, iy]` / mm 坐标 | **能用**(只读) |
| **Shift + 左键** | 手动定位到那一点(与 hmi 的点击同义,走显示坐标) | 吞掉,只留一行提示 |

查看是只读的,所以不受扫描那道闸限制;点在没有网格的地方(区域框外那圈余量)
**取消选中**,而不是什么都不做。

数值那一行**没采到也写出来,写"未采集"**:空着的话跟"这里根本没选中"分不清,
而这两种情况的下一步动作完全不同(一个去重测,一个去检查是不是点歪了)。

### 布局:画布随窗口长,参数栏自己滚

左画布 | 右参数栏,中间一根**可以拖的分隔条**(`QSplitter`)。窗口变宽,多出来的地方
全给画布;参数栏塞在 `QScrollArea` 里,自己上下滚 ——
六组框加起来约 1330px,而窗口通常只给它 670。

> 参数栏**不能用"固定宽度"**。里面那些输入框有自己的最小宽度,固定宽度一旦压到它
> 下面,`QScrollArea` 就把右边**裁掉** —— 横向滚动条关着的话连拖都拖不回来。
> 交给 `QSplitter` 之后宽度分配由它算。

#### 横幅**不进布局**(2026-09-20 改)

那条横幅(`m_banner`)原先跟其他控件一样 `addWidget` 进竖向布局,排在分隔条上面 ——
于是它**出现时把下面的分隔条连同右栏整体往下推一行高,消失时又弹回去**。
而这条横幅是来回闪的(连接/断开/每次提示/每条回零状态),结果是参数栏一直上下跳,
**点偏一格就点到别的按钮上**。它本来就只是"说一句话",不该占体积。

现在它是**浮在画布顶上的一层**:不进布局,`placeBanner()` 自己算位置与折行高度
(`heightForWidth(canvas 宽 - 2×6px)`,左右各留 6px 边距),`raise()` 到最上层;
窗口 `resizeEvent` 与 `QSplitter::splitterMoved` 各接一次,画布一变宽就重算。
另外挂 `Qt::WA_TransparentForMouseEvents` —— 它纯是信息,点在它身上要能穿过去
落到画布上,否则"点了画布没反应"看起来像卡了。

#### 画布顶上留一条空带给它(2026-09-21 改)

上面那版是**纯浮层**:横幅是浮起来了,但它**压住画布左上角那三行 HUD 的前两行**
(`0 / 2695 点 剩 0`、`空闲 —— 参数可改`)—— 那正是扫描中要盯的数。
现在的做法是给画布加一条**专用空带**:`MapCanvas::kBannerBand = 20`,
并进 `kPadT`(见 mapcanvas.cpp 顶上那组留白),画图区与 HUD 一起**整体下移 20px**,
顶上那一段什么都不画 —— 横幅就落在它里面(`kBannerInset` 那 6px 是横幅在带子里的边距)。

- **算式**(都是量出来的):横幅占画布内 `6..34`;画图区顶 = 那原有的 20(超量程红字那条)
  + `kBannerBand` 20 = **40**;HUD 第一行从画图区顶 +6 = **46** 起 —— 中间剩 **12px**。
  **带子的下限是 8**:再小横幅就压住第一行了。横幅那 6 与 28 在别处
  (scanwindow.cpp 的 `kBannerInset` 与那三条样式表),改那边要连这个一起改。
- **代价**:画图区小 20px(高度受限时正方形跟着缩;宽度受限的窗口只是顶上多留白)。
  这是"用一点画布换 HUD 不被压住"。**空带里那条分界线是看不见的** ——
  画布底色与空带是同一块,看上去就是"图往下挪了一点"。
- **横幅换成一句更长的话会折成两行**(高 56,占到画布内 `62`),那就超出这条带子、
  重新压住第一行。带子按**一行**定:多数提示是一行,按两行定要白扔 40px。
- 另外**它是画布自己的留白,不是窗口再摆一个控件** —— 摆在画布外面也一样要占体积,
  而且横幅得改成按窗口坐标定位,反而把"横幅属于画布顶上"这件事拆散了。

**验证**(1360×860 的窗口,离屏跑真正的 `ScanWindow`,横幅由构造时那句
「未连接…」带出来、8 秒后由 `m_bannerTimer` 自己收掉):

| | 画布 | 参数栏(那个 `QScrollArea`) | 横幅 |
|---|---|---|---|
| 横幅在 | `9,40` **938×785** | `951,40` **400×785** | `15,46` 926×28,visible=1 |
| 横幅没了 | `9,40` **938×785** | `951,40` **400×785** | visible=0 |

两组**逐字段相等** —— 横幅的出现与消失对画布和参数栏**一点几何都没动**。
横幅自己:顶边离画布顶 6px、宽 926 = 画布宽 938 − 12、高 28(一行)。
抓图核过:三行 HUD 全在(`0 / 2695 点 剩 0` 在画布内 y≈53),顶上就是一条空带。

### 限位相关的显示:**四个地方、两个问题**

| 问题 | 谁回答 | 在哪儿看 |
|---|---|---|
| **扫描会不会中止** | `6041h` bit11 | 状态栏那对灯 + 画布红环 + 横幅 |
| **开关本身压着没有** | `60FDh` bit2/1/0 | 参数栏「轴信号」那三个灯 (每根轴一行, 见下) |

两者**可能不一致**(开关压着而驱动器没置 bit11,或者反过来),**不一致时以 bit11 为准**。

| 位置 | 内容 |
|---|---|
| 状态栏(常显,两根轴各一个) | **信号灯 + 文字**:`○ 限位X 正常` / `● 限位X 有效!`(红底) / 不知道时灰灯 + `限位X --` |
| 参数栏「轴信号」(见下) | 每根轴一行**五个灯**:使能 / 故障 / 原点 / 正限位 / 负限位 |
| 画布 | 实测位置点外面一圈红环 + `X 轴撞限位` |
| 横幅 | 上升沿弹一次(不自动消失) |

#### 信号灯:一条规矩,四种样子

**灯亮 = 这个名字代表的事正在发生**;灭 = 没发生;灰 = 不知道。

| 样子 | 画法 | 什么时候 |
|---|---|---|
| 灰 | `#4a5058` | **不知道** —— 没连接 / 轴上没遥测 / 丢过帧 / `60FDh` 不在映射里 |
| 灭 | `#20242b` 空心 | 这件事**没发生** |
| 绿亮 | `#33d17a` | 使能:带电 / 原点开关:正压着 |
| 红亮 | `#ff4d4d` | 出事了:故障 / 撞限位 / 正负限位开关压着 |

正常运行时只亮一盏(使能)。

- 四种样子**都画满**,不搞"只在出问题时才显示"。
- 灯和字**装在一个小容器里**(`lampUnit`),免得布局把它们排散。**现在只有状态栏那两盏还
  带字**(`○ 限位X 正常`);参数栏「轴信号」那块表**每格只有灯,没有字**(2026-09-21)。
- 状态栏那一格是**灯、字、字色一起变**的(`refreshAxisSignals` 最后一节),少写一样就会
  出现"绿灯配红字"。参数栏那边没有字可配,`setSignalCell` 只管一盏灯,所以它只记
  `LampGrid::lampLast` 一样东西。

### 参数栏最上面那块:「轴信号」(五个灯 + 故障复位)

```
        使能   故障   原点   正限位  负限位
 轴X     ●      ○      ●      ○      ○    [        ]
 轴Y     ○      ●      ○      ○      ○    [故障复位]
```

**每格只有一盏灯**(2026-09-21,用户要求"灯旁边不要有当前状态的文字说明"): 状态全由亮暗
说 —— 绿亮 / 红亮 / 灭 / 灰四种样子,含义见上面那张表。原先每盏灯旁还有一行字
(`● 已使能` / `○ 无故障` …),那是同一件事说两遍,而且五列并排时那行字占的宽度把灯挤小、
整块表看着像一张表而不是五个信号。**去掉字之后,一列的信息量全压在灯和 tooltip 上**,
所以每一格的 tooltip kTip 里都要自己说清"这一列亮/灭各是什么意思"(表头只有个名字,
而"亮"在五列里不是同一件事),再拼上那句 kLampRule。灰 = 不知道这件事也因此**只剩灯这一种
表达**,灯本身必须一眼能分出"灭"和"灰"。

**原先分成「轴信号」与「限位开关」两块,2026-09-20 合成一块: 一行一根轴, 复位在两行最右侧**
(用户要求)。合成时权衡的是这两件事:

- **宽度**。五列"灯 + 字"加复位那格**实测 ≈413px**(原先那块四列的是 ≈280px;
  参数栏当时按上一块的 394px 定宽)。参数栏的最小宽度因此从 ≈394 涨到 ≈413 ——
  画布少 20px, 默认 1360 宽的窗口下不影响用。这是这个版面唯一的代价。
  当年"4 个信号列是上限"那个估算(≈59px/列)偏保守, 实测每列 ≈50px。
  **2026-09-21 去掉每格那行字之后,这块表窄了一大截**: 列宽改由表头那两个汉字定
  (≈36px/列),五列加复位按钮那格远不到 413px 了。**参数栏的最小宽度没有重新实测** ——
  它取的是 `side`(340,写死)与各块框里最宽那个的较大值, 现在这一块已经不是最宽的了
  (输入框那几行才是), 所以这次改动不该让参数栏变窄或变宽。
- **语义**。它们回答的是上面那张表里**两个不同的问题**, 并排放在一行里就会看起来像
  同一个问题的两个答案 —— 而它们可以不一致。这一点靠两处保留: 表头上前两个名字(6041h)
  与后三个名字(60FDh)是分开的, 每一格的 tooltip 里写着自己问的是哪一个字、哪一位;
  判据在 `refreshAxisSignals()` 里也照旧**分两次算**, 两组连 `known` 的条件都不同。
  **合成的是版面, 不是数据。**

**「故障复位」跨两根轴, 在两行最右边那一格** —— 它是"两行各一个、按轴复位"的反面:
命令(`postFaultReset`)是全总线的, 只对 `6041h` bit3 置起的那几根动手, 所以按钮不带轴参数。

| 信号 | 位 | 亮起来的意思 |
|---|---|---|
| 使能 | `6041h` bit2 | 电机带电 —— **这个轴现在能不能走** |
| 故障 | `6041h` bit3 | 驱动器报故障(扫描中会自动中止) |
| 原点 | `60FDh` bit2 | 原点开关正压着(绿亮,**不会中止扫描**) |
| 正限位 | `60FDh` bit1 | 正限位开关正压着(红亮) |
| 负限位 | `60FDh` bit0 | 负限位开关正压着(红亮) |

- 信号名写在**表头**上,不是每格重复一遍;**每格只有一盏灯,连状态的字也没有**
  (2026-09-21 之前每格是 `lampUnit(灯, 字)`, "已使能/未使能"那样)。想知道某盏灯
  亮/灭/灰各指什么,鼠标停在灯上 —— kTip + kLampRule 拼出来的那句。
- 这一块排在参数栏**最上面**(它是最容易被滚动区裁掉的那一批)。
- **判据跟状态机完全一致**(`ScanController` 自动中止那一段用的就是 `!valid || !mirror_ok`)。
  判得比状态机宽的话会出现最坏的那种不一致:**灯说"正常",而程序已经因为"丢了过程数据帧"
  中止了** —— 而且此时 `sw` 是陈值。
- 限位在参数栏和状态栏**两处**都有,两处都由 `refreshAxisSignals()` 一次算出。
  **撞限位的判定本身只有一处**:`ecatcmd::limit_hit()`(定义在 `hmi/ecatworker.h`),
  在 `EcatThread::publish()` 里算一次,发成 `AxisTelem::limit_active`,界面只读不算。
- 只在**真变了**才动控件(`LampGrid::lampLast`, 2026-09-21 之前还有 `textLast`),
  30Hz 每帧给一堆控件重设样式表会把重绘刷爆(横幅那个上升沿判断是同一个理由)。

红色**不复用采失败那一格的 `C_FAIL`** —— 那个红的意思是"这一格没采到数"(数据问题),
这个是机械压在开关上(要立刻去处理)。
`bit11` 只说"有轴压着",不说方向;方向要看 `2310h`~`2312h`
(实测 **X0 = 原点 / X1 = 正限位 / X2 = 负限位**)。

#### 原点用绿灯

- 压到原点是**回零时的正常动作** —— 它是位置信息,不是故障。
- 原点用绿(亮 = 「原点开关正被压着」正在发生),不改画法。
- **代价**:三个格子用同一组词(压住 / 松开),于是**颜色是唯一说"这个会撞"的东西** ——
  所以原点的 tooltip 会说出来:「绿亮 = 正压着,这是位置信息不是故障 ——
  扫描经过原点开关不会中止」。

#### 原点那一位进不了判据

`ecatcmd::limit_hit(sw, dig_known, dig_pos, dig_neg)` 的参数里**根本没有原点那一位**。
`scan_selftest` 里有一条回归护栏:只把原点位压住,扫描必须一路跑到
`Done`,`autoAborted` 一次都不能响。

### `60FDh`:读不到是常态

`60FDh` bit2/1/0 = 原点 / 正限位 / 负限位,是 `2300h` 电平反转与 `2310h`~`2312h`
功能映射**之后**的结果 —— 调用方不再取反(反过来说:`2300h` 配反了三个灯一起反相)。

**本机实测生效的 `1A00h` 只有 `6041h`/`6064h`/`606Ch`
三项 10 字节**(`docs/ykd2205pe_ci402.md`),没有 `60FDh`。这时三个灯是**灰 + `--`**,
而那**不是**"三个都没压住" —— `AxisTelem::dig_known` 是单独一个字段,就是为了让
这两件事分得开:读不到 60FDh 时那三位也是 0,而 0 在"开关压着没有"这个问题上是个
**看起来完全正常**的答案。

两条出路,优先走第一条:

1. **用厂家上位机改一次驱动器的 `1A00h`** —— 零代码,而且断电也在;
2. 勾上「高级选项」框里那个「让 60FDh 进 TxPDO」,让主站在连接时补一次:
   仅写 RAM,收尾(断开 / 关窗)时还原,崩在收尾之前那一次改写会留在驱动器里直到断电
   —— 与当年那个 CLI 的 `motor_test --allow-pdo` 同一风险等级。过程数据从 10 字节变 14 字节。
   **这一项默认开着**(`scan.ini` 的 `adv/want_dig_in`)。

它是**连接期**参数:**改了不重连不生效**。连接期间照样能改(这一个勾不按连接态变灰),
但界面上会说清"下次「连接」才生效"。
`em_setup` 会打一行 `printf` 如实报告绑上了没有。

### 色标:冷到暖的彩虹色带,OKLab 插值

冷到暖的 13 级(`#0b2a5c` → `#e42b12`):**蓝 → 青 → 绿 → 黄 → 红,值越大越红**。
取色是**在 OKLab 里插值**的,**不是 sRGB** —— sRGB 插值在中段会发灰、发脏。

> **这是从"单色相顺序色标"换过来的,当时那条判据不再成立,是有意换的。**
> 原来那版是蓝色的 13 级(`#0d366b` → `#cde2fb`),过的是"明度严格单调、色相跨度 4°"
> 那一关。彩虹色带**明度不单调** —— 黄色最亮,红色反而比黄色暗。
> 换来的是"冷热一眼可读",代价由数字兜住(见下)。

最冷那一端(近零)压得很暗,于是它跟底色对比度低(**1.3:1**,原来那版是 1.5:1),
**必须有数字兜底** —— 右侧色标条上写着锁定的最小/最大值,鼠标移到格子上也出数。
**热力图永远不能只有颜色。**

色标条**跟画图区一样高**(上下各让 8px 给两端的数字):图多高条子就多长,一眼能跟格子对上。
原来封在 240px,窗口一拉大条子就比图矮一截,看着像另外半张图的东西。

> **色阶是锁定的,不跟着数据实时变。** 边采边自动缩放的话,
> 每来一个点整张图的颜色都会重排一遍。所以是两个数字(默认 0..1)加一个「按数据定标」
> 按钮,要改是**显式**改一次。超出范围的格子夹到两端,数值仍可在悬停里读到。

**「自动跟随数据」那个勾是上面这条规则的一个出口**(2026-09-21 加)。勾上之后色阶 =
**已采数据的最小/最大**,由 `MapCanvas::applyAutoFit()` 在 `paintEvent` 里每帧跟一次,
两个输入框变成只读的读数(值由程序写,打字进去下一拍就被覆盖),「按数据定标」跟着变灰 ——
它做的事每帧都在做了。

- 数据没有任何一个点时**保持现有色阶不动**(`wattsRange()` 返回 false),不除零;
- 只有一个点(或所有点都一样)时按 ±5% 加宽成一个窗口,免得整片一个颜色;
- 值没变时**不调 `setShadeRange()`** —— 那里面是 `update()`,而 `applyAutoFit` 是在
  `paintEvent` 里调的,每帧调一次就成了画布自己给自己排重画,一直空转;
- 它是**模式**,所以记进 `scan.ini`(`shade/auto_fit`,缺省**关**)—— 两个上下限是数值,
  仍然不记(见 §16.1)。勾一次就记住了,不必每次开程序重来;
- **代价就是上面那段警告**:新采到一个更极端的值,整张图重排一次颜色,"变化"里有一部分是
  色阶自己在动。所以要拿两张图对比时把它关掉 —— 关掉那一刻色阶就定在当场那个范围上。

**「最小」「最大」是一对,任何时候都得 `0 <= 最小 < 最大`**(色阶是拿这两个数做线性映射的,
反着的区间除零 —— `shadeOf()` 里有一条兜底走中间色,但那只是不崩,画出来是错的)。两条规矩
各有一处落点,而且**必须一致**:手动填的走控件自己的 `setRange(0, …)`(敲 -5 会被夹成 0),
按数据定标 / 自动跟随走 `MapCanvas::dataShadeRange()` 里的同一个 0 下限 —— 只管一头的话,
自动跟随开着时框里显示 0、画图用的是负数,界面上那个量程就是假的。

顶头了怎么办:**推着走**。改一个越过另一个,就把另一个一起推过去,**跨度不变**
(`onShadeLoChanged` / `onShadeHiChanged`,跨度的来源是画布上还没被覆盖的那一对值):

- 最小 0 / 最大 1,把最小改成 5 → 最大自己抬到 6(想把整段量程往上挪时最顺手);
- 上限往下压也一样,但**下限顶到 0 就到底**:再压下去只能掉头把上限抬回来,色阶可以很窄,
  不能反过来;
- 推的那一下**屏蔽信号**:不屏蔽的话被推的框会再发一次 `valueChanged`,它拿到的是半新半旧的
  一对值,中间会闪一次错的量程。

> 另一条路是"夹住改的那一头"(不让最小超过最大)。没选它:想把量程整体挪一格得先改另一个,
> 绕一圈,而且敲进去的数会被静默改掉,看着像吞键。推着走至少**改动都看得见**,还配一条横幅
> ("最小顶到最大上了 —— 最大跟着抬到 6 (跨度不变)")。
>
> 这两条规矩**没有自测覆盖**:`scan_selftest` 只链 `Qt6::Core`,这套逻辑在 `ScanWindow`
> 里(要 Widgets 与整扇窗口)。能测的只有"自动跟随这个模式记得住"那一条(§16.1)。

色标条本身(2026-09-21 一并改的): **高度封顶 320px**(原来 240,窗口拉大时它比图矮一截;
改成跟画图区等高又太长),**两端 + 中间整数刻度都写数字**。中间那几个刻度按 1/2/5×10^k
挑步长(`niceStep`,目标 5 段,于是 0..1 出 0.2/0.4/0.6/0.8 四个),写淡一档、带一个小刻度线;
两端是锁定色阶那两个数,写亮一档。挑不出整数刻度(量程特别偏/特别窄)就退回标中点,
条子矮于 140px 也不补 —— 数字会挤成一团。

**三种状态在画面上分得清清楚楚**:

| 状态 | 画法 |
|---|---|
| 还没扫到 | 只有底色的空位 |
| 扫到了、读数失败 | **紫红实心块**(`#c026d3`,常量 `C_NODATA`)—— 那才是要人去处理的一格 |
| 扫到了、有数 | 冷到暖的顺序色标 |

> **失败格为什么是紫红,不是红:** 色标最高档就是红的,失败格再来一块纯红就分不出
> "这一格没采到数"和"这一格功率最大"了。紫红在整个色带之外,一眼能分。
> 注意别跟 `C_FAIL` 混了 —— 那个红还在用,管的是**超量程的框**和框上那行红字。

> **实测位置那个点刻意是白的,不是 hmi 的蓝色。** 因为这块画布上蓝色已经是**数据**了
> (彩虹色带的冷端就是蓝)。白色在所有色阶上都最跳。
> hmi 那边画布上没有数据,所以它用蓝色。

### 刷新与缓存

热力图是一张 `nx×ny` 的 `QImage`(**一格一像素**),放大时用最近邻
(`SmoothPixmapTransform = false`)。3025 个像素比一次 `QPainter` 填充还便宜,
所以每帧重建;只有扫描路径是跟几何走的,才做几何变动的缓存 —— 而它缓存的是**单位**,
所以"窗口缩放了"这种几何变化不需要把缓存作废(见 §9)。

---

## 10. 安全护栏

| 场景 | 行为 |
|---|---|
| 程序启动 | **一个字节都不写总线**(只会 `postRange` 一个本地成员) |
| 「连接」 | `em_open` → `em_setup(...,1)` → `em_enter_op`。进 OP 开始发帧但**不发使能**;**不弹确认框** —— 它写什么由按钮 tooltip 一直说着 |
| 「使能」 | **让电机带电的两个按钮之一**(另一个是「回零」)。**不弹确认框、没有持久勾选、不自动使能**;"现在带不带电"由这颗按钮自己说:**已经使能时它是灰的、写着「已使能」**(判据是**每一根轴都已使能**;还有一根没使能就仍可按,按下去使能剩下的那些) |
| 「故障复位」 | **不弹确认框**(按下去就发)。**只对 6041h bit3 置起的轴写字节** —— 一根都没报故障时**一个字节都不写**,而这句话用横幅说出来("看起来没有轴报故障…这一下大概率一个字节都不会写");扫描进行中直接拒绝(先「中止」);复位成功后该轴停在**未使能**,要接着走请重新点「使能」;每轴最多 1 秒,期间**不可中断**、按钮显示"正在复位…" |
| 「X/Y 正/反向回零」及「X/Y 找正/负限位」 | 驱动器自带 HM 模式(**回零 = 方式 24 / 29**; **找限位 = 方式 18 / 17**, 手册 V2.4 p46~p48 叫"以限位为原点")。**不弹确认框**:事前的说明都在按钮 tooltip 上(**「驱动器会自己带电并移动,软件拦不住它撞开关」**、**该轴会先失能(竖直轴此时失去保持力矩)**、方向只说电机轴正反、方向不对就换按钮),框里**只有速度值、超时值与八个按钮**(八个按钮**共用这两个值**;由速度派生的找段 / 返回段速度、加减速、**最多走几圈**只在**控制台**的回零收尾句里;**超时 2026-09-22 起也是框里的一个值**,见 §27),出路是「停止」= 立即中止。**发起之前先过一道闸**(没连上 / 位置未知 / 丢帧 / bit3 故障 / 还有轴在走 → 拒绝且**一个字节都不写**);找限位在这道闸之后**再过第二道**(60FDh 读不到 / 正负限位同时有效 → 拒绝且**一个字节都不写**,并预告这一趟走 a) 还是 b) 分支);只对**被点的那一根**判据,不看另一根、**也不看它使能没使能**;回零期间「使能 / 失能 / 停止点 / 设为区域中心 / 开始扫描」全部锁住(「停止」除外,那时它兼任中止);回零结束后**自动切回 CSP 并保持使能**,并**从 `6064h` 重新锚定显示零点** |
| 找限位期间碰到限位 | **不弹红色「撞限位」横幅**(找限位就是要去撞它,那是这一趟的目的);但**限位灯与状态栏那格照常变红**、stdout 那行证据照打 —— 它们说的是"信号此刻有效",是事实。找完之后压着限位再启扫,那条红横幅与自动中止**照常发生** |
| 「停止」**在回零进行中** | **立即中止**(≤2ms 被 `em_home()` 的轮询看见),**并且保持使能** —— 中途位置不明,卸力会让竖直轴自由下滑。走的是 GUI 直呼 `em_request_stop()`(见 §2 那条例外),**不是**命令队列 |
| 「开始扫描」 | 过 Preflight;**不弹确认框** —— 这一趟的范围在按之前就全在眼前(参数栏里填的数 + 扫描栏那两行**网格 / 点数 / ±范围 / 预计全程** + 参数不合法时的红字)。按下去之后横幅把**出发的那一份**(区域 / 点数 / 预计时长 / 取样源 / 写到哪) 念一遍,并**不自动消失** |
| 暂停 / 中止 | 目标冻在当前位置,**保持保持力矩**(故意不写 `6040h=0x0000`) |
| 扫描中 WKC 掉 / bit3 故障 / bit11 撞限位 / 掉出 OP | **自动中止** + 红色横幅 + 模态 |
| 扫描中改参数 / 换取样源 | 控件**全部锁住**。几何改到一半的扫描没有意义 —— 落进 CSV 的 `(ix,iy)` 和实际位置就对不上了 |
| 关窗 / 断开 | **先中止扫描** → 失能 → 还原 PDO 映射 → 降 `PRE_OP` → 关网卡。扫描在跑时**也不再问一句** —— 关窗就是中止,已经采到的点都在 CSV 里 |
| 收尾未能确认失能 | **模态告警**,要求立即断驱动器动力电源(CLI 退出码 10) |
| 全程不写 | `2102h`(EEPROM)、`607Dh` 软限位、`2400h/2408h/2409h/2201h` |

> **收尾顺序不能反。** `disconnectAndStop()` 是**先中止扫描再断总线** ——
> 反过来的话状态机会看到"掉出 OP",然后作为一个**异常**去自动中止,
> 一条本来正常的收尾路径会变成红色告警。

> **启动时不弹任何对话框,动作前的确认框也一律没有。**
> "会发生什么"改由三样东西在**按之前**说清: **按钮自己的文字与 tooltip**
> (「使能」在已带电时就是灰的「已使能」)、参数栏里实时算出来的那些数、
> 扫描栏那两行(网格 / 点数 / 预计全程)。
>
> **留下来的模态只有三个,而且全是"出事后"的**: 扫描自动中止、收尾未能确认失能
> (可能仍带电)、收尾超时。它们不是确认框 —— 没有"取消"可点。

> **这一条只对 `scan` 成立。** `hmi/mainwindow`(以及 CLI 的 `--allow-*`)那边
> 确认框照旧, 一条没动 —— **上表里那几行与 `hmi` 不再逐条对应**。
> 共享的东西只有 `hmi/ecatworker`+`motor_api` 那层闸
> (哪些写能发、哪根轴该复位、回零的四条拒绝), 它们**一行没改**。

> **「故障复位」自己带一道闸。**
> `em_fault_reset()` 的动作顺序是**先写 `6040h = 0x0000`(Disable voltage = 卸力)
> 打十帧,再抬 bit7** —— bit7 是上升沿触发,不先把 0 压下去就构不成沿,这个顺序本身是对的。
> 问题在于它**到函数末尾**才报告"本来就没有故障":那时候十帧卸力已经打出去了。
> 对一根**健康的保持轴**做这件事,那一轴会**真的卸力,竖直滑台会掉下来**。
> 所以判据 (`ecatcmd::axis_needs_reset`) 必须在**调用之前**,
> 一根轴都没报故障时**一个字节都不写**。
> 状态未知的轴(`mirror_ok = false`)**跳过、不猜**。
> 复位的对象由 `6041h` bit3 决定,**不由操作员点了哪个按钮决定**:所以按钮不带轴参数,
> 一次复位所有报故障的轴。

---

## 11. 构建

`scan` 与 `hmi` 用**同一个 preset**(`hmi-qt-ucrt64`),**不改 preset、不加第二条
configure 命令**:

```cmake
option(EC_BUILD_SCAN "构建扫描采集程序 (需要 Qt6 + C++ 编译器)" ${EC_BUILD_HMI})
if(EC_BUILD_SCAN)
  add_subdirectory(scan)
endif()
```

`${EC_BUILD_HMI}` 作默认值 ⇒ 现有的 `cmake --preset hmi-qt-ucrt64` 一次把两个都编出来;
而只想编 hmi 的人可以 `-DEC_BUILD_SCAN=OFF`。

```bash
cmake --preset hmi-qt-ucrt64
cmake --build out/build/hmi-qt-ucrt64            # 全部
cmake --build out/build/hmi-qt-ucrt64 --target scan
```

产物:`bin/scan.exe`、`bin/scan_selftest.exe`。

`scan_selftest` **不链 SOEM 也不链 Widgets**(只用到 `Qt6::Core`)——
它几秒钟就能跑完,不需要设备、不需要网卡、不需要界面:

```bash
PATH="/c/msys64/ucrt64/bin:$PATH" ./bin/scan_selftest.exe
# ... 597 passed, 0 failed, 0 skipped
```

不带参数(它只把 `argv` 交给 `QCoreApplication`),退出码 = 有没有失败(0/1)。
组名与断言说明是中文,按 UTF-8 打在控制台上 —— 看到乱码先怀疑代码页与字体,
那是终端的事,不是它输出错了。

> **`scan_selftest` 被钉在 `-O1`。**
> 本目标在 `-O2`/`-O3` 下链接必然失败 —— `collect2: ld returned 5 exit status`
> (也见过 1/9/116),`ld` 自己的报错一个字都没有,输出文件被建出来但内容是**一片零**。
> `bin/scan.exe` 与 `bin/hmi.exe` 在同样的 `-O3` 下链接正常。
>
> 症状**只跟优化级别有关**,与代码写法无关:`-O0`/`-O1` 成功,`-O2`/`-O3` 失败;
> 单独 `ld -r` 同一组目标文件是成功的,只有完整链接才炸;
> `-ffunction-sections`、`-fno-asynchronous-unwind-tables`、`-fno-ipa-icf` 都试过,没用。
> 所以这是 **GCC 16.1.0 (MSYS2 UCRT64) 这一套 `ld` 自己的问题**,不是本仓库代码的问题。
> 出厂的 `bin/scan.exe` 保持 `-O3` 不变。**GCC/binutils 一升级就可以把那行删掉**,
> 然后重跑一遍自检确认。

---

## 12. 本次改到的东西

- **`hmi/ecatworker.h` / `.cpp` 只加不改**(纯增量,`hmi.exe` 行为逐字节不变):
  `m_range` 成员(默认 `HMI_RANGE`)、`postRange()`、`doRange()`、`CMD_RANGE`、
  `BusTelem::range`。`HMI_RANGE` 这个宏**保留原样**,只是从"唯一的量程"变成"缺省量程"。
- `scan/` 全部新增。
- 顶层 `CMakeLists.txt` 加 `option(EC_BUILD_SCAN ...)` + `add_subdirectory(scan)`。
- `.gitignore` 加 `/scan_out/`。

---

## 13. 验收状态

### 已验证(可复现,不需要设备)

- 构建 **0 warning 0 error**;三个产物都是有效 PE:
  `bin/scan.exe` (755105 B)、`bin/scan_selftest.exe` (357751 B)、`bin/hmi.exe` (485981 B)。
  (字节数是**当次构建的读数,不是契约** —— 换个工具链版本就变。)
- **`scan_selftest` 597 条断言全过**(`597 passed, 0 failed, 0 skipped`),覆盖:
  (这条数会随每一轮改动往上走 —— §1 那个数、这里、以及各节末尾"验了多少条"都指同一份东西,
  改了记得三处一起改。表里最后几组是后加的,所以老节里出现的 237 / 542 是当时的读数。)

  | 组 | 内容 |
  |---|---|
  | grid | 27×27 @ 0.5 → 55×55 = 3025 点、坐标 −13.5..+13.5、蛇形顺序、自环步数 = 0、`autoRangePul = 800000`(2026-09-22 前是 725000,见 §28)、`posTolPul = 1250` |
  | csv | meta 往返、几何一变就拒绝并**指名道姓**、数值读回、**重测取最后一行**、真损坏要报出来 |
  | arrive | 到位判据、窗口被打破要重新计时、该报错就报错、**位置没到但 `at_target` 已为真**(`doStop` 的陷阱) |
  | run | 一整轮扫完、暂停冻在原地且继续会重走当前点、中止不写半点、**续扫只补没采过的点** |
  | abort | 撞限位 / 驱动器故障 / 掉使能 / 拔网线(WKC) / 旁路改目标 / 走不到 —— 六条自动中止全过,外加"到位判据要的是实测位置" |
  | retest | 单点重测追加一行,且**不改动别的格子** |
  | preflight | 缺一条都不许动、量程装不下区域就拒绝 |
  | faultreset | `axis_needs_reset` 真值表(6 条)、`pick_faulted_axes` 在 9 种输入下挑得对不对(**含"状态未知的轴跳过,不猜"**) |
  | limitsw | bit11 判定、精判据两条分支、现场诊断那句话不把"不知道"说成"都没压着"、**只压原点照样跑完一轮**、`60FDh` 未知时 bit11 照样中止、中止文案点名压住的开关 |
  | home | 方向 → 方式号(24/29)、`home_vel_slow` / `home_vel_clamp` / `home_accel_for` 的**全域不变式**(返回速度恒 ≥1 且 ≤ 找段速度、斜坡恒为 0.1 秒、加速度封顶不回绕)、`home_refusal` 五条分支与**分支的顺序**(丢帧时不许拿一个没收到过的状态字里的 bit3 说事)、`home_end_state` 由**实测状态**定而不是返回码排列组合、三种结局的措辞、`6061h` 说人话(「还没读过」与「读失败」必须是两句不同的话) |
  | homelim | 18/17 的方式号与**恰好四个**的白名单、首段方向的**真值表**(17 与 18 反、同一个方式的 a) 与 b) 又各反一次、24/29 不随"压着没有"变)、目标/另一侧各自对应 60FDh 的哪一位、两道否决的文案(都含「拒绝」、都点名要查什么、都不是同一句话)、四格分支预告**两两不同**且方向与 `home_method_first_dir` 同源 (见 §20) |
  | plancap | 上限 200000 点:超了**一个点都不建**且「开始扫描」拒绝(理由是"点数")、正好 200000(400×500)必须建得出来、退回去必须能恢复(见 §16) |
  | prefs | 文件不存在时给缺省(第一次运行与读了个半截的 ini 是同一条路)、存进去读回来**逐项相等**(含 `\Device\NPF_{GUID}` 的反斜杠)、不能扫的参数**盖不掉**上一次能用的那份(见 §16) |
  | advprefs | 旧 ini(只有 `area_x_unit`/`speed_pul_s`/`manual_speed`)**必须读成开/开/关** —— 显式带缺省那条路;四项全反着存一遍再读回来逐项相等;`home_vel_from_pref` 的 `-1`/`0`/正常/过小/过大五档(见 §19) |
  | editgate | 不在编辑态不留标记、退出编辑态后 `markDirty` 是 no-op、`begin` 第二次返回 false、`undirty` 清标记但不退出编辑态且非编辑态不动手、`save`/`cancel` 三个标志一起清、`drop` 只在**真有改动**时返回 true 且「已丢弃」留到下次 `begin`、双反相警告的真值表 + 措辞钉死 (见 §19) |
  | faultcode | `603Fh` 那七条码逐条、十六进制照实写、手册之外说「手册之外」、每个码都有一句处置办法(手册之外的不给)、两个哨兵是负数且与 `0x0000` 三者互不相同、拼成 `"轴X 603Fh = ……"` 后 `603Fh` 只出现一次、横幅不点名没故障的轴且两根都故障时各自的码都在 (见 §22) |
  | meter | 手动源一次请求一个数、随机源在 基值±噪声 内、脚本源按行取且游标对得上、**两个未决请求撞在一起**时闸门必须由调用方把 |
  | ophir | 没插设备时干净报错、不挂死(要 COM,插了表头之后的实时读数不在这里) |

- **`scan_selftest` 抓出了三个真缺陷**,都在写完之后、有硬件之前:
  1. `beginAppend()` 把新数据行**粘在半行后面**(见 §8)
  2. `m_want_ok` 确认门可能**永远开不了**,扫描会跟着别人的目标走(见 §6)
  3. **暂停会被自己判成外部干预** —— "暂停按钮变成了中止按钮"(见 §6)
- `grep -rn "soem.h\|ecx_" scan/` **只命中注释**(3 处,全是说明文字),
  界面侧一处 include、一处调用都没有 —— 与 hmi 同一条不变量。
- `bin/scan.exe` 起来后**窗口正常、中文不乱码**;启动时打印
  `量程已从 ±500000 改为 ±725000 脉冲`(即 §3 那个算式),列出 10 块网卡,8 秒无响应异常。
  > 括注里的 725000 是那次实跑的数,现在是 800000(§28);状态栏那句话的形状没变,只有数变了。
- **`bin/hmi.exe` 回归通过**:窗口正常,控制台输出与改动前一致
  (**没有**那句"量程已从…",因为它从不调 `postRange`)。
- 全部改动文件的 `git diff --stat` == `git diff --ignore-cr-at-eol --stat`(无 EOL 噪声);
  仓库**没有 `.gitattributes`,`core.autocrlf=false`**。索引里全是 `i/lf`,而工作区**两种都有**
  —— `scan/scanwindow.cpp`、`motor_api/ec_motor.c` 这些是 `w/crlf`,
  `scan/selftest.cpp`、`hmi/ecatworker.*`、三个 `.md` 是 `w/lf`。
  **所以:改哪个文件就照哪个文件自己的行尾**(用 Python 重写整份时必须显式指定 `newline`,
  否则会给一个 `w/crlf` 的文件灌进 LF,反之亦然),改完拿上面那对 `--stat` 比一下,
  数字不一样就是灌错了。
- **本次三件事都验过**(用临时探针 + 自检,不需要设备):
  - 参数记忆:第一次跑写 `bin/scan.ini` → 第二次跑起来 `area_x = 12.5`(不是缺省的 27);
    把 ini 里的网卡改成**第 10 块**(`\Device\NPF_{809345E5-…}`,Intel I219-V)→
    起来后下拉框停在 **index 9** 而不是 index 0;
    再把那个 GUID 改成一个不存在的 → **退回 index 0,不崩**(状态栏给一句提示)。
  - 滚轮:**照 Qt 真实投递路径**滚过全部 12 个输入框(先投给 `childAt()` 命中的那个,
    再沿父链走),每个框三列:没人点过 / 点过之后 / 翻过页之后。结论是
    **没人点却被改 0/12、点过之后能改 9/12(另 3 个是 Qt 自己不动的下拉框)、
    翻页后又被改 0/12**。原因是真实投递先落在内部那个 `QLineEdit` 上,
    见 §16.2、§16.5。
  - 上限:自检里 400×500 建得出来、400×501 拒绝。

### 尚未验证(需要人在设备旁、手放在物理急停上)

- **整条真机链路一次都没跑过。** [hmi_click_position.md](hmi_click_position.md) 的
  「尚未验证」一节写明:**hmi 的「连接」这条路一次都没在真机上走过**。
  scan 共用 `ecatworker.cpp`,所以"连接 → 使能 → 走"这一段的上游同样只有静态检查。
  **上面那些"已验证"全部是用 `FakeBus` + `FakeMeter` 得到的**。
- **默认区域 27×27 mm = ±13.5 圈 = ±675000 脉冲,这个行程是算出来的,不是量出来的。**
  hmi 那边的 ±500000 是一个**选定的软量程**,不是实测的机械行程。
  机器上到底有没有 ±13.5 圈,**没人验过**。硬件限位是真实存在的
  (`2311h`/`2312h` 实测 = 2/3,X1 = 正限位 / X2 = 负限位,见
  [ykd2205pe_ci402.md](ykd2205pe_ci402.md)),区域算错就是一头撞上去。
  **第一次先跑一行**:把区域改成 `27 × 0.5`(分辨率 0.5)= **55 点**、
  约 2 分钟,看方向和两端到不到位,再决定要不要放开整片。
- **`2201h` 的实**值**从没被记下来过**,而它决定"1 mm = 50000 脉冲"这个前提成不成立:
  默认 0 = 细分有效(当量来自 `2400h`,实测 50000),1 = 电子齿轮比有效
  (当量来自 `2408h`/`2409h`,实测 1000/4000 —— **差 50 倍**)。
  扫描的区域、分辨率、速度全都建在这个当量上。
  对象本身**是读得到的** ([ec_motor.h](../motor_api/ec_motor.h#L147) 定义了
  `EM_OID_GEAR_ENABLE`),但**现在没有程序会去读它** —— `motor_api/ec_motor.h` 里那个宏
  **只剩定义、一个调用方都没有**。
  原先有两道防线兜着它:**校验矩阵**(`sm_baseline`/`slide_verify` 都不比这个对象,
  所以 `--baseline` 覆盖不到它)以及 `sm_spec_table[]`(`2201h`+`2400h` 与
  `2408h`/`2409h` 同为一组,四项都是 `dangerous` + `SM_CMP_EXACT`,漂移即拒绝 S3/S4)。
  **两道防线连同那份基线文件都在 2026-09-21 一起没了**,所以现在**没有任何东西会挡住
  一个错的当量**。出路只有两条:用厂家上位机看一眼 `2201h`,或者自己临时加一行
  `em_rd_u16(bus, slave, EM_OID_GEAR_ENABLE, 0, &v)`。**弄清它之前别按缺省区域扫。**
- 暂停 / 继续 / 中止各按一次,确认冻住位置、保持力矩、继续时重新走完当前点。
- 扫描中拔网线 —— 确认**自动中止 + 红色横幅**。
- 扫描中按物理急停 —— 这是唯一的真急停,软件只负责冻住目标。
- **真机报一次故障,确认 603Fh 读得到**(§22):红横幅上那句码要与驱动器面板 ALM 灯的
  红闪次数对得上(手册:1 过流 / 2 过压 / 3 欠压 / 6 通讯 / 8 传感器),**对不上就先别信
  这条 SDO 读**。顺手验的是"故障期间驱动器的邮箱通道还答不答 SDO"——
  不答的话第一拍之后会停在 `603Fh 读不到 (SDO 没应答)`,而那是 §22.5 里写明要对赌的那一条。
  刻意造一个故障(拔动力线 / 断编码器)成本很低,比等它自己坏划算。
- 关窗收尾,确认失能 → 还原映射 → 降 `PRE_OP` 的顺序,以及"可能仍带电"的模态告警。
- 真功率计 (PD300R + Juno+):**接口与自检这一侧验过了**(COM 对象注册、绕开注册表的
  typelib 派发、没插设备时干净报错),**插上表头之后的实时读数还没验过** —— 见 §17.4。
- 界面开着时 Npcap 被它独占,**同时不要再跑 CLI**。

#### 只有真机才能定的六件事

**代码能保证、也必须保证的**三条:`dig_known == false` 时**永不**渲染开关状态(灰 + `--`);
`kRefineLimitWithDigIn == 0` 时 `60FDh` **永不**参与中止判定;
撞限位只有 `ecatcmd::limit_hit()` 一处定义。**下面这六条是代码答不了的**:

1. **`60FDh` 在不在生效的 `1A00h` 里?** 新加的那行 `em_setup` 输出一次运行就答了。
2. **只有原点开关压住时,bit11 会不会置起?**(电机释放、不发指令,同时读 `6041h` 与 `60FDh`。)
   若会,扫描经过原点就会误中止。
3. bit11 是不是**方向门控**的(只在朝开关发指令时置起)。
4. `2310h`/`2311h`/`2312h` 是否仍是 1/2/3(这三个是输入端子功能码,决定三个开关各认
   什么信号;配错就等于换了一整套判据)。
5. `2300h`(输入有效电平逻辑)与接线是否一致 —— **配反了三个灯全反**。
   原先那份参数基线把它记为 WARN 级正是这个原因(基线 2026-09-21 已删,这条结论留下)。
6. 扫描中压限位会不会中止,以及**新灯与 bit11 在现场是否自洽**。

第 2 条那条诊断不用另写工具:`refreshAxisSignals()` 在 bit11 的**上升沿**会把当时的
`sw` 与 `60FDh` 三位一起 `printf` 到 stdout —— 30Hz 的面板灯可能一闪而过,stdout 会把
证据留下。

#### 第 7 条:bit11 的另一半可能

上面六条问的都是"bit11 在什么**时候**置起",漏了一个方向:**它认的是哪一路信号**。
CiA402 把 bit11 定义成 `internal limit active`,而"内部限位"到底指硬件限位开关还是
**`607Dh` 软限位**,规范没规定死。所以候选原因多了一个:

7. **`607Dh:01`/`:02` 是不是 0/0?** 若是,则任何位置都在软限位窗口之外 → bit11 可能
   **恒为 1**,而那与"有没有开关压着"完全无关。
   2026-09-21 之前,诊断**不用改代码**:`bin/motor_test.exe` 不带参数跑一次(S0~S2,
   一个字节都不写)就会打印 `607Dh:01 负向软限位` / `:02 正向软限位` / `6064h 当前位置`,
   三个数放一起看就够定这件事。**那个程序已经删了**,现在要这三个数得自己临时加三行
   `em_rd_i32(bus, slave, EM_OID_SOFTLIM, 1, &v)`(子索引 2 同理)与
   `em_rd_i32(bus, slave, EM_OID_ACT_POS, 0, &v)` —— 三个都是 4 字节,`em_rd_i32` 正好。
   别的工具都没有替代品 —— `em_setup` 那些输出里**没有** `607Dh`。
   横幅现在也会把人往这条上引。

> 第 2 条说"真有开关压着,只是原点",第 7 条说"**根本没有开关压着**"。
> 两者在界面上长得**一模一样**(bit11 置起),但一个开
> `kRefineLimitWithDigIn` 就能修,另一个开着反而更糟(`60FDh` 三位恒 0 → 精判据永不成立
> → 保护没了)。所以**必须先拿到证据再翻那个开关**。

#### 实测:六条里答掉了四条,成因是**极性配反**

现场症状:扫描一按就开不了,拒绝的话说"轴0 的 6041h bit11 已经置起"。
`scan_out/scan_20260918_131031.csv` 只有表头、零行数据 —— 说明卡在**前置门禁**,
不是扫描中途掉下来的。**当时**一次只读运行(`motor_test.exe` 不带参数 +
`slide_motion.exe --dump-baseline`)读到的现场值如下 —— 这两个程序 2026-09-21 已删,
**下面这张表是当时测的,保留作为记录**:

| 对象 | 实测 | 结论 |
|---|---|---|
| `6041h` | `0x0A31` | Ready to switch on(电机**没使能**),**bit11 = 1** |
| `2310h`/`2311h`/`2312h` | `1` / `2` / `3` | **正是原点/正限/负限,映射没问题** |
| `2300h` | **`0`** | 输入有效电平逻辑 = **常开**(有效 = 高电平) |
| `607Dh:01` / `:02` | `-2000000000` / `2000000000` | 软限位大开,**不是它** |
| `6064h` | `61148` | 在行程内 |
| `1A00h` 生效映射 | 含 `60FDh:00 32 bit @ +10.0 字节` | **`60FDh` 在映射里**,`dig_known` 会是 true |

对应的条目:**1 答"在"**;**4 答"仍是 1/2/3"**(映射排除);**7 答"不是 0/0"**
(软限位排除);**5 答"配反了"**。

第 5 条为什么是成因:现场接线是 **NPN 传感器(高电平 = 未触发)**,而 `2300h = 0`
(常开)的含义是"**高电平算触发**"。两者正好反着 —— 空载时 X1/X2 停在高电平上,
驱动器把"没触发"读成"触发",于是 `60FDh` 的 bit1/bit2 **同时**置起、bit11 恒为 1。
"正负限位同时压着"这个物理上不可能的组合,就是极性配反的指纹(手册 V2.4 p84 那句
"这三位是 `2300h` 反转之后的结果"反过来读)。

电机当时还在 Ready to switch on、`bit3` 故障位也没置 ——
它不是"运动撞上去的",而是从静止就恒定的。

**修法有两条,层次不同:**

1. **改驱动器的 `2300h`**:bit0~bit2 置 1(常闭)→ `0x0007`。`2300h` 是 U16 RW
   (手册 V2.4 p84),高几位原样保留;**手册没写它会不会被驱动器自己落 EEPROM**。
   这一条修的是**根**:驱动器自己的限位保护、`60FDh`、`6041h` bit11、上位机一次全对,
   而且 bit11 那道闸**保持有效**。
   **这是程序现在的默认动作**:「高级选项」里的「写驱动器 2300h = 0x0007」默认勾着,
   连接时逐轴读原值 → 写 bit0~bit2 → 回读确认,收尾写回原值(`em_di_set_logic`
   / `em_shutdown`),**从不写 `2102h`**。任一根读不到原值就整体拒绝,一个字节都不写。
2. **「上位机侧取反」**(见下):只治软件这一侧。
   **`60FDh` 与 bit11 都是驱动器给的,勾这个框不会让驱动器改变主意** ——
   它自己的限位保护仍按那套错的极性算。能改 `2300h` 就该去改它。

**两条同时上 = 双反相,不是"等于没反",是更坏**:判据 `LIMIT_RULE_INVERT` 看的是
反相后的两个限位开关,双反之后它们空载时都是 1 → 判据**恒成立** → 扫描永远开不了
(只多报,不漏报)。界面上这两个勾一起出现时有一行红字(`editgate::doubleInvertWarning`)。

#### 「上位机侧取反」—— 它必须连判据一起换

给 `2300h` 改不动(或不想动驱动器)的场合留的一条路。实现在
[hmi/ecatworker.h](../hmi/ecatworker.h) 的 `ecatcmd::LimitRule`:

| 判据 | 何时生效 | 式子 |
|---|---|---|
| `LIMIT_RULE_BIT11` | 默认 | `bit11` |
| `LIMIT_RULE_REFINED` | `kRefineLimitWithDigIn` 打开 | `bit11 && (正/负限位压着)`,未知退回 `bit11` |
| `LIMIT_RULE_INVERT` | **取反开着** | `正/负限位压着`,未知**一律中止** |

- **取反生效时 bit11 完全不参与。** 这台机器上它恒为 1 —— 它不是判据了,是个常数。
  写成独立的第三条而不是复用 `bit11 && (...)` 的形式:bit11 恒 1 时两者等价,
  而哪天 `2300h` 被改对、取反忘了关,bit11 一变 0 就会把
  `bit11 && ...` **整条恒置为 false**,保护静悄悄地全没。
- **取反必须连带换判据,不能做成两个独立开关。** "取反开着而判定仍只看 bit11"
  在这台机器上就是"永远开不了扫描"。绑在一起之后,"取反"这一个动作的语义才是完整的:
  **把限位判定从"驱动器的意见"换成"开关的真实状态"**。
- **读不到 `60FDh` 时一律中止。** 三条判据里只有这一条把"未知"判成中止,因为此时
  **退无可退**(bit11 不用了,开关又读不到)。它不会在扫描中误触发:`dig_known` 里的
  `mirror_ok` 是**闩锁**的(`em_dig_in_known` = `off_dig_in >= 0 && mirror_ok`,
  而 `mirror_ok` 只在轴初始化时清零)—— 所以这一支只可能在连接之后、第一帧完整过程
  数据到达**之前**命中。

**默认关,进了 ini**(`adv/npn_sw_invert`,见 `scan/scanprefs.h`):取反是"这台机器的
X0~X3 就是这么接的"的一条**断言**,而断言错了的后果不是"灯显示不对",是**保护反过来**
(真压着限位时它说没压着)。默认关是因为「写驱动器 `2300h`」那条正路默认开着 ——
两条一起开就是上面那个双反相。真要单独靠它,得由人在「高级选项」里点「编辑」勾上、
再点「保存」;这是**运行期**参数,勾一下下一帧就生效。

文案也跟着分了两套(`limit_switch_text` / `limit_hit_advice` / `limit_hit_headline`
都多了一个 `di_invert`):同一个"正负限位同时压着",**取反关着**是极性配反(去查
`2300h`),**取反开着**就只能是两路输入真的都被读成低电平(去查供电与接线)。

回归护栏在 `scan/selftest.cpp` 的 `limitsw` 那一组,末尾那条端到端的是重中之重:
**现场原样搬进去(bit11 置起 + 两个限位都报压着),取反关着必须拦得住、开着必须放得行。**

#### 回零(HM):只有真机才能验的六件事

**回零是软件唯一兜不住的动作** —— 发起之后朝哪走、什么时候停、撞不撞开关,全由驱动器按
`6098h` 与它自己读到的原点信号决定。所以下面这些**必须在设备旁、手放在物理急停上**做,
而在这之前**一个字节都不能写**:

0. **先修极性,而且只能修 `2300h`。**
   上面那条实测已经定了:这台机器是 NPN 传感器 + `2300h = 0`,极性配反,
   `60FDh` 的 bit1/bit2 空载就都置起。**而回零用的是驱动器自己读到的开关电平,不是上位机
   算出来的那一份** —— 所以「高级选项」里的「上位机侧取反」**对回零一点用都没有**
   (它只改上位机这一侧的中止判据)。极性不修就去回零,驱动器会认为限位恒压着。
   **能改 `2300h` 就去改它**(bit0~bit2 置 1 → `0x0007`); 这也是程序的默认动作
   (「高级选项」的「写驱动器 2300h」默认开,连接时写、收尾还原)。

1. **`em_home()` 这条调用链本身。** 从头到尾一次都没在真机上跑过。**现在全仓库唯一
   的调用方是界面**(`ecatworker.cpp` 的 `doHome()` —— 「原点模式」框那八个按钮);
   原先 CLI 那一侧 (`motor_test --home`) 已经在 2026-09-21 删掉了,所以**以后要验它
   只能靠真机界面**。
2. **收尾那五步的顺序,尤其是"先失能 → 再切 CSP → 再使能 → 再重新锚定"。**
   `em_arm()` 是在**使能那一刻**把 `607Ah` 钉到当时的 `6064h`;重新锚定排在它**之后**,
   三者(`origin`/`tgt` / `607Ah` / `6064h`)才自洽。**验收点:回零做完之后再点一次画布,
   必须没有意外运动。**
3. **`g_stop` 的纪律。** 回零被「停止」中止一次之后,再点一次「使能」或「故障复位」,
   看它是否正常执行 —— 不会做的话说明 `drainCommands()` 里那次 `em_clear_stop()`
   没生效(故障复位那条尤其危险:它是在**已经卸力之后**才会失败的)。
4. **中止延迟。** 回零中点一次「停止」,应当是**几十毫秒内**停下(轮询是 2ms,
   界面上按钮到 30Hz 刷新那一拍另算),**并且保持使能**。控制台应打出
   `[中止] 撤掉 6040h bit4, 保持使能`。
5. **那两段失能窗口有多长、竖直轴会滑多少。** `6098h` 只能未使能时写、切回 CSP 也只能
   未使能时改,所以轴在**回零之前**与**收尾**各失能一次,竖直轴这两段都会失去保持力矩。
   拿秒表/相机量一下这个时间(**这个数要写回本文件**),再决定竖直轴能不能这么用。
6. **方式 24 在这台机器上到底朝哪个物理方向走。** 「正向」说的是**电机轴**的正反向,
   与画布上的哪一边没有必然关系。所以第一次要在**低速下限 100 pul/s**、人在急停旁边
   的条件下点一次,看它朝哪边走。**反向不对就换另一个按钮,
   别硬顶**;找不到开关就先手动把滑台挪到开关附近再回零。
   **速度框的缺省是 50000 (1 圈/秒), 第一次必须手动把它压到下限 100** ——
   缺省值取的是"能干活的速度",不是"第一次该用的速度"(见 §18.7)。
   100 pul/s 在 30 秒里只走 0.06 圈,所以压到下限试方向时
   **必须先把滑台挪到开关旁边**,否则它只会超时,看不出方向对不对。
   > **划改(2026-09-22)**:30 秒 → 缺省 120 s,所以这一趟能走 0.24 圈而不是 0.06 圈。
   > **这条建议本身没变**:0.24 圈照样够不着开关,压到下限试方向仍然得先把滑台挪近。

> 2 与 5 这两条是**互相咬合**的:收尾那一段失能窗口里滑台会下滑,而重新锚定是在重新使能
> **之后**做的,所以下滑的那一段被算进了新零点里。量下滑量的时候顺手就验了第 2 条。

### 已知的小瑕疵

- 控制台日志的前缀是 `[hmi]` —— 那是 `hmi/ecatworker.cpp` 里的字面量,
  而 scan 把那个文件原样编了进来。不影响功能,但排查时看着别扭。
  要改的话给 `EcatThread` 加一个默认 `"hmi"` 的 `setLogTag()` 即可(仍是纯增量),
  本次没做。
- **`BusTelem::busy` 在连接期间看不到**(既有,不是本轮引入的):`doConnect()` 置
  `m_busy = true`,但连接全程阻塞、
  期间 `publish()` 跑不到,而 `m_busy` 在函数出口就置回 `false` —— 所以按钮形态其实
  从来没跟着变过。**与本轮 `resetting` 是同一个坑的两份**(见 §15 追加节第四个坑)。
  真要"连接期间锁住按钮"得照 `doFaultReset()` 那样加锁直写 `m_telem.busy`。
  (**2026-09-21 仍没动**,理由见 §21.2:连接期间 `m_ax` 还没建好,不适合从帧里发遥测。)
  (同一个 `m_busy` 上**另有一个真 bug**,已经修了:两条早退路径漏清它,
  见下一节。两件事别混:这里说的是"阻塞期间界面看不到",那里说的是"状态收不干净"。)
- **`m_busy` 漏清** —— 见 §15 追加节的第五个坑,已修(`doConnect` 拆成壳 + 内层)。

---

## 14. 本次不做

- **回零(HM)**:区域中心靠人把滑台推过去再点「设为区域中心」对位。(后来做了,见 §18;
  `em_home()` 一行没改。)
- **连续扫描(on-the-fly)**:只做逐点停。连续扫要另一套"位置触发采样"的时序,
  是另一个量级。
- **真功率计驱动**:见 §7.1(`OphirMeter`)。
- **加工数据**:不做插值填补、不做拟合、不做峰位分析。出 CSV + 热力图就停。
- **画布缩放/平移、色阶自动跟随、sidecar 状态文件、功率计工厂/注册表**。
  (视野是**固定** 30×30 —— 固定视野是"不给缩放",不是"能缩放"。)
- 不抽 `ecat_core`(那是 [qt_hmi_layout.md](qt_hmi_layout.md) 的 P1)。

- **不由 `60FDh` 触发任何中止** —— 开关那三个灯**纯显示**。精判据的开关 (`kRefineLimitWithDigIn`)
  保持 `0`。扫描区域本来就可能正好停在一个开关上,
  用监视量去触发会白中止一趟一小时的活。
- **不做轴级选择复位** —— 一次复位所有报故障的轴("该复位哪根"是驱动器的事实,不是人的选择)。
- **不做 OP 期 `60FDh` 的 SDO 轮询** —— 进 OP 之后的 SDO 往返正是上一期怀疑会让驱动器掉出
  OP 的诱因;要读就把它放进 PDO。
- **不给 `hmi` 加故障复位按钮** —— 但共享的那条 note 措辞**在两边都成立**。

---

## 15. 界面优化(五条)

| 要求 | 做法 | 落在哪 |
|---|---|---|
| 恢复默认按钮 | 「扫描参数」组底部的 `恢复默认`;数值从 `Params` **现算**,不手抄 | `ScanWindow::applyDefaults()` / `onRestoreDefaults()` |
| 固定画布 30×30 | 视野常数 `kViewHalfUnits = 16.0`(30 mm + 每边 1 mm 余量);正方形画布改为居中(**2026-09-22 改名为 `scan::kCanvasHalfUnits` 并搬到 `scanplan.h`,见 §28**) | `MapCanvas::viewHalfUnits()` / `plotRect()` |
| 右栏可滚、左栏随窗口 | `QSplitter`(画布 stretch 1)+ 参数栏装进 `QScrollArea` | `ScanWindow::buildUi()` |
| 滑台移动速度可设(单点点击) | 新增「手动速度」输入框,作用于**点画布**与**全部回中**;**只在没扫描时**推给工作线程 | `ScanWindow::pushManualSpeed()` |
| 限位信号显示 | 状态栏常显两根轴 + 画布红环 + 上升沿横幅 | `ScanWindow::refresh()` / `MapCanvas::drawMarkers()` |

结论:

- **手动速度不进 `Params`。** 它跟扫描几何无关,也不该写进 CSV 表头(CSV 表头是用来判
  续扫兼容性的)。所以它是窗口自己的状态。
- **「重测选中点」不归手动速度管。** 它走的是扫描状态机(`armRun` → `startPoint`),
  用的是「扫描速度」。
- **手动速度是"没扫描时才推",不是"点按钮时才设"。** 扫描中的速度归
  `ScanController::start()`;扫描一结束/中止,`running` 变假,`refresh()` 自己就把手动
  速度推回去。
- **点击被夹在生效量程内**(见 §9)。
- **分隔条可以拖**,不是"画布伸缩 + 右栏固定宽度"。理由见 §9 的布局一节。

> **`手动速度` / `恢复默认` 这两个控件是纯界面的,`scan_selftest` 覆盖不到** ——
> 它只跑状态机,不建窗口。所以它们的验证是"编译通过 + 跑起来看一眼 + 几何量一遍",
> 属于 §13「尚未验证」那一类里最轻的一档:**没有人在真机上验过手动速度真的生效**。

### 追加:坐标标尺 + 轨迹随窗口缩放

| 要求 | 做法 | 落在哪 |
|---|---|---|
| 画布上要有坐标标尺 | 下边 X、左边 Y,每 1 mm 小刻度、每 5 mm 带数字,与网格线同一组位置;`mm` 写在左下角 | `MapCanvas::drawRulers()` |
| 窗口缩放时蛇形轨迹不跟着伸缩 | `m_path` 改为存**单位**,像素由 `drawPath()` 用 `QTransform` 现算(cosmetic 笔) | `MapCanvas::rebuildPath()` / `drawPath()` |

- **标尺是给手动定位用的。** 点一下画布 = 走到那个显示坐标。悬停读数给的是精确值,
  标尺给的是"这个点大概在哪儿"。
- **轨迹不伸缩是个真 bug。** 缓存了像素就必然要有人负责作废它,而"窗口大小
  变了"根本没被算作几何变化。改成存单位之后,这类失效标记整个不需要了。
- 四条留白 (`kPadL/R/T/B`) 写成一处常量,标尺和色标条都从那儿取宽度 —— 否则改了
  `plotRect()` 而标尺还按旧宽度摆,数字会画到框上去。
- 窗口下沿那条带子归 X 标尺了,所以 HUD 里"悬停读数"和"操作提示"两行改成贴着**画图区**
  的下沿画;画布小到 5 mm 放不下一个数字时 (`k < 8`) 数字隔一个画一个,刻度线一根不少。

验证方式是"看一眼 + 量一遍":抓了 577×609 / 1077×733 / 494×449 三种画布尺寸的
截图,确认标尺数字在两种大小下都可读、轨迹在三种尺寸下都铺满区域框 (27 mm)。
**没有在真机上验过**。

### 追加:限位信号灯 + 两个左键互换

| 要求 | 做法 | 落在哪 |
|---|---|---|
| 加入限位信号灯 | 状态栏每根轴"灯 + 字"一组,跟文字同一份遥测驱动 | `paintLamp()` / `makeLamp()` / `lampUnit()` |
| Shift+左键手动定位,左键查看网格 | 两个动作对调:`pickCell()` 走只读那条路,**排在扫描那道闸前面** | `MapCanvas::mousePressEvent()` / `pickCell()` |

- 灯必须跟旁边的字说同一件事,理由见 §9「信号灯:一条规矩,四种样子」。
- **对调之后会动滑台的那个键多一步** —— 见 §9「两个左键」。
- 查看是只读的,所以**不受 `setManualAllowed()` 管**;扫描中吞掉的是手动定位,不是全部点击。

验证:用 `QApplication::sendEvent()` 往画布上**真发鼠标事件**(不是直接调函数),
核对 `selectedIx/Iy`。画布 937×669 时 `k = 19.09 px/单位`,点画布像素 (528.5, 304.5)
= 单位 (4.30, 1.10):

| 点在哪 | 期望 | 实测 |
|---|---|---|
| 网格内 | `round((4.30+13.5)/0.5) = 36`, `round((1.10+13.5)/0.5) = 29` | `(36, 29)` ✓ |
| 画图区内、网格外的余量 (742, 334) → 14.85 单位 | 取消选中 | `(-1, -1)` ✓ |
| 标尺 / 色标条 (853, 334) → 21.3 单位 | 不动(那里连画图区都不是) | 不变 ✓ |

截图确认:"未采集"那一行在没数据时也写出来,`[36, 29] (4.50, 1.00) 单位` 的坐标与
上面反推的格点一致;信号灯绿/红两态是临时把 `paintLamp` 钉住抓的图(30Hz 的 `refresh()`
会立刻把它刷回"没连接",所以是停掉定时器再抓的)。临时截图已删。
`scan_selftest` 不建窗口,这两条**没在真机上验过**。

### 追加:参数栏「轴信号」

| 要求 | 做法 | 落在哪 |
|---|---|---|
| 右侧参数栏里再加信号灯 | 新组「轴信号」,排在参数栏**最上面** | `ScanWindow::buildAxisPanel()` |
| 一个信号一个灯 | 2 轴 × 3 信号 = 6 格,每格 = `lampUnit(灯, 字)` | `makeLamp()` / `lampUnit()` |
| 分别对应每个轴三个信号 | 使能(bit2) / 故障(bit3) / 限位(bit11),判据同一处算出 | `refreshAxisSignals()` / `setSignalCell()` |

- 信号灯的语法整个换掉了(加三盏灯之后"绿 = 没事"不再成立),§9 那一节
  是重写的;状态栏那两盏也一起改成了新语法 —— 同一根信号不能有两套读法。
- 顺带把 §9 那张"限位显示的位置"表从 3 处改成 4 处(多了参数栏这一处)。

踩了两个坑,都是布局/验证层面的:

| 坑 | 现象 | 修法 |
|---|---|---|
| `setColumnStretch(4, 1)` | 凭空多出一个空列,前三列被挤成**一个字宽** —— 文字显示成"已" | 改成 `setColumnStretch(3, 1)`;注释写明**列号只能 0..3** |
| 在同一个事件循环轮次里抓图 | 看着像文字被裁掉,其实是布局请求还没被处理 | 抓图挪进 `QTimer::singleShot(500, ...)` |

第二条是**测试假象,不是产品 bug**:第一眼看上去跟第一条一模一样
(文字不全),实际是"设了字就立刻拍",拍到的还是旧尺寸。

### 追加:每根轴三个限位开关 + 故障复位

每根轴物理上有**三个**开关输入,不是"一个限位":

| 开关 | 含义 | 端子功能 (`2310h`~`2312h`) | `60FDh` 位 |
|---|---|---|---|
| X0 | 原点 | `2310h` = 1 | bit2 |
| X1 | 正限位 | `2311h` = 2 | bit1 |
| X2 | 负限位 | `2312h` = 3 | bit0 |

依据:[ykd2205pe_ci402.md](ykd2205pe_ci402.md) 的位定义与端子功能实测。
代码里那句 `2310h X1 = 正 / X2 = 负` 是**错的**(把 `2311h` 的值写成了 `2310h`),
四处全部改正:`scanwindow.cpp`(tooltip + 横幅)、`scancontroller.cpp`(两处中止文案)、
`scancontroller.h`(注释)、本文。

| 要求 | 做法 | 落在哪 |
|---|---|---|
| 每根轴三个限位信号 | 原先是独立的一组「限位开关」:原点 / 正限位 / 负限位,读 `60FDh` bit2/1/0 (**2026-09-20 并进「轴信号」, 见上**) | `ScanWindow::buildAxisPanel()` |
| 加一个复位按钮,驱动器显示故障时可以复位 | 原先是「轴信号」组第三行、跨全部列; **2026-09-20 起在两行最右边那一格、跨两根轴**, 清 `6040h` bit7 | `ScanWindow::onFaultResetClicked()` → `postFaultReset()` |
| 原点与正负限位要分开 | 原点 **绿亮不中止**,正/负限位红亮 | `LampGrid` 两块 (仍在, 排在同一张表上) + `ecatcmd::limit_hit()` |

#### 一个决定性的实测事实

**本机生效的 TxPDO (`1A00h`) 只有 3 项 / 10 字节**:`6041h`/`6064h`/`606Ch`。
ESI 声明里那份 12 项 / 39 字节的清单(含 `60FDh:32`)**不是生效的映射** ——
同一份文档自己写着「不要相信任何一份『出厂映射』」。

所以 `60FDh` 走**两条路**:默认**只绑不补**(它本来就在映射里才绑,零副作用),
另加一个**默认关闭**的连接期开关 `setWantDigIn()` 可以在连接时补一次(仅写 RAM,
收尾还原)。界面只给一个勾选框,而且**只在未连接时可改** —— 改了不重连不生效,
不锁住就会出现"勾了没反应"。**优先走厂家上位机改一次 `1A00h` 那条零代码路径。**

#### 撞限位:收成一处

统一成 `ecatcmd::limit_hit()`,在 `publish()` 里算一次,发成 `AxisTelem::limit_active`,
`SCAN_LIMIT_BIT` 这个宏**删掉了**。

发布的行为 = **bit11 单独判定**。精判据(bit11 **且** 60FDh 说正/负限位压着,
原点不算)写好了但藏在 `#define kRefineLimitWithDigIn 0` 后面,规则本身做成带参数的形式,
于是**两条分支在同一次构建里都可测**。不打开的理由是有一类配置会让它变弱
(`2310h`~`2312h` 功能码改成 0 时 `60FDh` 三位恒为 0,而 bit11 照样会因限位置起;
此时 `dig_known = true` 而精判据永不成立,保护就没了)。**先按真机实测结果决定。**

> 另一件记录在案、本轮**不改**的事:`healthProblem()` 对 bit11 **没有消抖** ——
> WKC 要连续 10 帧坏才中止,而 bit11 单帧置起就中止。文档自己写着开关脉宽要大于 10ms、
> 而输入滤波设的是 2,所以暂态是可设想的。**不投机加消抖**(20000 pul/s 下 100ms 消抖
> 要花掉约 2000 pul)。改为:在上升沿那一支**顺带 `printf` 出当时的 `60FDh`** —— 见 §13。

#### 踩的坑

| 坑 | 现象 | 修法 |
|---|---|---|
| `setColumnStretch(N+1, 1)` (**同一个坑第二次**) | 凭空多出一个空列,文字被挤成一个字宽 | 列数写成枚举 `AX_NCOL`/`LIM_NCOL`,造列和拉伸**读同一个值**;拆成两块之后列号不再是常数 3 |
| 判据写在 `.cpp` 里 | `scan_selftest` **不编 `ecatworker.cpp`**,逻辑写在那儿就等于永远验不到 | 纯判据全部做成 `hmi/ecatworker.h` 里的 `inline`(`ecatcmd` 命名空间) |
| 测试自己写错:`dig_pos=true` 却期望"不撞" | 红了一条 —— 那一位压着本来就该算撞 | 见下 |
| `resetting` 只靠 `publish()` 往遥测里拷 | 界面**永远**看不到"正在复位…" | 复位里那两次**加锁直写** `m_telem.resetting` 才是起作用的那半,见下 |

**第四个坑是一个真 bug。** `publish()` 与 `doFaultReset()` 跑在**同一根线程**上
(`run()` 里 `drainCommands` 在循环顶、`publish` 在循环底),而复位那一整段是**阻塞**的
(每轴最多 1 秒)。所以那两三秒里 `publish()` 一次都跑不到,等它跑到时 `m_resetting`
已经被置回 `false` —— 只把 `t.resetting` 做成"从成员每周期拷一份"(和 `m_busy` 一样的
写法)的话,`BusTelem::resetting` **恒为 false**,界面上「正在复位…」文案、按钮禁用、
tooltip 里"期间按钮不可用"那句承诺全是死代码。

当时(2026-09-20)让界面看见的是**复位里那两次加锁直写 `m_telem.resetting`**:界面线程是
独立的 30Hz 轮询 `telemetry()`,工作线程阻塞期间它照样读得到。**两处都要**,而且删掉
任何一处都会悄悄退化成"按钮看起来没反应"。规则写下来:

> **凡是"某个阻塞命令执行期间界面要看的状态",都必须由那条命令自己加锁写进 `m_telem`,
> 不能只依赖 `publish()` 的每周期拷贝。**

> **2026-09-21 补**:上面这条规则现在有了更强的那个版本 —— 让**阻塞期间照样每帧发一次遥测**
> (`em_set_cycle_hook` + `EcatThread::BlockTick`,见 §21)。直写仍然是"第一个字节之前就
> 写上"的那半(界面靠它立刻把「停止」换成立即中止),但**"每帧都在变的量"只能靠每帧发**:
> 回零/找限位期间那三盏开关灯就是这么修好的。规则因此改写成:直写管"状态位在动作开始前
> 就位",`BlockTick` 管"动作全程都在刷新" —— **两样都要,且都不能表达对方的另一半**。

同一条规则也说明**既有的 `m_busy` 有这个毛病**(不是本轮引入的):`doConnect()` 置
`m_busy = true`,但连接也是阻塞的,同样两秒内一次 `publish()` 都跑不到,所以
**连接期间按钮其实不会变形态**。这是**既有行为,本次没动**(见 §13 小瑕疵)。

#### 真机反馈:勾选框勾不上 + 一句说错的"已撞上"

| 坑 | 现象 | 修法 |
|---|---|---|
| `doConnect()` 两条早退路径漏清 `m_busy` | 连接**失败过一次**之后 `busy` 永远是 `true` → 界面把 `onair` 当成"占着总线",于是 `m_connected` 永远为真:使能/设零点/开始扫描全亮着而底下什么都没有;勾选框 `setEnabled(!onair)` 于是再也勾不上 | `doConnect()` 拆成**壳 + 内层**:`m_busy` 只在壳里写一对(置起 / 清掉),内层七条出口一条都不管 |
| 把"连接期参数"实现成"连上了就不许改" | 人恰恰是**连上之后**看见三个灯全灰才知道要勾它,而那时框是灰的 | 永远可勾,改用"说出来":已经连上了就提示"**下次「连接」才生效**"。勾错代价为零 —— `em_require_dig_in()` 只在 `em_setup` 里读一次 |
| 横幅把"驱动器报了一位"讲成"已撞上限位" | 操作员看到的是一句**事实错误**的话,于是去处理一个不存在的问题 | 只说知道的两件事(bit11 置起 + `60FDh` 说了什么),按三种情况给三种"现在能做什么" —— 见下 |

**第二个坑的教训**:`setEnabled(!onair)` 把**发现问题的时刻**和**能改的时刻**拆开了 ——
而人只会在前者想到后者。"灰掉一个控件"和"说一句为什么"之间,凡是两者都能做的时候
都应该选后者。

**第三个坑是措辞**:bit11 是 CiA402 的 `internal limit active`,本机到底拿它表示什么
**还没实测过**(§13 那六条里的第 2、3 条)。所以横幅现在按三种情况说话:

| `60FDh` 的情况 | 横幅说的 |
|---|---|
| 读不到 | 分不清是原点还是限位 —— 并告诉人**怎么让它可读**(勾那个框,或用厂家上位机改一次 `1A00h`) |
| 正/负限位有一位压着 | "先手动把它走离压着的那个开关" |
| 只有原点压着 | 明说**"压着的是原点开关"**,而"任何开关压着就置起"这件事本机还没实测过 |
| 三个都没压着 | 往别处找:607Dh 软限位?还是 `2310h`~`2312h` 功能码没配?—— 但**这两个数现在没有程序会打印**(原先那个只读诊断的 CLI 已删,`em_setup` 也不打 `607Dh`),要看得自己临时加 `em_rd_i32` |

状态栏那一格同时从 `限位X 撞上!` 改成 **`限位X 有效!`** —— 它只有一格宽,
那就放驱动器自己那句话,别替它下结论。

横幅是 `hint(s, true)`(**不自动消失**);bit11 下降沿 / 丢帧 / 断开都会把它清掉,
而且**只清"还是我们自己那条"的**(比对原文,免得把中止之类别的消息抹掉)。

第三条:**失败的那条断言是测试写错了,不是产品错了。**
它把"只压原点"写成了 `(dig_pos=true, dig_neg=false)` —— 而 `dig_pos=true` 正是
"正限位压着",命中才是对的。
**`limit_hit()` 的参数里根本没有原点那一位**,所以「原点不算」不是靠一条 `if` 记得写对,
而是**传不进去**。

#### 可测性:哪些验了、哪些只能上机看

- **验了**(`scan_selftest` 143 → **188** 条):`axis_needs_reset` 真值表、
  `pick_faulted_axes` 在 9 种输入下挑得对不对(**含"状态未知的轴跳过,不猜"**)、
  `limit_hit` 两条分支、诊断那句话不把"不知道"说成"都没压着"、
  **只压原点照样跑完一轮 25 点且 `autoAborted` 一次没响**、`60FDh` 未知时 bit11 照样中止、
  中止文案点名压住的开关。
- **只能上机看**:两个框的列宽不被挤扁、`60FDh` 未知时三个灯是**灰 + `--` 而不是"松开"**、
  原点绿 / 限位红 / 全未知三态。**真正的复位动作与"只有故障轴被写"这一条在真机上没验过**
  —— 需要先诱发一个故障。见 §13 那六条。

验证:先抓**未知态**(六盏灯全灰、文字全是 `--`),再手工钉住一帧抓**模拟态**
(轴X 绿已使能/灭无故障/灭正常;轴Y 灭未使能/红有故障/红有效,状态栏同时显示
`○ 限位X 正常` 与 `● 限位Y 有效!`)。30Hz 的 `refresh()` 会立刻
把手工状态刷掉,所以是**停掉定时器再抓**。临时截图 (`out/ax_*.png`) 已删。
`scan_selftest` 不建窗口,**这六盏灯同样没在真机上验过** —— 上真机第一件事是拔掉一根
轴的遥测看它是不是六盏一起变灰,而不是各灰各的。

**勾选框那一条后来补测过**:`scan` 里临时加一段
探针,`findChildren<QCheckBox *>()` 把每一个勾选框的 `isEnabled` / `isVisibleTo(窗口)` /
`visibleRegion()` 和整条父链打出来,`SCAN_PROBE=1 ./bin/scan.exe` 跑一次。实测
`enabled=1 visible=1`、`local=(13,85 362x24)`、可见区域**与自身矩形完全重合**
(说明没被任何祖先裁掉),父 `QGroupBox` 的可见区域也等于它的几何 —— 即:它不但能勾,
而且尺寸是完整的 362×24。探针用完已删(`scan/main.cpp` 的 blob 回到 `14ce4be3`,与 HEAD 逐字节相同)。

---

## 16. 三条现场反馈:记参数 / 滚轮 / 卡死

1. 每次开程序都要在十条 `\Device\NPF_{GUID}` 里重新认出上次那张网卡;
2. 参数框上滚一下滚轮就改了值 —— 参数栏本身就是要滚的,手一滑就改错;
3. **参数调到某些值,程序直接卡死。**

第 3 条是缺陷,前两条是可用性。

### 16.1 记忆:exe 旁边的 `scan.ini`(`scanprefs.h/.cpp`)

新文件 `scan/scanprefs.{h,cpp}`,**只链 `Qt6::Core`** —— `scan_selftest` 不链 Widgets,
把 ini 的读写放进一个能用自检真跑一遍的文件里,"键名写错一个字母"这种
**不报错、只是下次记不住**的错才有人抓。所以读写与
`Prefs` 结构体单独一个文件:`Params params; QString nic; int manual_speed; int home_vel;`
加上「高级选项」那三个 bool,加上色标那个 `shade_auto`。

- **位置**:`QCoreApplication::applicationDirPath()/scan.ini` —— exe 旁边。跟着程序走,
  一眼能找到;想清零就删了它。`QSettings` 用 `IniFormat`,**不用注册表**。
- **记**:区域 X/Y、分辨率、1 mm = ? 脉冲、速度、停留、稳定、采样、蛇形、起始方向、
  **上次连的网卡**、手动点画布用的速度、**回零速度**(`ui/home_vel`)、
  **「高级选项」那三项**(`adv/want_dig_in` / `adv/npn_write_drive` / `adv/npn_sw_invert`)、
  **色标是不是自动跟随**(`shade/auto_fit`)。
  手工改坏的 ini 会被夹回量程内(`ecatcmd::home_vel_from_pref`)。
  **改键的坑**:`QSettings` 对缺项给一个无效 `QVariant`,`toBool()` 一律 false ——
  读那几个 bool 必须显式带缺省,否则一个旧 ini 会把"默认开"读成"用户关掉了"。
- **不记**:
  - **CSV 路径** —— 文件名带时间戳,记住它等于把上趟的数据盖上或者把新数据写到别处;
  - **色标上下限** —— 它是"这一张图"的显示参数,把上趟锁的小范围套到新数据上,
    新图会**看着是空的**;每次都从 0..1 起。
    注意这跟上面那条"色标自动跟随**记**"不矛盾:**模式**记不住就得每次开程序重新勾一遍,
    而**数值**记住就是把上趟的量程套到新数据上 —— 前者记的是"怎么显示",后者记的是
    "显示到什么范围",后者一旦是陈的就会把人骗了;
  - **功率计选择** —— 真机与模拟源
    之间切换是有代价的 (选到真机会马上去开设备,几百毫秒到一两秒),而记住它意味着
    下次开程序**一进去就去找表头** —— 表头不在时那是一次注定失败的打开,还会在状态栏
    留一句红字。同理不记波长/量程/模式三项:那是**设备**的当前配置,由设备自己报回来,
    在 ini 里存一份就成了第二个真相。
- **什么时候写**:①按「连接」时(**连之前先记住这张卡** —— 否则连上了再改掉下拉框,
  记下的是最后选的那条而不是真连上的那条);②关窗时;③点某一框的「保存」时。
  那两处顺手写的同时会把正在编辑的那一框的当前值一起记下,并把它的快照跟上
  (否则「取消」会退回一份已经不在 ini 里的"上次保存值")。
- **写不进去不算错**:目录只读、盘满、文件被占着,都只是"这一次没记住"。
  `prefsSave()` 里没有返回错误这一路。
- **过不了 `validate()` 的当前参数不覆盖旧的**(`prefsMergeParams`):
  在 **连接时** 和 **关窗时** 都只记"能用得起来的那份"。
  判据用的就是界面「开始扫描」按不按得动的那一条(`validate`)。
- **量程(`range_pul`)一律归零**:它是从区域算出来的(`autoRangePul`)。

**网卡怎么选回来**:`buildUi()` 里那个 `adaptersListed` 读回 `m_savedNic`,
在 `\Device\NPF_{GUID}` 里 `findData()`;找不到(换了机器、卡拔了)**退回第一项**,
状态栏给一句"上次用的网卡 (…) 不在列表里 —— 请重新选一块。(记在 exe 旁边的 scan.ini 里)"。
**不弹模态框**。

### 16.2 滚轮:按住 Ctrl 才认(`WheelNeedsCtrl`)

规矩:**滚轮只滚参数栏;按住 Ctrl 才按 Qt 原来的规矩改值**。没按 Ctrl 时,滚轮
**不吃这一滚**,让它继续往上传给参数栏滚动区 —— 于是同一个滚轮在参数栏上照旧是滚动条。

**这条判据在 2026-09-23 换过一次。** 原来是"点过它(拿着焦点)就认滚轮",而**那个例外本身
就是坑**:焦点停在下拉框上时,那一滚**既改了值、又没滚成页面**,于是焦点一直留着,接着滚
接着改。现场那句话("滚动菜单的时候容易误修改参数")就是这么来的 —— 而**当时一直使能的框只有
「功率计」那一块**(其余几块不在编辑态时控件是禁用的,禁用控件根本收不到滚轮),误改全落在
它身上(**那天稍后编辑态整个撤掉了, 控件平时就能改, 见 §31** —— 闸只剩这一条, 反而更要靠它)。
换成 Ctrl 这个**一次性、明确**的手势:想用滚轮调值就按住,不想动它就松开。
程序里没有别处用 Ctrl+滚轮(画布不认滚轮),不打架。
**这一换不是"多加一道闸",是去掉了那条例外** —— 判据从"焦点"换成"修饰键"。

**错在"事件到底落在哪个控件上"。** 过滤器只装在 `QDoubleSpinBox` 这些外层控件上时,
探针直接 `sendEvent` 给 spin box 是"过了",而真机上不是这么走的。
装在 `qApp` 上的探针抓下来的真实投递顺序是:

```
QWidgetWindow  ->  QLineEdit 'qt_spinbox_lineedit'  ->  QDoubleSpinBox
```

**先接到滚轮的是 spin box 内部那个 `QLineEdit`**,而 `QAbstractSpinBox` 正好
在那个 line edit 上装了自己的事件过滤器(内部编辑框的按键与滚轮都走那一条),
**值就是在那一条里被改掉的**。过滤器只装在 spin box 上,这一滚根本轮不到。
同理:判"选中没有"必须从
**收到事件的那个控件往上找**到被闸的输入框再比焦点(`guardedAncestor`),
拿 line edit 自己去比是永远比不上的(焦点在 spin box 上)。

所以现在:

```cpp
/* 装在输入框 + 它的每一个子控件上 */
void guard(QWidget *w) {
   m_guards.insert(w);
   w->installEventFilter(this);
   for (QWidget *k : w->findChildren<QWidget *>())
      k->installEventFilter(this);
}

/* 收到事件的那个控件 -> 找到它属于哪个被闸的输入框 */
QWidget *input = guardedAncestor(w);
if (input == nullptr) return false;        /* 这一滚不落在参数框上 */
if (static_cast<QWheelEvent *>(e)->modifiers().testFlag(Qt::ControlModifier))
   return false;                           /* 按住 Ctrl: 按 Qt 原来的规矩改值 */
e->ignore();                               /* 没按 Ctrl: 不吃, 交给父级去滚 */
return true;
```

第三处:**名单是"找出来的",不是手写的**。手写的表**漏了不报错** ——
只是那个框还在被滚轮改。现在改成遍历窗口里所有
`QAbstractSpinBox` 与 `QComboBox`,能漏的唯一方式是"新加了一个既不是
`QAbstractSpinBox` 也不是 `QComboBox` 的输入控件"。

**焦点那一条仍然留着, 但理由换了。** 滚轮这条闸**管不到键盘**:焦点留在一个**已经滚出
视线**的框上时, 上下箭头与打字会改到一个看不见的框。所以 `buildUi()` 里那条照旧接:

```cpp
connect(sideScroll->verticalScrollBar(), &QScrollBar::valueChanged, this, [sideScroll](int) {
   QWidget *fw = QApplication::focusWidget();
   if (fw != nullptr && sideScroll->isAncestorOf(fw))
      fw->clearFocus();       /* 人翻页把框滚走了 -> 别让键盘改到一个看不见的框 */
});
```

只清**滚动区里面**那个焦点(顶栏那张网卡表在滚动区外面,不受影响)。
它从前是滚轮判据的另一半("翻页就作废选中"), 判据换成 Ctrl 之后只剩上面这个用途。

两个实现细节:

- **`e->ignore()` 与 `return true` 要一起用**。Qt 的规矩是"滚轮事件沿着父链上传,
  直到被接受";过滤器返回 `true` 就**吃掉了**这一次投递,父级 `QScrollArea` 再也收不到,
  参数栏就**滚不动了**。先 `ignore()`(于是 `accepted=false`)再把 `true` 返回,
  才是"我不改值,这一滚交给上面去滚页面"。
- **判 Ctrl 要读事件自己带的 `modifiers()`**,不能用"此刻键盘上按着没有"那类查询:
  这一滚可能已经在队列里排了一会儿, 改的是**投递那一刻**的修饰键状态。
- **不能用 `w->hasFocus()` 那类判据**:焦点在内部那个 `QLineEdit`/`QDoubleSpinBox` 上,
  谁是焦点跟"哪个是我要闸的输入框"是两件事,只能沿父链找(`guardedAncestor`)。

**范围**:只闸 `QAbstractSpinBox` 与 `QComboBox`,不是全局。
`QLineEdit`(CSV 路径)不闸 —— 滚轮改不了它的文字,它那一滚照旧落给参数栏。

**2026-09-23 换判据时重新量过一遍**(临时探针:offscreen,不连设备,用已经编好的目标文件
链接而成,跑完即删):

| 量的是 | 结果 |
|---|---|
| 不按 Ctrl, 滚在数字框上 | 值 `200 -> 200`(没动) |
| 不按 Ctrl, 滚在下拉框上 | 取样源下标 `1 -> 1`(没动) |
| **框已经拿着焦点**, 不按 Ctrl 滚过去 | `200 -> 200`(没动) ← **现场抱怨的正是这一条** |
| 按住 Ctrl, 滚在数字框上 | `200 -> 190`(改到了), 页面 `0 -> 0`(没滚) |
| 按住 Ctrl, 滚在下拉框上 | 下标 `1 -> 2`(改到了) |
| 对照: 事件直接投给滚动区视口 | 页面 `0 -> 60`(滚起来了) |

最后那条对照是**必须的**:它证明滚动区自己收得到滚轮、也真的会滚 —— 否则上面几行的
"页面没滚"分不清是"闸把事件吃了"还是"这个测试环境根本滚不动"。

**没量到的一条**(别把它当成已验):闸拦下那一滚**究竟有没有上传到滚动区**。
合成投递(`sendEvent` 给控件)量到的是"事件停在输入框那一层, 视口一次都没收到";而真实滚轮
走的是 `QWidgetWindow::handleWheelEvent` 那条路, 与合成投递不是同一段代码, 探针够不着它
(要投真事件得引 Gui 的私有头)。真机上这一条的判据只有**手感**:滚参数栏时鼠标划过数字框
与下拉框, 页面**应当照旧滚**。

### 16.3 卡死:点数上限 200000(`kMaxPlanPoints`)

**根因**。`ScanController::rebuildPlan()` 从前是"参数一动就照着算",
`nx*ny` 有多大就建多大(改之前的原文):

```cpp
m_nx   = axisCount(m_p.area_x_unit, m_p.res_unit);
m_ny   = axisCount(m_p.area_y_unit, m_p.res_unit);
m_plan = buildPlan(m_p);                      /* nx*ny 个 Point */
m_plan_p = m_p;

const size_t n = (size_t)std::max(0, m_nx) * (size_t)std::max(0, m_ny);
m_done.assign(n, 0);                          /* 再加三份同样大的 */
m_have.assign(n, 0);
m_watts.assign(n, 0.0);
```

而 `validate()` 里那条"点数 200000 太多"的提示**只是一句话** —— 它挡「开始扫描」,
但**不挡 `rebuildPlan()`**。区域上限 500、分辨率下限 0.001 ⇒ **2.5e11 个点**。

数值小一点(比如 1e8 个点、约 4 GB)时 `assign` **会成功** —— 于是没有崩溃、没有报错,
只有:填 `plan` 的那个循环一直在跑,加上 `MapCanvas::paintEvent` 每帧重建
`QImage(nx, ny)` 再逐像素填。**界面从此不动了**,而且**也没崩**,所以看起来就是"卡死"。

**修法**。上限提成**一处定义**(`scanplan.h`):

```cpp
constexpr long long kMaxPlanPoints = 200000;
```

`validate()` 与 `rebuildPlan()` **读同一个常量** —— 一处分界,两处执行。
`rebuildPlan()` 现在先算 `nx/ny/total`,超限就**什么都不建**:

```cpp
if (nx <= 0 || ny <= 0 || total > kMaxPlanPoints)
{
   m_nx = m_ny = 0;                    /* ← 这两行不能省, 见下 */
   m_plan.clear(); m_done.clear(); m_have.clear(); m_watts.clear();
   m_plan_p = m_p;
   return;
}
```

- **`m_nx = m_ny = 0` 是必须的**:`cellDone()` 里按下标访问 `m_done[iy*m_nx+ix]`,
  留下 `m_nx/nx` 而把数组清空就是**越界读**。零化之后"没有网格"在全程序里只有一种表示。
- **空网格在别处都安全**:`rebuildImage()` 见到 `nx<=0` 直接返回(画布空着)、
  `start()` 判 `nx<=0` 就拒绝、参数栏用 `paramsError()` 把「开始扫描」按住。
- **画的是"要的那张网格",不是"建出来的那张"**:参数栏与状态栏显示的是
  **算出来的 `nx/ny/total`**(哪怕超限),因为操作员要的是
  "我要的这一片到底有多少点" —— 显示 0×0 会让人以为分辨率没生效。
  超限时那一行写清 `网格 400 × 501 = 200400 点 … 超过上限 200000 —— 网格没有建
  (画布此刻是空的, 「开始扫描」按不动)`。

**200000 是资源闸**:`plan` 是 `Point` 数组、`m_done/m_have/m_watts`
各一份、热力图要一张同尺寸的 `QImage`、还有一条预览路径。两万个点以内怎么都轻松,
二十万个点是一张 447×447 的图 + 几十 MB。`validate()` 里那句话也照着它写:
"先确认分辨率是不是少打了个零"。

### 16.4 这一轮动到的文件

- **新增** `scan/scanprefs.h/.cpp`(只链 `Qt6::Core`);
- `scan/scanwindow.h/.cpp`:`WheelNeedsCtrl`(头里一行前置声明 + 成员写成具体类型,
  因为装闸时要调它的 `guard()`;2026-09-23 之前叫 `WheelNeedsFocus`)+ `nicShort()` +
  `loadSettings()` / `saveSettings()` +
  `buildUi()` 末尾 `guard()` 遍历装闸 + 接滚动条那条"一滚就作废" + `adaptersListed`
  选回网卡 + `pushParams()` 里超限那一行;
- `scan/scanplan.h`:`kMaxPlanPoints`;`scan/scanplan.cpp`:`validate()` 用它;
- `scan/scancontroller.cpp`:`rebuildPlan()` 的超限分支;
- `scan/selftest.cpp`:`plancap` / `prefs` 两组(188 → **237** 条);
- `scan/CMakeLists.txt`:`scanprefs.cpp scanprefs.h` 进 `SCAN_COMMON_SRC`;
- `.gitignore`:**`/bin/scan.ini`** —— 它记着**这台机器**的网卡,换台机器那份 GUID 就没意义了;
- `README.md`、本文。

### 16.5 验证到什么程度

**验了**(见 §13;全部不需要设备,用临时探针 + 自检):

- 参数记忆跨进程:第一次跑 → 第二次跑 `area_x = 12.5`(缺省是 27);
- 网卡:把 ini 指到**第 10 块**(Intel I219-V)→ 起来停在 **index 9**;
  指到一个不存在的 GUID → **退回 index 0 不崩**;
- **滚轮(2026-09-21 那一轮, 判据还是"点过才认")**:探针**不再直接 `sendEvent` 给 spin box**,
  改成照 Qt 真实路径投递(先给 `childAt()` 那个控件,再沿父链走),
  12 个输入框逐个三列:

  ```
  共 12 个输入框: 没人点却被改 0 (要 0); 选中后能改 9 (要 12);
                  翻页清掉焦点 11 (要 12); 翻页后又被改 12 (要 0)
  ```

  逐行看是 `区域 X 27 -> 没人点:27 没动 -> 选中:28 改了 -> 翻页后:28 没动` 这样的三列。
  两个"没到 12"都查过、都不是漏:

  - **选中后能改 9/12**:另外 3 个是**下拉框**。闸对它们是放行的(`闸放行·Qt没动`),
    但 `QComboBox::wheelEvent` 在弹出列表还没被创建过时会直接不吃滚轮;
  - **翻页清掉焦点 11/12**:那 1 个是顶栏的网卡表,它**在滚动区外面**,
    代码里用 `sideScroll->isAncestorOf()` 圈定了范围。

  **这张读数只回答了"值有没有被改", 没回答"页面滚没滚"** —— 而 2026-09-23 换判据时量到:
  合成投递下那一滚**不上传**(停在输入框那一层, 视口一次都没收到)。两轮的结论都见 §16.2
  下面那张表。
  另外上面那条"`QComboBox` 在弹出列表没被创建过时会直接不吃滚轮"**没复核住**:
  换判据后按住 Ctrl 滚过去, 下标 `1 -> 2`, 而那时弹出列表还没建过。
- 上限:400×500 建得出来、400×501 拒绝且「开始扫描」按不动。

**没验**:

- **真机上"连上那张卡之后确实能进 OP"** —— 本轮一次都没连过设备(没有设备)。
  记忆这条链路只验到"下拉框选对了"。
- **卡死的门槛手感**:200000 是不是合适(会不会有正当的扫描需要更多点),
  要等真按区域、真按分辨率跑几次才知道。
- 参数栏那个滚动区里,**滚轮在参数框上从"改值"变成"滚页面"之后手感如何** ——
  只能上机看(尤其是触控板:一次滑动会连发很多个 wheel 事件)。**"翻页就作废选中"
  这条同样只有手感能评**。
- **"滚轮闸"这条规则没有任何自检覆盖**:`scan_selftest` 只链 `Qt6::Core`,
  装不了控件也就测不了这条 —— 要长期守住,得有一个能链 Widgets 的测试目标,本轮没做。

---

## 17. 「读一次」按钮 + 三个模拟源的自检

### 17.1 动机:接上表头之后第一件想做的事,原来做不到

真机那一路接通之后,界面上能证明"接上了"的只有一行状态行 (表头/探头型号 + 序列号)。
想看到**一个数**,必须先开一趟扫描 —— 而一趟扫描是一小时。

所以功率计面板里加了一个 **「读一次」** 按钮:向**当前取样源**要一个数并显示,
不用跑整趟扫描。它走的是扫描用的同一条路 (`requestReading` → `readingReady`/
`readingFailed`),所以**这一下通了就等于采集那条路也通了**。

显示里带**一次往返的实测毫秒数**:那个数必须远小于扫描参数里的读数超时,否则真机上
会每点都超时。

### 17.2 它为什么必须有一条硬闸

`PowerMeter` 的约定是"**恰好**回一次" + "**调用方**负责保证同一时刻只有一个未决请求"
(`powermeter.h`)。关键词是**调用方**——而现在调用方**有两个**:

| 调用方 | 什么时候发 |
|---|---|
| `ScanController` | 扫描进行中,一整趟几千次 |
| 「读一次」按钮 | 人工点 |

两个请求撞在同一个源上时:

- **模拟源**:老老实实回两次,两个数分给两边 —— 于是扫描的 CSV 里**悄悄少一个点或
  错一个点**,不报任何错;
- **真机 (Ophir)**:它按时间戳只认严格更新的采样,于是其中一边**白等到超时**。

两种都是默默发生的。所以闸门只能由界面来把:**控制器不在 `Idle` 时按钮禁用**
(`ScanWindow::refresh` 里那条 `setEnabled`),并且按钮的 tooltip 会说明为什么按不了。

两个槽 (ready/failed) 里还多比了一次 `sender() != m_meter`:换取样源时那个未决请求会
作废,但**旧源的回话可能已经排在事件队列里**了,比一次 `sender()` 就不用赌队列顺序。

### 17.3 顺序:先摆好状态,最后才发请求

`onReadOnceClicked()` 里 `m_readPending` / 起始时刻 / 兜底定时器**全部摆好之后**才调
`requestReading()`。源有一个**同步回话**的口子:没打开时三个模拟实现
都是直接 `emit readingFailed` 然后 return(在 `requestReading()` 的调用栈里就回来了)。
把顺序倒过来就是**在别人的调用栈里改自己的状态**。(§7 那条
"`emit` 一定在调用栈之外"的说法**只在打开之后成立**。)

### 17.4 自检:三个模拟源

三个模拟源**此前没有一条测试**,本轮补上一组
(本轮 **288 → 307** 条;这 19 条里 4 组是新用例,其余是组内多条断言),钉的就一件事,
也正是那条闸所依赖的:

> **一次请求恰好回一次** (`readingReady` 或 `readingFailed`,不多不少),且值对得上。

- 手动源:没打开 → 恰好一次失败、零个读数;打开 → 恰好一个读数、值就是设进去的那个;
- 随机源:跑十次,**每一次**都只许回一个数,且落在 `基值±噪声` 之内,并且十次不能全撞在
  同一个数上 (那说明 `setNoise` 没接上,热力图会是一片死色);
- 脚本源:`#` 注释与空行跳过 → 按行取 → **取完一轮绕回第一行**,`cursor()` 对得上;
- 最后一条**不是**在验某个源,是在验那条约定本身:两个未决请求撞在一起时,源会**老实回
  两次**(`checkEq(n, 2)`),谁也不知道哪个数属于哪一次 —— 这就是 §17.2 那条闸必须存在的
  理由。这层逻辑在界面上 (要 Qt Widgets),而本自检按约定**不链 Widgets**,所以它是
  "用测试说明为什么必须挡",不是"用断言验挡没挡住"。

**踩到的坑 (自检直接段错误,而且一行输出都没有)**:第一版是每个请求就地写
`QObject::connect(&meter, signal, [&loop](...){...})` —— 这个三参数重载的连接宿主是
**信号发送方** (那个源),而 lambda 按引用捕获的 `QEventLoop` 早就析构了。下一次请求
回话时,**上一次留下的那个 lambda 也会被叫起来**,碰的正是那个已经没了的循环。
收进一个 `ask()` 助手,把连接挂在 `loop` 自己身上 (context 传 `&loop`,loop 一析构连接
就断) 之后才好。顺带还发现:同步回话那一支会让 `loop.quit()` 在 `exec()` 之前被调用,
而 Qt 对"循环没在跑时的 quit"是**什么都不做** —— 于是每次白等到超时 (不报错,只是慢)。

输出为什么一行都没有:`printf` 到管道是块缓冲的,段错误把整个缓冲区丢了。

### 17.5 另外补的两处

- **`m_lReadout` / `m_lMeter` 明写 `Qt::PlainText`**:这两格显示的是**设备来的字**
  (探头型号、序列号、过量程原因)。QLabel 默认 `AutoText`,那串字里只要有个 `<`
  就会被当 HTML 解析 —— 于是要么显示不全,要么被标签吃掉。
- **换取样源时清掉那一格**:一个从没读过的源旁边挂着一个数,是最坏的一种残留;
  未决请求同时作废。

### 17.6 这一轮动到的文件

- `scan/scanwindow.h/.cpp`:`m_btnRead` / `m_lReadout` / `m_readTimer` / `m_readPending` /
  `m_readSentMs` + 三个槽 + `fmtWatts()` + `readOnceTip()` + `kReadOnceTimeoutMs`;
- `scan/powermeter.h`:把"调用方"那条约定写清 —— **调用方不止一个**,那条约定是它们之间
  要协调的;
- `scan/selftest.cpp`:`test_meter_sources()` + `ask()` 助手 (237 → **307** 条);
- `README.md`、本文。

### 17.7 验证到什么程度

**验了**(不需要设备):`scan_selftest` **307 通过 / 0 失败 / 0 跳过**,0.7 秒跑完
(288 → 307;`ophir` 那两条仍然按 §13 那样跑 —— 本机 COM 对象在,`ScanUSB` 数到 0 个设备);
`bin/scan.exe` 起得来、**构造期不崩** (新控件挂进 `refresh()` 的那条路走过一遍)。

**没验**:按钮在真表头上的实际手感与那个**往返毫秒数** —— 表头不在 (本机的
`ScanUSB` 数到 0 个设备,Windows 里也没有任何 Ophir USB 设备、没有一个异常设备)。
插上之后跑一次 `bin/scan_selftest.exe` 就会把
`ophir: live reading through OphirMeter` 那条腿从"验没坏"变成"验能用"。

---

## 18. 回零:驱动器自带的正/反向找原点

需求:**「加入驱动器自带的正方向和负方向寻找原点功能 当然找原点的时候我按停止键
依然能做到直接停止」** —— 后半句才是这件事的难点。

### 18.1 驱动器侧的原语

`em_home()` ([ec_motor_motion.c](../motor_api/ec_motor_motion.c)) 完整实现了回零的全部
动作:写 `6098h` 方式 / `6099h:01`·`:02` 速度 / `609Ah` 加减速 / `607Ch` 偏移 →
`6060h = 6 (HM)` → 抬 `6040h` bit4 启动 → 每 2ms 轮询 `6041h` bit12(到位)/ bit13
(回零错)/ bit3(故障),而且**自己泵帧**。**唯一的调用方是界面**(`doHome()`) ——
原先还有 `motor_test --home`,那个程序 2026-09-21 删了。

**正/负方向就是方式 24 与 29**(见
[ykd2205pe_ci402.md](ykd2205pe_ci402.md) 那张表):24 = 原点信号为原点、**正向**高速先找;
29 = **反向**高速先找。**35 没用** —— 它是"以当前位置为机械原点",它**不去找**,
不是这两个按钮的意思。

**「按停止键直接停」也早就埋在 `em_home()` 里**:轮询里有一句
`em_stop_requested()`,**命中就撤掉 `6040h` bit4 并**保持使能**(中途位置不明,
卸力会让竖直轴自由下滑)。缺的是**怎么把那个"停止"送进去**。

### 18.2 队列救不了回零

停止请求今天只能经 `postStop()` / 命令队列进来,而队列是在 `EcatThread::run()` 顶部
由**同一个会被回零阻塞住的线程**排空的。回零期间投一条 `CMD_STOP`,要等 `em_home()`
退出来才被看见 —— 也就是**最长 30 秒之后**。

所以「停止」在回零期间必须**绕过队列**:由 GUI 线程直呼 `em_request_stop()`。
这就碰到了 [hmi/ecatworker.h](../hmi/ecatworker.h) 顶部那条不变量 ——
**「GUI 线程一个 motor_api 函数都不调」**。

处理方式是**明写出来** —— 那条不变量正是这块代码可审计的全部依据,
**违反它比改写它更糟**。新例外安全的理由也是结构性的:`em_request_stop()` 只往一个
`static volatile sig_atomic_t` 里存 1,不碰 `em_bus_t`、不碰网卡、不做 I/O;
motor_api 自己就是按「信号处理器里也能调」来设计它的。

用它的地方**只有两处**(见 §2 那节与 §10 的护栏表):「停止」按钮在回零中的那一支,
以及 `disconnectAndStop()` 的第一步。**`hmi` 不受影响** —— 它没有回零功能,
那两个按钮的连线一个字没改。

### 18.3 收尾那五步

`em_home()` 有**五条返回路径**,没有一条留下的状态是调用方可以不管的:

| 路径 | 它留下什么 |
|---|---|
| 到位 | 使能 + HM + bit4 已放下 |
| 被「停止」中止 | 使能 + HM + bit4 已撤(**不切模式**) |
| bit3 故障 / bit13 回零错 / 超时 | 使能 + HM + **bit4 还举着** |

所以 `doHome()` 只能有**一个出口**,收尾无条件执行:

```
0. em_clear_stop()                 // 见 18.4
1. em_disable(ax)                  // ★ 超时那条路上, 这一步才是真正让它停下来的
2. em_set_mode(ax, EM_MODE_CSP)    // 已使能时会被拒 —— 所以必须在 1 之后
3. em_enable(ax)                   // em_arm 会把 607Ah 钉在 6064h
4. 重新锚定: m_origin[ax] = em_pos(ax); m_tgt = 0; m_want = 0;   // ★ 必须在 3 之后
5. 读实测状态判定结局, 复位 m_homing, 一条汇总 note()
```

- **第 1 步不是冗余**:超时/bit13 那条路上,`em_home()` 返回时驱动器**还在找**,
  让它停下来靠的就是这一步(那几个控制字都没有 bit4)。
- **第 4 步的位置**:`em_arm()` 是在**使能那一刻**把 `607Ah` 钉到当时的
  `6064h`。若先锚定再使能,失能窗口里滑台自重下滑的那一段会让 `m_origin` 停在**下滑
  之前**、而 `607Ah` 钉在**下滑之后** —— 下一次 `interpolate()` 就会以旧位置为目标
  **全速把滑台开回去**。这就是 18.5。

### 18.4 `g_stop` 是进程级的,而且会粘住

`em_clear_stop()` 在**全仓库此前一次都没被调用过**,是块死代码。
不清的后果:回零时被置起的那个 1 会留给**下一条命令**,让它在自己的第一次检查处当场中止
(`em__cw_step` / `em__wait_sw` / `em_set_mode` / `em_home` / `em_fault_reset` 都读它)。
表现是「点了使能没反应,控制台说被中止了」;而**故障复位那条更糟** ——
它是在写完 `6040h = 0x0000`(**已经卸力**)之后才中止的。

纪律:**`drainCommands()` 里每出队一条命令就 `em_clear_stop()` 一次。**
于是每条命令都从干净开始,运行中被置起的标志只由正在跑的那一条理会。

**唯一的洞是收尾**:回零被中止后,收尾那几步会**立刻**被那个还没清的标志打断
(第一步 `em_disable` 内部就有 `em__cw_step`),轴就停在「使能 + HM + bit4 可能还举着」
—— 正是要防的状态。所以 `doHome()` 的收尾第一步**再清一次**:那个停止请求瞄的是
**运动**,而收尾的全部职责是抵达一个确定状态。

### 18.5 陈旧的显示原点是这次改动里最危险的一行

回零之后驱动器自报的 `6064h` 会跳到它的回零坐标系(等不等于 0 由 `2214h` 决定,
`em_home()` 刻意不断言)。**不重新锚定 `m_origin` 就是一次没人按过按钮的全速运动。**

所以重新锚定是**无条件**的,而且**必须在重新使能之后**(18.3 第 4 步)。

配套的不变量:**工作线程在所有它真动过的路径上重新锚定,GUI 每次派发回零都 `m_epoch++`
一次。** 于是「锚定而不 +1」(危险方向)结构上不可能;「+1 而不锚定」只是让续扫多问一句
(那是保守的错误方向)。GUI 无法知道工作线程那道闸是拦还是放,所以**宁可多发一代**。

### 18.6 闸必须在动作之前

`em_home()` 自己的四条拒绝(已使能 / 无完整帧 / 有故障 / 方式越界)**全部在第一次写之前**。
**危险的是外围**:`doHome()` 第一步 `em_disable()` **真的会撤掉
保持力矩**(竖直轴当场下滑)。等 `em_home()` 去发现"不该做",力矩早撤了。

所以闸读的是**刚取到的**状态(不是上一轮发布的遥测快照,同 `doFaultReset` 的规矩),
判据顺序也是定的:`!bus_ready → !origin_ready → !mirror_ok → fault → any_axis_moving`。
`mirror_ok` 排在 `fault` **前面** —— 一个从没收到过的状态字里的 bit3 不是信息
(同 `axis_needs_reset` 那条规矩)。`any_axis_moving` 用工作线程自己的真相
(`m_want[k] != m_tgt[k]`),不看有 33ms 滞后的遥测:回零期间插补器是停的,
另一根还在走的轴会**停在半途**。

### 18.7 界面上是怎么摆的

- **框名叫「原点模式」,里面只有速度、超时两个值和八个按钮**(逐轴,可以只回一根)。
  `[保存][取消]` 摆在**框标题那一行的右端**(2026-09-23 起没有「编辑」了; 按钮也从"速度
  右边那一格"挪到了标题上,见 §31.4)。速度与超时占满第 1~4 列 —— 原先 3~4 列是那两个
  按钮的地方, 现在空着会把速度框右边留出一块。
  版面是**一行一根轴,四个按钮**: 行首一个「轴X / 轴Y」标签,右边依次
  **正向回零 / 反向回零**(方式 24/29,基准是**原点开关 X0**)、
  **找正限位 / 找负限位**(方式 18/17,基准是那一侧的**限位开关**,见 §20)。
  按钮文字里**不带轴名** —— 一行四个按钮,每个再带个"X "就排不下,而"这行是哪根轴"
  正是错点一下代价最大的那件事,所以轴名写在行首(同「轴信号」那块表的行标签)。
  发起按钮**只看被点的那一根**的判据,不看另一根,**也不看它使能没使能** ——
  未使能也能回零(`em_home()` 自己会先失能再使能,而且它**要求**未使能才能写 `6098h`)。
  八个按钮**共用同一个速度值**:`6099h:01/:02` 与 `609Ah` 是同一对参数,再放一个速度框
  会出现"哪个在生效"这种界面上看不出来的组合。
- **回零中只改正在动的那一根的按钮文字**:回 X 时 Y 那两个不动;找限位时左边两列仍写着
  "正向/反向回零"(那一趟不是找原点),找原点时右边两列仍写着"找限位" —— 说反了人会以为
  "找完还要再找一次"。文字变短("回零中…")也不会把列宽撑开。
- **速度输入框**喂 `6099h:01`;**`6099h:02` 与 `609Ah` 都由它派生**
  (返回速度 = 1/4,式子就是 `ecatcmd::home_vel_slow()` 那一句;加减速按**斜坡时间 0.1 秒**算,
  见下面那条)。上下限与工作线程的夹取**读同一对宏**,所以**界面上显示的数就是线上发的数**。
  **不进 `scan.ini`** —— 同"错了就是安全问题的值必须每次由人重申"那条。
  > **更正(2026-09-22)**:上面这句是错的 —— 它**一直**在存,键名 `ui/home_vel`
  > (`scan/scanprefs.cpp` 的 load/save 各一行),`README.md` 里也是这么写的。写这段时漏了。
  > 现在这一框里又多了个同样进 ini 的 `ui/home_tmo_s`,见 §27。
- **速度的上限是 `HMI_VEL_MAX`(100000 pul/s),缺省是驱动器自己 `6099h:01` 的实测值
  50000**。第一版这里写的是 2000/500 —— 照 `em_home_cfg_default()` 那个值
  抄的,而它跟机器自己的配置差 25 倍。「手动速度」+ 点画布
  本来就能以 100000 pul/s 朝同一个开关走,把回零单独压到 2000 并不构成一道真实的保护,
  只是让这个功能没法用(500 pul/s = 0.01 圈/秒,30 秒走 0.3 圈)。**真正该防的是
  "找不到还不肯停",那由超时兜着。**
- **加速度是"斜坡时间 0.1 秒"推出来的**(`ecatcmd::home_accel_for`)。
  写死 5000 配 2000 pul/s 是 0.4 秒斜坡;配上放开的 100000 就是 **20 秒**
  斜坡,一次回零整个花在加速上,**操作员把速度调上去却看不出任何区别**。
  0.1 秒这个数来自驱动器自己那对实测值:`6099h:01 = 50000` 与 `609Ah = 500000`
  相除就是它。速度超过 50000 之后加速度**封顶在 500000**(器件自己那个数,
  `6083h`/`6084h` 也是它),斜坡变长到 0.2 秒 —— **这个程序里没有哪一处该比机器自己
  配的加速度更硬**。
- **不弹确认框,框里也不再写说明文字**。那三段话只剩两个常驻的地方:
  · **按钮 tooltip**(事前):「**驱动器会自己带电并移动,软件拦不住它撞开关**」、
    **该轴会先失能(竖直轴此时失去保持力矩)**、轴 / 方向 / 方式号、以及"方向不对就换
    另一个按钮,别硬顶";
  · **速度输入框的 tooltip**(事前):"够不着开关请先把滑台挪近,**不要**为了够得着
    去调高速度"(这句原先在框里那行随速度变的字上,那行删了之后搬进 tooltip)。
  出路是「停止」= 立即中止(回零中唯一写着出路的地方是顶上那条**不自动消失**的横幅)。
- **2026-09-20 删掉框里那两行小字**(一行随速度实时变: 本速度 / 返回速度 / 加减速 /
  最多走几圈 / 几秒判超时;一行报两根轴的 `6061h`): 面板只剩速度与八个按钮。
  前者的**数**不再在界面上露出(要回看只有控制台回零收尾那条结论句),
  后者的**证据仍然在**控制台结论句里(`EcatThread::doHome` 的 `note` 单独一格打印
  `6061h`,且把期望值 `EM_MODE_CSP` 一起写进去) —— 所以工作线程那一次读与
  `AxisTelem::mode_disp` 都**留着**,不是"没人用的死字段"。
- **回零期间**:该轴按钮文字变「回零中…」、顶部出横幅(那条横幅**不自动消失** ——
  它是这 30 秒里唯一写着出路的地方;**它浮在画布顶上、不占布局体积**,见 §9「横幅不进布局」)、
  「使能 / 失能 / 停止点 / 设为区域中心 / 开始扫描」全部锁住。**「停止」除外**。
- **回零结束后自动切回 CSP 并保持使能**,停在落点带保持力矩,
  一次点完就能接着点画布。
- **超时是 30 秒,而它同时是一条距离上限**:速度 × 30 秒。
  按缺省速度 50000 pul/s 算是 **30 圈** —— 而缺省扫描区域才 27 mm(27 圈),
  也就是说**缺省状态下从区域一头找到另一头也够**。速度调低时能找的范围跟着缩小
  (2000 pul/s 只有 1.2 圈),所以碰不到开关时该做的是**先把滑台手动挪近**,
  不是把速度调快。
  > **划改(2026-09-22)**:超时不再写死 30 秒,现在是界面上一个可改的框(缺省 120 s,
  > 夹在 5…600 s),**但上面这句的规矩一个字都没变** —— 它同时是距离上限这件事没有变,
  > 只是「够不着开关先挪滑台、别为够得着调参数」现在同时管着速度和超时两个框,见 §27。

### 18.8 这一轮动到的文件

- `hmi/ecatworker.h`:顶部不变量改写(第二个例外)、四个 `HMI_HOME_*` 宏、
  `BusTelem::homing/homing_axis/homing_method`、`ecatcmd` 八个判据
  (`home_method_for` / `home_dir_text` / `home_vel_slow` / `home_vel_clamp` /
  `home_refusal` / `home_end_state` / `home_cause_text` / `home_end_text`)、
  `postHome()` / `requestMotionStop()`、`CMD_HOME`、`Cmd::method`、`doHome()`、`m_homing*`。
- `hmi/ecatworker.cpp`:`drainCommands()` 的 `em_clear_stop()`、`doHome()`、`publish()`
  与 `teardown()` 的 `m_homing` 三处。**`hmi` 的按钮连线一个字没改。**
- `scan/scanwindow.h` / `.cpp`:`buildHomePanel()`(第三个侧栏框)、`onHomeClicked()`、
  `onStopClicked()`(「停止」从直连 `postStop` 改成槽,**非回零路径逐字节不变**)、
  `refresh()` 的门禁与横幅、`disconnectAndStop()` 开头那一句。
- `scan/scancontroller.cpp`:`armRun()` 的 `t.homing` 闸、「使能」那句措辞、
  续扫世代说明加上回零这第二个成因。
- `scan/selftest.cpp`:`test_homing()` + `FakeBus::setHoming()`。
- `README.md`、[ykd2205pe_ci402.md](ykd2205pe_ci402.md)、本文。
- **`motor_api` 一个字没动,`scan/CMakeLists.txt` 没动。**

> 这一节记的是**回零那一轮**动的东西(方式 24/29)。找限位那一轮另见 §20 ——
> 它落在同一个 `doHome()` 里,加上去的判据与文案在那里逐条列出。

### 18.9 验证到什么程度

**验了**(不需要设备):自检全绿(当次读数见 §13)、
新增那批全是纯判据:方向→方式号、返回速度派生与全域不变式、夹取与输入框 range 不漂移、
闸的五个分支**及其顺序**、收尾结局的真值表、以及每一句措辞;
`hmi` 与 `scan` 两个目标都 **0 warning 0 error**;`bin/scan.exe` 起得来、**构造期不崩**
(连起两次,5 秒后仍在,网卡清单照常列出)。

**没验**:**`em_home()` 这条调用链一次都没在真机上跑过** —— 收尾那五步的顺序、重新锚定、
`g_stop` 纪律、中止延迟、两段失能窗口的时长与竖直轴下滑量、以及"方式 24 在这台机器上
到底朝哪边走",**六条全在 §13 的硬件清单里**。
**而其中第 0 条是前提:这台机器的限位极性是配反的(§13 实测),
而回零用的是驱动器自己读到的开关电平 —— 「上位机侧取反」对回零一点用都没有,
必须先修 `2300h`(「高级选项」那一项默认就开着)。**

## 19. 「高级选项」框 + 参数框的编辑门控

两个需求合在一轮做:把让限位灯与开关对得上的那两件事做成**默认开**、集中到一个新框里;
再给每一块参数框加一道门 —— 平时只读,点了这一框的「编辑」才能改。

### 19.1 现场那个根,以及两条修法的层次

`60FDh` 的 bit1/bit2 空载就同时置起 → `6041h` bit11 恒为 1 → `2204h = 0`(超程停车)
把两个方向都挡死 → 回零进得去、一动不动(§13 的实测)。修法两条,层次不同:

| | 动作 | 治哪一侧 | 谁读它 |
|---|---|---|---|
| **写驱动器 `2300h`** | `bit0~bit2 = 1`(常闭)→ `0x0007`,高几位原样保留 | **根**:驱动器自己的限位保护、`60FDh`、`6041h` bit11 一次全对 | 驱动器 |
| **上位机侧取反** | 只换本程序的判据(`LIMIT_RULE_INVERT`) | 上位机:三个灯与中止判据 | 本程序 |

**两条一起上 = 双反相,而且不是"等于没反",是更坏**:判据此时只看反相后的两个限位开关,
双反之后它们空载时都是 1 → 判据**恒成立** → **扫描永远开不了**(只多报,不漏报)。
所以:第一条默认开,第二条默认关;两个都勾着时界面上出一行红字
(`editgate::doubleInvertWarning`,判据与措辞都被 `scan_selftest` 钉住)。

回零是**驱动器**自己按它读到的电平走的 —— 上位机侧取反对回零一点用都没有 (§13 第 0 条)。

### 19.2 写 `2300h`:三步,任一步不确认就整体拒绝

`motor_api` 里原先**没有任何写参数的路径**。新增的只有两个入口:

- `em_allow_param_write(bus, on)` —— 与 `--allow-pdo` **分开的一道授权**
  (改映射是通信配置,改驱动器参数是另一回事,代价不同);
- `em_di_set_logic(bus, want)` —— ① 先逐轴读原值(`em_rd_any`,
  **宽度按驱动器自报的来**,只认 1 / 2 字节,别的宽度不猜也不写)→ **任一根读不到就整体
  拒绝,一个字节都不写**;② 已经等于 `want` 的轴跳过;③ 写后逐轴回读 `memcmp` 确认。

回读不是可选项:本仓库这份 SOEM 在加急路径 (`psize<=4`) 把 SDO abort 当写成功
(`ec_motor.c` 顶上那条不变量),`em__verified_write` 就是为这个存在的。

收尾**挂在 `em_shutdown` 的失能之后、还原映射之前**,判据是 `di_logic_changed[]`,
**不查授权**(授权管"改",还原是把这一趟改掉的撤掉;再要一次授权会导致"没授权时收尾反而
留下改动")。**从不写 `2102h`**,所以不进 EEPROM 那条路。
**手册没写 `2300h` 会不会被驱动器自己落盘** —— 所以对外文案里不许出现"断电即回"。

CLI 侧(2026-09-21 起已删,下面是当时的改动):`motor_test` 多一行只读诊断
(顺带把它在 `sm_baseline.c` 里的宽度从 U8 改成 U16,
手册 V2.4 p84 是 U16 RW;`sm_bytes_to_i64` 的宽度取自 SDO 回包、`dt` 只管符号,
所以这次改的是标签,基线文件与行为都不变)+ `--allow-param` / `--npn`
(`--npn` 不带 `--allow-param` 直接拒绝,退出码 `EM_EXIT_USAGE`)。

### 19.3 「高级选项」框

摆在参数栏最底下 (设好了就别再动的东西)。三项,全部进 `scan.ini`:

| 项 | 默认 | 时机 | 说明 |
|---|---|---|---|
| 让 `60FDh` 进 TxPDO | **开** | 连接期 | 连接时补写 `1A00h`,收尾还原,仅 RAM;过程数据 10 → 14 字节 |
| 写驱动器 `2300h = 0x0007` | **开** | 连接期 | 见 §19.2 |
| 上位机侧取反 | 关 | **运行期** | 勾一下下一帧生效,并整个换掉限位判据 |

**顺序硬约束**:`loadSettings()` 是在 `buildParamPanel()` 里面被调的,而它要把存下来的值
写进那三个勾 —— 所以 `buildAdvPanel()` **必须排在 `buildParamPanel()` 之前**
(布局上仍摆在最后)。

`hmi.exe` 的行为**一字不变**:`EcatThread` 侧那两个成员初值都是 `false`,由 `ScanWindow`
按 `scan.ini` 推过去。`hmi` 那边唯一的新输出是每根轴一行**只读**的 `2300h = …`。

### 19.4 编辑门控:状态机在头里,`setEnabled` 只有一个写点

> **已改(2026-09-23)**:§19.4~19.6 记的是当时那一轮的形状。当天先撤掉 `[编辑][保存][取消]`
> 三个按钮与编辑态, 随后按用户要求把 `[保存][取消]` 装回来 —— **「编辑」与"同一时刻只许一块
> 框编辑"那套静默丢弃不再存在**; 三项实质变化: ①框值随时可改, 没有"编辑态"这道门;
> ②「保存」只写**这一框**管的字段, 不再是整份 `saveSettings()`; ③标题上的「未保存」标记与
> 「取消」都拿 **ini 里那一份**当基准(不再是"进编辑态时那一份")。保留的是下面那条**运行中锁**与
> `need_manual` 判据、以及 `setEnabled` 只有一个写点这条规矩 —— 见 §31。读代码时以 §31 为准。

`scan/editgate.h`(**唯一的新文件,纯逻辑、不依赖 Qt**,所以 `scan_selftest` 能覆盖):

```
Gate { editing, dirty, discarded }
begin / markDirty / undirty / drop / save / cancel / titleMark / doubleInvertWarning
```

界面侧:

- 每块框一个 `PanelGate { Gate; box; btnEdit/btnSave/btnCancel; title_base; items; snapshot }`,
  成员表 `GateItem { w, lock_running, need_dev, need_manual }` —— **那三个按钮不进成员表**
  (进了编辑态它们反而必须按得动);
- `[编辑][保存][取消]` 那一行插在框首 (`QFormLayout::insertRow(0, …)`);
  回零框是 `QGridLayout` 而它只有三行 (速度 0 / 轴X 1 / 轴Y 2),**摆在速度右边那一行**
  (2026-09-21 改, 原先是框底单独一行 `(3,0,1,5)`): 这一框里只有速度是参数, 那三个按钮
  管的也就是它 —— 摆在同一行一眼能看出"这三个按钮管的是这个数", 还省下一行高。
  它占 3~4 列, 并且传 `Qt::AlignRight` **顶着框的右边界**(不传的话这一格被填满、
  按钮贴在速度框后面, 右边空一大块);
- **`ScanWindow::refreshEditability()` 是这些控件 `setEnabled` 的唯一写点**,
  由 30Hz 的 `refresh()` 每拍调用。原先 `refresh()` 里那张"扫描中锁住"的批量表
  (原来 19 个控件)整段删掉,搬进各框的 `lock_running`。
  写在按钮槽里没用 —— 下一拍就被覆盖;
- `enabled = 编辑态 && !(运行中 && lock_running)`,下拉框另需 `count() > 0`,
  真机那三项另需 `meterDevOk()` (与 `onMeterInfoChanged` **共用这一个判据**),
  色标的「按数据定标」另需 `need_manual`(自动跟随**关着** —— 开着时它做的事每帧都在做,
  灰掉)。别的框不留神写一句 `setEnabled` 会当场被盖掉,所以这一条也写在这个函数里;
- 屏幕上的七块框里**五块在表里**: 扫描参数 / 功率计 / 原点模式 / 高级选项 / 色标(功率)。
  「轴信号」与「扫描控制」不在 —— 它们只有按钮和灯, 没有"值"可回滚。
  「色标 (功率)」一度被摘出去过(2026-09-21),
  理由是"纯显示设置, 改它不碰滑台也不碰总线, 何必先点一次「编辑」" —— **又挂回来了**, 因为
  摘出去之后那一框没有`[编辑][保存][取消]`, 屏幕上一块框跟旁边六块长得不一样, 而里边
  「自动跟随数据」明明是**要记进 ini 的模式**, 却找不到一处该按"保存"的地方。
  门控在这一框唯一做的事就是"先点一下编辑", 代价很小;
- 这一框没有 `lock_running` 项: 色阶怎么画与滑台怎么走无关, 运行中也能改;
- 进编辑态拍快照;`dirty` 不是"改过没有"的布尔标记,而是**逐项与快照比对**算出来的
  (`gateDirty`):程序自己也会改控件值,布尔标记分不出是谁改的,而且改回原值也不会自己
  消失(`editgate::undirty` 干这件事)。比对只在编辑态里做,非编辑态不碰标题;
- 「取消」**先**退出编辑态**再**回灌控件 —— 回灌的每个信号都会经 `gateDirty`,顺序反了那个
  「未保存」标记永远清不掉。回灌**刻意不挂 `QSignalBlocker`**:
  "改动当场生效"就是靠那些信号重新下推的,拦了就成了"界面回到旧值、控制器还拿着新值";
- 但回灌**只对 SpinBox/ComboBox/CheckBox 有效** —— `QLineEdit::setText` 不发 `textEdited`,
  所以「功率计」那一框回灌完要补一次 `pushScriptPath()`(框里的文本和实际读的那份脚本是
  两份东西),"取消"才算真退回去;
- 程序自己改控件值的地方(色标那一框的**两个数**:按数据定标与自动跟随每拍刷的读数、
  **真机三项重填选项表**、**CSV 缺省名**)末尾调 `gateRebase()`:把那一项的快照跟上再重算标记
  —— 只动这一项,整框重拍会把同框里别处的未保存改动一起"原谅"掉。漏了它的症状是:程序改过的
  值会被「取消」滚回一份陈值,而滚完程序又改一次,两边打架。
  自动跟随那条路(30Hz 调的)还多一句"值没变就直接返回":`gateRebase` 会重写框标题,
  每拍重设一次标题就是每拍重排一次版面。
- **被推着走的那个框不算"程序改的"**:「最小」「最大」是一对,改一个顶到另一个头上会把
  另一个一起推过去(见 §色标),推的那一下信号是拦掉的,`gateRebase` 也**不调** ——
  那是操作员这一改的**后果**,不是程序自己定的值。「取消」就该把这一对一起退回去。
  (推完那个框照样会显示「未保存」,因为 `gateDirty` 是逐项比对算的,不是靠信号点灯。)

### 19.5 运行中哪些能改

| 框 | 成员 | 运行中 |
|---|---|---|
| 扫描参数 | 区域 X/Y、分辨率、1 mm = ? 脉冲、方向、方式、CSV 路径 + 「…」、恢复默认 | **锁住** |
| 扫描参数 | 扫描速度、手动速度、单点停留、稳定窗口、每点采样 | 可改 |
| 功率计 | 取样源、手填值、噪声、脚本 + 「…」、真机三项(另需设备开着) | 可改 |
| 色标 | 上下限、自动跟随数据、按数据定标 | 可改 (自动跟随开着时上下限是读数, 打字改不动) |
| 回零 | 回零速度 | 可改 |
| 高级选项 | 三个勾 | 可改 |
| 轴信号 (内含限位灯) / 扫描控制 / 顶栏 | 只有按钮和灯 | 不在表里 |

(编辑态撤掉之后这张表**仍然成立** —— 它就是现在的 `GateItem.lock_running`,见 §31。)

锁住的那一组都是**"这一趟怎么走 / 往哪写"**:跑到一半改掉,落进 CSV 的 (ix,iy)
就和滑台实际站的地方对不上了 —— 那张表按扫描开始时定的几何算出来的。

没锁的那些是**当场生效**的:扫描速度/稳定窗口/每点采样由控制器**每个点**读一次,
改完从下一个点起算 —— 于是同一张表里前后两段的节拍会不一样,而 CSV 的 meta 段记的是
**开始时**那一份。要可复现就把这一趟跑完再改。

### 19.6 三件事的取舍

> **已改(2026-09-23)**:下面第一条的前提(**同一时刻最多一块框处于编辑态**)随「编辑」一起
> 没了 —— 现在几块框可以同时是"未保存"的, 所以「保存」**不能**再复用整份 `saveSettings()`
> (那会把别块框还没保存的改动悄悄固化、让它们的「取消」按不动), 改成 `panelSavePrefs(pi)`
> 只写这一框管的字段。第二条照旧, 只是"快照"现在叫基线。见 §31。

- **「保存」就是现成的 `saveSettings()`**,不另写一份。理由是同一时刻**最多一块框**
  处于编辑态 —— 别块框此刻持有的必然是上次保存的值,所以重写整份 ini 不丢东西。
  **推论:切框时的静默丢弃不是界面上的客气,是这条正确性的前提,省不掉。**
- **「连接」与关窗也会落盘**(原行为)。它会把正在编辑那一框的当前值一起写进去 ——
  所以那之后要把它的快照跟上、标记清掉,否则「取消」会退回一份已经不在 ini 里的
  "上次保存值",而那个按钮承诺的就是 ini 里那一份。
- **「恢复默认」受门控**(它一按就是几何全变),并且会把「高级选项」那三个勾也带回缺省
  —— 否则那个按钮对这三个勾就是句空话。**回零速度刻意不在它的范围内**:
  为试回零特意压小的速度不该被顺手抬回去。

### 19.7 这一轮动到的文件

- `scan/editgate.h`(**新**)、`scan/CMakeLists.txt`(进 `SCAN_COMMON_SRC`);
- `scan/scanprefs.h/.cpp`:四项 (`ui/home_vel`、`adv/want_dig_in`、`adv/npn_write_drive`、
  `adv/npn_sw_invert`),**读的时候显式带缺省**;
- `scan/scanwindow.h/.cpp`:`buildAdvPanel()`、五块框的门控头、`refreshEditability()`
  (替换掉原来那张批量锁表)、`gateSnapshot/Rollback/Rebase`、`meterDevOk()`、
  `pushScriptPath()`(把「脚本」那一框的文本与功率计里实际读的那份脚本对齐)、
  `m_btnCsv` 从局部量变成成员、`loadSettings/saveSettings` 扩四项、
  限位框那两个勾搬走;**三个灯与两张网格一个字没改**;
- `motor_api/ec_motor.h/.c`、`ec_motor_internal.h`:`EM_OID_DI_LOGIC` 等常量、
  `em_allow_param_write`、`em_di_set_logic`、`em_shutdown` 的还原步、头注释不变量;
- `motor_api/motor_test.c`:只读诊断行 + `--allow-param` / `--npn`;
- `hmi/ecatworker.h/.cpp`:`setNpnWriteDrive`、连接期的写入与只读探读、
  `ecatcmd::home_vel_from_pref`、`limit_*` 文案里的旧勾选框名;
- `slide_motion/sm_baseline.c`:`2300h`/`2301h` 的宽度标签 U8 → U16;
- `scan/selftest.cpp`:新增 `test_editgate` / `test_advprefs`(旧 380 条一条没删,
  只有两条措辞断言里的勾选框旧名字跟着改了);
- `README.md`、[ykd2205pe_ci402.md](ykd2205pe_ci402.md)、本文。

### 19.8 验证到什么程度

**验了**(不需要设备):`scan_selftest` **542 通过 / 0 失败 / 0 跳过**;
全量重建 **0 warning 0 error**;`selftest.cpp` 的 diff `+187 / −5`,那 −5 行就是两条措辞
断言里的旧勾选框名字(旧 380 条一条没删);`git status --porcelain SOEM/` **空**;
`motor_test --npn` 不带 `--allow-param` 被拒(退出码 1);
`motor_test --allow-motion` 不带 `--allow-pdo` 仍被拒(退出码 4);`--help` 里两个新开关都在。

**没验**(需要设备):

- ~~**`1A00h` 到底含不含 `60FDh`**~~ —— **2026-09-20 只读实测答了"在"**:
  两个从站都读到 `1C13h -> 1A00h`,4 项 = `6041h`/`6064h`/`606Ch`/`60FDh`,112 bit = 14 字节。
  所以这台机器上「让 `60FDh` 进 TxPDO」默认开**一个映射字节都不写**(代码只追加缺项);
  §19.3 那句"按不在设计"是保守写法,结果一样 —— 缺项才补;
- **`2300h` 是不是 RAM-only**:写 `0x0007` → 断动力电再上电 → 只读看是否仍是 `0x0007`。
  这决定文案里能不能出现"断电即回"(**现在一律不许出现**);
- **极性真对了**:写完之后把 `2004h`(物理电平,`2300h` 之前)与 `60FDh` 成对看 ——
  空载时 bit0/bit1 必须都 0、`6041h` bit11 从 1 变 0,手压一个开关对应位要跟着变;
- **全链路**:「高级选项」→「编辑」→ 勾/取消勾 →「保存」→「连接」→ 控制台有写入行、
  三个限位灯由灰变亮;「断开」→ 控制台有还原行,只读确认回到原值。顺带验「取消」;
- **双反相**:两勾都上 → 红字出现,此时起扫应当被拒(**这是预期行为**);
- **崩溃残留**(建议先在非生产机上做):连接后直接杀进程 → 只读看残留 → 确认下一次连接
  **不做任何"自动纠偏"**;
- **回零**:极性修对之后按 §13 那张真机清单走(它的第 0 条现在由默认动作满足)。

**只读实测**(2026-09-20,当时的 `motor_test.exe` 不带任何授权开关,一个字节都没写;
两个从站读数相同。**那个程序 2026-09-21 已删,下面这组数是当时测的**。
**注意这一组与 §11 那处"本机实测生效的 `1A00h` 只有 3 项 / 10 字节"对不上**
(这里是 4 项 14 字节)。两处都照原样留着,不替谁改口径 —— 这份映射**被改过**,
所以**同一天里前后的读数都可能不同**,任何一次读数都只是那一次的。实用结论只有一条:
**每次都实读**,别把哪一次的读数当常量。

```
2300h 输入有效电平逻辑 = 0x0000  X0=常开 X1=常开 X2=常开
2004h 输入端子物理电平 = 0x0006  X0=低 X1=高 X2=高      <- 2300h 之前
60FDh 数字输入        = 0x0003  正限位(bit1)=有效 负限位(bit0)=有效
6041h 状态字          = 0x0A31  Ready to switch on (电机释放) | Voltage | 硬件限位有效
1C13h -> 1A00h = 6041h/6064h/606Ch/60FDh, 112 bit = 14 字节
```

即:空载时两个限位输入物理上都是**高**,而 `2300h = 0` 把高电平当"已触发",
于是正负限位同时报有效、`6041h` bit11 置起 —— §19.1 那个根在实机上的读数。
前后两趟只读读数里 `60FDh` 分别是 `0x0003`(原点无效)与 `0x0007`(原点也有效) ——
变的只有原点那一位,正/负限位那两位两趟都是有效。这趟 `2004h` 读到 `X0=低`。

---

## 20. 「找限位」:以限位开关为原点

现场还有一种基准需求:**不找原点开关 X0,而是把那一侧的限位开关本身当成机械基准**。
手册正文把它叫「找限位」:方式 **18 = 原点 = 正限位**,方式 **17 = 原点 = 负限位**
(V2.4 p46~p48)。这一轮把它加进界面与 CLI —— 与「回零」是两个按钮排,
但走的是**同一条** `em_home()`。

### 20.1 只有基准不同,别的都一样

| | 按钮 | 方式号 | 基准 |
|---|---|---|---|
| 回零 | X/Y 正向 / 反向回零 | 24 / 29 | **原点开关 X0** |
| 找限位 | X/Y 找正限位 / 找负限位 | 18 / 17 | **正限位 / 负限位开关** |

`motor_api` 侧**一行逻辑都不用改**:`em__home_method_ok()` 的白名单本来就含 17/18,
而 `em_home()` 里**没有任何按方式分支的代码** —— 它只负责写 `6098h`/`6099h`/`609Ah`/`607Ch`、
切 HM 模式、使能、抬 `6040h` bit4、等 `6041h` bit12/bit13。朝哪走、什么时候停、走几段,
全部由**驱动器自己**按方式号决定。

缺的只有三处:**白名单**(`doHome()` 里那道)、**界面**、**CLI**。

### 20.2 手册的两条分支,与那张真值表

只有 17/18 这两个方式,手册才分 a)/b) 两条分支。分岔的判据是**回零启动那一刻,
减速点(= 那一侧的限位开关)信号有效还是无效**:

| 方式 | 原点 / 减速点 | a) 启动时**没**压着 | b) 启动时**已经**压着 |
|---|---|---|---|
| 17 | 负限位 | 反向**高速** → 遇上升沿减速停止 → 正向**低速** → 遇下降沿后停机 | **正向低速**运行 → 遇下降沿后停机 |
| 18 | 正限位 | 正向**高速** → 遇上升沿减速停止 → 反向**低速** → 遇下降沿后停机 | **反向低速**运行 → 遇下降沿后停机 |

两条分支的终点都是**开关的释放点**(刚退开一点的那个位置)。b) 就是"从压着的开关上
退开"的正规做法 —— **它的首段方向与 a) 相反**,这是现场最容易看错的地方:
按「找正限位」它却朝反方向走,操作员会以为点错了按钮。所以发起之前**必须**先预告走哪条。

方向与快慢的文案**只从 `ecatcmd::home_method_first_dir()` 出**(原来是从"方式号等不等于 29"
推出来的,那套对 24/29 够用,对 17/18 会把 a) 说成 b)):

```
                 tgt_active = false    tgt_active = true
   24 (原点/正向)      正向                 正向        <- 24/29 不随"压着没有"变
   29 (原点/反向)      反向                 反向
   18 (正限位)         正向                 反向        <- b) 与 a) 相反
   17 (负限位)         反向                 正向        <- 而 17 与 18 本身又是反的
```

`scan/selftest.cpp` 把这张表钉死了(含"24/29 两格相等"那两条 —— 挡住"顺手把 17/18
那一套套到 24/29 上")。

### 20.3 两道否决,以及为什么只有两道

找限位比回零多一道闸,**位置在现有那道 `home_refusal` 之后、宣告 `m_homing = true`
与 `em_disable()` 之前** —— 早了没状态(状态字可能还没收到),晚了已经卸力(竖直轴会滑)。
拒绝时**一个字节都不写**,文案里必须有「拒绝」两个字(界面横幅靠关键词上红色)。

| 条件 | 为什么拒绝 | 出路 |
|---|---|---|
| `60FDh` 读不到 | 分不出这一趟该走 a) 还是 b) —— 而两条分支的首段方向**相反**,分不出来就不能发起 | 「高级选项」里勾上「让 60FDh 进 TxPDO」再重新「连接」 |
| 正/负限位**同时**有效 | 滑台不可能同时在两头,这一对读数里至少有一个不是真的 | 查 `2300h`(极性)与 `2310h`~`2312h`、以及 X0~X3 的接线 |

判据用的是**驱动器自己**那两位(`em_di_poslim`/`em_di_neglim`,即 `2300h` + `2310h`
之后的结果),**不是**界面反相后的 `dig_pos`/`dig_neg` —— 驱动器按它自己的读数决定怎么走,
上位机反相只改显示。这一点是这两道否决与界面那排限位灯分道扬镳的地方。

**为什么只否决这两条**:b) 分支本身是正规做法不是要拦的情形;"另一侧压着"更没事
(朝目标去正好是远离那一侧)。而"极性配反导致单边认反"(驱动器以为正限位压着,
其实是负限位压着)这一条**拦不住** —— 它的征兆与"真的压着"完全一样,软件无从分辨。
所以现场的前置条件是**先把 `2300h` 修对**(见 §19),那件事在那之前就已经是默认动作了。

不做的事:**不在 worker 里偷偷 SDO 读一次 `60FDh` 再放行**。界面侧让操作员先把那个勾
打开,比在后台悄悄读一次更可解释;而且 `60FDh` 本机已经在生效映射里,这条否决平时走不到。
`motor_test` 那一侧是例外(诊断工具),它**有** SDO 兜底 —— 见 §20.6。
(那个诊断工具 2026-09-21 已删;界面侧这条路本来就**不**做 SDO 兜底,现在也一样。)

### 20.4 界面上那条红色「撞限位」横幅必须闭嘴

找限位**故意**去撞限位,所以 `6041h` bit11 / 目标那一位置起是这一趟的**目的**。
但 `refreshAxisSignals()` 那条路会**弹红色「撞限位」横幅并把它当故障**,而且它和
"正在找限位"那条**状态**横幅争同一个 `m_banner` —— 争赢的结果是唯一一句
"按「停止」可立即中止"被顶掉。

豁免条件 `finding_limit && i == t.homing_axis`:

- **跳过 `hint(banner, true)`**;
- **保留**那一行 stdout 证据(它说的是"这个信号此刻有效",是事实)与
  **限位灯 / 状态栏那格**(照常变红);
- `m_limShown[i]` 照常置 true(不反复重算)、`m_limBanner[i]` **刻意不写** ——
  这样限位掉下去时清理分支把它归 false,于是**找完限位之后压着限位再启扫,
  那条红横幅与自动中止照常发生**。这条豁免只覆盖"正在找限位的那一根"。

### 20.5 落点 = 释放点,所以坐标 0 会贴在那一头

找完之后 `m_origin[axis] = em_pos(ax)`(收尾第 4 步,与方式无关)把这个释放点定为
显示坐标 0。**那一侧几乎没有行程** —— 继续用旧扫描区域跑会一路撞限位。
所以按钮 tooltip 上常驻这句话,提醒先按实测行程重算区域。

**不自动改区域**:改区域是操作员的决定。也**不加 `607Ch` 原点偏移控件** ——
17/18 的"原点"就是它停下的那个释放点,偏移保持 0;想把 0 挪到别处,界面上已经有
「零点」那一个按钮了。再加一个偏移来源会让"0 到底在哪"多一个不可回溯的地方。

### 20.6 动到的文件

- `hmi/ecatworker.h` —— `ecatcmd` 新增纯判据:`home_lim_method_for` /
  `home_method_allowed` / `home_method_is_limit` / `home_method_short` /
  `home_lim_switch_name` / `home_lim_target_active` / `home_lim_other_active` /
  `home_method_first_dir` / `home_lim_refusal` / `home_lim_branch_text`。
  能判的都放这里,理由与 `home_refusal` 相同:**`scan_selftest` 不编 `ecatworker.cpp`**。
- `motor_api/ec_motor.h` —— 四个方式号宏(`EM_HOME_MODE_*`)与**哪一位是目标**那两个宏
  (`EM_HOME_LIM_TARGET_BIT`/`_OTHER_BIT`)。放 C 头里是因为**当时有两个使用者**:
  界面侧(`home_lim_target_active`)与 `motor_test.c` 的发起前预检 —— `motor_test.c` 是 C,
  include 不了 `ecatworker.h`。两边各写一份就会各错一份,而错了正好是"方向反了"。
  **2026-09-21 后只剩界面侧一个使用者**(CLI 删了)。宏留在原处没动 —— 挪它只会白白
  改一堆引用点,而且它描述的是 `6098h` 方式号本身,不是界面的事。
- `hmi/ecatworker.cpp` —— `doHome()` 的白名单换成 `home_method_allowed()`;
  新增预检 + 分支预告(位置见 §20.3);结论句改用 `home_method_short`
  (找限位的方式号**本身就带方向**,不再叠一个"正向/反向")。
- `scan/scanwindow.h` / `.cpp` —— `m_btnLim[2][2]`、`onHomeClicked(axis, dir, find_limit)`、
  `buildHomePanel()` 找限位那两列新按钮(2026-09-20 起版面并成"一行一根轴四个按钮",
  `kHomeBtnText` 一份文字同时喂建面板与 `refresh()`)、`refresh()` 的按钮文字与横幅、
  `refreshAxisSignals()` 的那条豁免。
  **`setEnabled` 的写点没增**:新按钮那一次在原来那个逐轴循环里,走的是同一条刷新路。
- `motor_api/motor_test.c` —— `--home-lim pos|neg`(需同时给 `--home`,**同一道授权**;
  值只收这两个词,不收数字)+ S7 的预检(镜像优先,SDO 兜底)与预告。
- `motor_api/ec_motor_motion.c` —— **只改注释/建议文案**(`em_home_cfg_default()`
  那句"换 29 或 35"、bit13 那条建议),一行逻辑没动。
- `scan/selftest.cpp` —— 新增五组「找限位」断言(方式号与白名单 / 首段方向真值表 /
  目标与另一侧是哪一位 / 两道否决 / 四格分支预告)。
- `README.md`、[ykd2205pe_ci402.md](ykd2205pe_ci402.md)、本文。

### 20.7 验证到什么程度

**验了**(不需要设备):

- 全量重建 **0 warning 0 error**;自检全绿(当次读数见 §13);
  `git diff --stat` == `git diff --ignore-cr-at-eol --stat`;`git status --porcelain SOEM/` **空**;
- 新增那批断言里最值钱的两条:**"24/29 的方向不随『启动时压着』变"**(挡住把 17/18
  那套套到 24/29 上)与**"四格预告里的方向与 `home_method_first_dir` 同源"**
  (两处说法不一致比说错更难查);
- `motor_test --home-lim pos` 不给 `--home` → **退出码 4**,文案点名"同一句授权";
  `--home-lim xx` → **退出码 1**;`--help` 里新开关在位。

**没验**(需要设备,手放在物理急停旁、回零速度先压到下限 100):

- **极性先修**:本机现在 `2300h = 0x0000` 而 X1/X2 空载物理为高 → 两侧限位同时报有效,
  找限位会被第二道否决拦住。**这是预期行为**,正好用来证明那道否决不是空话。
  先让「高级选项」的默认动作把它写成 `0x0007` 再连接(§19);
- **方式 18**:滑台放中间 → 按「X 找正限位」→ 朝正确方向高速走 → 碰到后反向低速退开 →
  停在释放点;控制台那句预告与实际相符;结束后 `6064h` 被定为 0;
- **a)/b) 两条分支都要走到**:先在中间跑一次(a),再手动把滑台开到压住正限位跑一次(b)——
  b) 应当是**直接反向低速退开**;
- **两道否决**:两侧同时有效时的拒绝文案;临时关掉「让 `60FDh` 进 TxPDO」再连接,
  确认因为读不到 `60FDh` 而被拒;
- **横幅豁免**:找限位碰到限位时**不**弹红色「撞限位」横幅,限位灯与状态栏那格照旧变红;
  找完之后压住限位再启扫,那条红横幅与自动中止**照常**发生;
- **`2204h` 的交互**:手册没写 HM 期间驱动器自己的超程停车(`2204h = 0`)与 a)/b) 谁优先。
  第一次在低速下试;超时那条路会打"bit11 全程举着 + 位置一步没变"的诊断
  (`ec_motor_motion.c`),撞上就照它那三处查。

---

## 21. 阻塞命令期间遥测不断流(`em_set_cycle_hook` + `EcatThread::BlockTick`)

**现场反馈(2026-09-21)**:"在回原点或者找限位的过程中信号灯不会实时更新"。

### 21.1 根因:一根线程,一个循环,而阻塞命令把这一圈顶住了

`run()` 的每一圈是 `drainCommands()` → `interpolate()` → `em_service()` → `publish()`,
**四个都在同一根工作线程上**。而回零 (`doHome`) 里的 `em_home()` 自己是**阻塞**的:
它在内部跑自己的过程数据循环 (2ms 一帧,`em__cycle`),直到找完才返回 —— 最长
`HMI_HOME_TMO_MS` = **30 秒**。这 30 秒里 `run()` 那一圈的 `publish()` 一次都跑不到,
于是 `m_telem` 冻在按下按钮那一刻的样子:**使能灯、三个开关灯、状态栏、位置全不动**。
> **划改(2026-09-22)**:那个上限已经不是常量了,现在是可改的回零超时(缺省 120 s,上限 600 s),
> 见 §27。本节其余部分(回调为什么存在、挂在哪四条命令上)一字未变 —— 超时变长只会让
> "冻结这段时间"变长,更需要这个回调而不是更不需要。

要紧的是"找限位"这一趟:它的目的就是**去把那个限位开关压上** —— 灯不跟着亮,操作员就
没有任何东西能说明它压上了,只能靠控制台里那几行字猜。

这与 §15 第四个坑(`resetting`)是**同一个根**,当时只补了那一条命令的一半:
`doFaultReset()` 加锁直写 `m_telem.resetting`,界面才看得见"正在复位…"。
回零当时也照抄了一份(直写 `homing` 那三个字段),**但方案本身只够表达"某个状态位",
表达不了"这三个开关现在什么样"** —— 后者是每帧都在变的量,只能靠"每帧发一次"。

### 21.2 改法:给总线加一个每周期回调,阻塞命令期间把它挂上

| 在哪 | 改什么 |
|---|---|
| `motor_api/ec_motor.h` | 新增 `em_cycle_fn` 与 `em_set_cycle_hook(bus, fn, user)`:每收完一帧(镜像更新之后)回调一次,`fn = NULL` 注销,**不挂 = 行为与本函数不存在时一模一样** |
| `motor_api/ec_motor_internal.h` | `struct em_bus` 多两个字段(`cycle_fn` / `cycle_user`)。`calloc` 建的 bus,所以初值天然是 NULL |
| `motor_api/ec_motor.c` | `em__cycle()` 末尾调一次回调(**短帧那一支也调**:"这帧不完整"本身就是调用方要知道的事) |
| `hmi/ecatworker.h` | 新增嵌套类 `EcatThread::BlockTick`(RAII:构造挂、析构摘) |
| `hmi/ecatworker.cpp` | 回调体就是 `publish(wkc)`;`drainCommands()` 里**四条会阻塞的命令**各套一个 `BlockTick` |

四条挂上的命令: **`CMD_ENABLE` / `CMD_DISABLE` / `CMD_FAULT_RESET` / `CMD_HOME`**。
不挂的四条(`CMD_STOP` / `CMD_ZERO` / `CMD_CENTER` / `CMD_RANGE`)只改几个成员,不碰总线。
**`CMD_CONNECT` 刻意不挂**:那几秒里 `m_bus` 已经非空而 `m_naxis`/`m_ax` 还没建好,
从那些帧里 `publish()` 出去的是"连上了但一根轴都没有"的半成品。所以
§13 那条"连接期间按钮不变形态"的小瑕疵**照旧存在**,见 §21.4。

三个必须守住的约束(漏一个就是自锁或行为漂移):

1. **回调在调用者线程里同步跑**,每帧一次 —— 里面不许再做重活,更不许调 `em_home`
   这类阻塞函数,也不许再收发帧(`publish()` 只读镜像,所以它合规);
2. **不许在持有 `m_mtx` 的作用域里构造 `BlockTick`** —— `publish()` 自己要拿这把锁,
   而它是非递归锁,同线程再进一次当场自锁。四个挂点都在锁的作用域之外;
3. 回调是**只读**的:它不发帧、不写 `607Ah`、不碰输出镜像 —— 回零的时序一个字节都没变。

### 21.3 为什么不是别的做法

- **把 `em_home()` 拆成"HMI 侧自己跑循环"**:那是把回零那套状态机(写 `6098h`~`607Ch`、
  抬/撤 `6040h` bit4、等 bit12/bit13、超时)抄第二份 —— 一份安全关键逻辑两个实现,
  这是最坏的选法;
- **另起一根线程读 `em_axis_t`**:那些字段是 `em__cycle` 在写、工作线程在读的,
  多一根线程读就是数据竞争;
- **只加锁直写某个状态位**(即 §15 那套):表达不了"每帧都在变的量",正是本节要修的东西。

### 21.4 边界(说清楚它没做哪些事)

- **回调不等于"界面 30Hz"**:它每帧 (2ms) 都发,界面仍旧按自己的 33ms 轮询读。
  多出来的不是刷新率,是"数据不再冻住";
- **连接期间看不到** `BusTelem::busy` 的老毛病**没动**(§13),理由见 §21.2;
- **`hmi.exe` 免费得到同一份修复**(`ecatworker.cpp` 两个目标共用),但它的按钮形态判据
  里没有回零那几条,所以对它而言只是"灯和位置不再冻住";
- **`motor_test` / `slide_motion` 完全不受影响**:它们没调 `em_set_cycle_hook`,
  `em__cycle` 里那一句就是一次空判断。

### 21.5 这一轮动到的文件

- `motor_api/ec_motor.h` / `.c` / `ec_motor_internal.h` —— 回调那一套(纯增量);
- `hmi/ecatworker.h` / `.cpp` —— `BlockTick`、四个挂点、两处注释(原先写着"直写是
  界面能看见的唯一原因",现在不是唯一了,已改)。

### 21.6 验证到什么程度

**验了**(不需要设备):

- 全量重建 **0 warning 0 error**(`ninja` 全目标:`scan` / `hmi` / `scan_selftest`
  —— 当时还有 `motor_test` / `slide_motion` 两个目标,它们**在那次构建里也是干净的**);
- `scan_selftest` 全绿;
- 逐个看过四个挂点都**不在** `QMutexLocker` 的作用域里(约束 2);
- 逐个看过 `doHome` / `doFaultReset` 里那些 `m_telem` 直写的字段(`homing` 三个 +
  `resetting`)**与同名成员同时置起**,所以回调里 `publish()` 覆盖不掉它们。

**没验**(需要设备):

- 回零那 30 秒里灯确实在动 —— 要看的是**找限位那一趟**:压上开关的那一刻,那一格灯
  该在 ≤ 一个界面周期 (33ms) 内变红;
- 回零时序**没有**被这次改动扰动(约束 3 是"只读"的,但真机才是证据):
  方式 18 与方式 24 各跑一次,与 §20.7 那几条一起看。

---

## 22. 驱动器故障要给出故障码(603Fh)

**现场反馈(2026-09-21)**:"当驱动器故障要给出故障代码"。

### 22.1 问题:6041h bit3 只说"有故障",说不了是哪一个

故障位立起来之后,界面上原本只有一句话(红横幅 + 自动中止那句"驱动器报故障")。
而驱动器那一侧,**过流 / 过压 / 欠压 / 动力线 / 通讯 / 传感器告警的处置办法完全不搭界**
(查机械 / 查供电 / 查接线 / 查干扰 / 查编码器),它们在界面上长得一模一样。
手册里分辨它们的东西就一个:**603Fh 故障码**(U16 RO,与 1003h 低 16 位同源,
表见 `docs/ykd2205pe_ci402.md` §报警与指示灯)。

**603Fh 不在生效的 TxPDO 映射里**(本机只有 6041h / 6064h / 606Ch,60FDh 是可选的第四项),
所以它只能 SDO 读 —— 而 `publish()` 是每帧 (2ms) 跑的,**里面不许有 SDO**:
一条 SDO 读就是几毫秒,挂在 2ms 的圈里等于把圈期交给 SDO 的往返时间,插补会抖。
这条约束与 §21 里 6061h 那条是同一个(见 `EcatThread::readModeDisp` 的注释)。

结论:码**一定是"故障沿之后一拍"才到**的。这不影响正确性,但决定了这个功能的形状 ——
界面上必须能容忍"故障起来了但码还没到"那一拍,而且不能在那拍编一个码出来。

### 22.2 改法:牌子挂在 `publish()` 里,读在圈顶做

| 在哪 | 改什么 |
|---|---|
| `hmi/ecatworker.h` | `AxisTelem` 多一个 `fault_code`;两个哨兵 `HMI_FAULT_CODE_UNREAD` (-2) / `_FAIL` (-1);判据与措辞 `fault_code_meaning` / `fault_code_action` / `fault_code_text` / `fault_axis_text` / `faulted_axes_text` / `fault_code_line` / `fault_banner_text`;`EcatThread` 多 `serviceFaultCodeReads()` 与 `m_fault_code[]` / `m_fault_read_want[]` |
| `hmi/ecatworker.cpp` | `run()` 圈顶(插补之前)调一次 `serviceFaultCodeReads()` —— 唯一的 SDO 读点;`publish()` 只搬值 + 挂牌子;`teardown()` 把码与牌子一起清;`doFaultReset()` 失败那一格带上复位前的码 |
| `hmi/mainwindow.h` / `.cpp` | 同 scan 侧那一套(`m_faultCodeShown[2]` + 两拍)。`hmi.exe` 与 `scan.exe` 共用 `ecatworker.cpp`,**两处的说法必须一致**,所以这里也改了 |
| `hmi/axispanel.cpp` | 单轴状态那行原来只写"故障: <状态字>",现在后面接一个"· 故障码 0xFF02 (过压)" |
| `scan/scanwindow.h` / `.cpp` | `m_faultCodeShown[2]`(横幅上**已经说过**的那个码);故障横幅分**两拍**弹;「故障」灯的 tooltip 点明码在哪看 |
| `scan/scancontroller.cpp` | `healthProblem()` 那句自动中止带上每一根故障轴的码 |
| `scan/selftest.cpp` | `FakeBus::setFaultCode` 注入点 + 四组断言(见 §22.5) |

数据流一句话:`publish()` 看见某轴 bit3 = 1 且 `m_fault_code[i]` 还是 UNREAD → 挂
`m_fault_read_want[i]`;下一圈圈顶 `serviceFaultCodeReads()` 摘牌子、读 603Fh、
把结果写进 `m_fault_code[i]` 并 `note()` 一整句(码 + 意思 + 下一步查哪儿);
再下一帧 `publish()` 把它搬进遥测,界面在那一帧补上横幅的第二拍。
**从故障沿到界面上有码,是两圈 (约 4ms) 加一个界面周期 (33ms)**。

四个必须守住的约定:

1. **一个故障回合只读一次**:读到存值,读不到存 `FAIL`,两种都把牌子摘掉。
   牌子要是每帧重挂,读不到时就成了每 2ms 一条 SDO 去撞一个不应答的驱动器(要等超时),
   比读不到本身糟得多。bit3 掉下去之后 `publish()` 把码置回 UNREAD,于是下一次故障会重新读;
2. **`0000h` 不许兼"还没读"**:它是"无错误"这个真实读数。所以是两个负数哨兵,
   与 §18 里 6061h 的 `HMI_MODE_DISP_UNREAD` 同一个坑、同一个解法;
3. **只在 `run()` 圈顶读**:`publish()` / `interpolate()` 以及 §21 那个回调里都不许出现 SDO;
4. **`m_fault_code` / `m_fault_read_want` 只有工作线程碰**,`teardown()` 里清干净 ——
   换了台驱动器还挂着上一台的码,是最难查的那种假象。

### 22.3 三种"不知道"是分开说的

| 取值 | 界面上的说法 | 什么时候出现 |
|---|---|---|
| `HMI_FAULT_CODE_UNREAD` (-2) | 还没读到 (正在读) | 故障沿那一拍,或阻塞命令期间(§22.4) |
| `HMI_FAULT_CODE_FAIL` (-1) | 读不到 (SDO 没应答) | 试过了,这一趟没拿到 |
| `0x0000` | 0000 (无错误) | 真读到了,驱动器自报无错误(多半是上一轮的故障位没清) |
| 其他 | `0xFF02 (过压)` 这样 | 真读到了 |

手册之外的码照实报十六进制加一个"(手册之外)",**不猜名字** —— 猜一个名字比说不知道坏得多
(与 §18 里 `mode_text` 对模式号 2 的处理同一条规矩)。

### 22.4 边界

- **阻塞命令期间读不到**:`em_home` / `em_fault_reset` 那几秒里 `run()` 的圈顶跑不到
  (那是 §21 修的另一件事),`publish()` 由 `BlockTick` 从帧里被调到,只挂得上牌子。
  所以**找限位途中报故障,横幅立刻有、码要等这一条命令退出来**;
- **码不跟着故障清掉而保留**:bit3 一掉,`publish()` 就把码置回 UNREAD。
  要留痕看控制台 —— `note()` 那条不会被擦掉。一根健康的轴旁边挂着一句 `0xFF02 (过压)`,
  等于在说它现在过压;
- **不解析 1003h 的故障历史**:那是 10 条环形缓冲,读法完全不同,这一轮不做;
- **不做"按码自动处置"**:码只是信息,驱动器的动作(退电/停机)由它自己按手册决定。

### 22.5 验证到什么程度

**验了**(不需要设备):

- 全量重建 **0 warning 0 error**(`ninja` 全目标:`scan` / `hmi` / `scan_selftest`)。
  中间有一轮 `scan.exe` 链接被占用挡住(旧的那份还在运行,`ld` 报 `Permission denied`),
  所有 `.obj` 那一轮就编过了;关掉窗口后补的那一次链接正常;
- `scan_selftest` **597 passed / 0 failed**;新增四组(下面那些)全绿;
- 逐条对过手册那张表 (`docs/ykd2205pe_ci402.md` §报警与指示灯);
- 新增的判据都是 `inline` 放在 `hmi/ecatworker.h` 的 `ecatcmd` 命名空间里 ——
  与 §18 同一个理由:`scan_selftest` **不编 `ecatworker.cpp`**,写在 .cpp 里就永远验不到。
  新增的断言:

  1. 表逐条 (0000/FF01/FF02/FF03/FF04/FF06/FF08) + 十六进制要照实写 + 相邻的码不许抄串 +
     手册之外的码说"(手册之外)";**表里每个码都要有一句处置办法**(少一条,红横幅上就少半句),
     而**手册之外的码不给处置办法**(没有依据的建议比没有建议坏);
  2. 两个哨兵是负数、互不相同、都不等于 0,而且 `还没读到` / `读不到` / `无错误`
     **三句话两两不同**;
  3. 拼进 `"轴X 603Fh = ……"` 之后 **`603Fh` 只出现一次**(值那一段要是自带索引,
     整句会变成"轴X 603Fh = 正在读 603Fh…");
  4. 横幅:没故障时不许提 603Fh;只有轴Y 故障时不许点名轴X;
     两根都故障时**各自的码都要在**;有真实码时带上"下一步查哪儿";
     带真实码的自动中止那句话里要有 `6041h bit3` / `0xFF02` / `过压` / `轴X`(且不出现 `轴Y`);
     UNREAD 那一拍照实说"还没读到"且**不许出现任何十六进制数**。

**没验**(需要设备):

- 真机报故障时读回来的码是否与面板 ALM 灯的红闪次数对得上。手册里 ALM 的闪次就是这几个
  数字 (1 过流 / 2 过压 / 3 欠压 / 6 通讯 / 8 传感器),**对不上就先别信这条 SDO 读**;
- 驱动器正在报故障时它的邮箱通道还答不答 SDO(应答的话第一拍之后就有码,不应答就要等
  `FAIL`)。这是"一个回合只读一次"这个决定的对赌点:真机上更常见的若是"应答得慢但不是
  不应答",那这一条应当改成**限次重试**,而不是一次就放弃。

---

## 23. 不连滑台也能用功率计:参数栏里那块一直露在外面的功率计框

**现场要求(2026-09-22)**:"我要这个程序没有连接滑台的时候也能连接使用功率计 读取功率计数据"。

### 23.1 先核实:那条路本来就是通的,挡住人的不是总线是界面

先把事实说清楚,因为它决定了这一轮改的是什么。

`PowerMeter` / `OphirMeter` **一个 EtherCAT 帧都不发** —— 它们走 `OphirLMMeasurement` 的
COM 接口,跟网卡、跟 `EcatThread` 没有任何关系。`ScanWindow::refresh()` 里给「读一次」算的
闸门也只看了 `m_meter->isOpen()`,**没有 `m_connected`**(见 §17)。也就是说:不插滑台、
不连总线时,程序其实**已经能**读功率计。

真正挡住人的是**可用性**,有两条:

1. 要选真机(`真机 PD300R`)得先点参数栏那块「功率计」框的「编辑」—— 而那一框从标题到
   里面每一格都是为扫描服务的(手填值 / 噪声 / 脚本路径 / 读一次,全是"扫描之前先把它设好"
   的那套东西)。不连滑台的人进不去,进去了也全是无关的控件;
2. 那块框里的东西**在扫描跑的时候归扫描状态机**。同一个源上再插一个连续读数,就撞上了
   §7 那条"同一时刻只允许一个未决请求"。

所以这一轮**不是**给功率计加一条脱离总线的通路(**它本来就有**),而是把它**从编辑门控里
拿出来、摆到明面上**。

#### 23.1.1 中间走过的一条弯路:独立窗口 + 顶部菜单栏(当天做出来又拆了)

第一版做的不是这个。第一版是把整块功率计搬进一个**独立的 `QWidget` 窗口**、参数栏只留一行
「取样源 + 一行状态」,窗口从**顶部 `QMenuBar`** 的一条「功率计」菜单点开
(`打开功率计窗口… Ctrl+M` / `读一次`),`main.cpp` 里还为此补了 `QMenuBar` / `QMenu`
那几条样式(不补就是白底浅灰字,等于看不见)。

那一版能编、能跑、自检全过,但**形状是错的**,提要求的人当场就指出来了:"不要是个顶部菜单栏
应该是一个外露的界面"。理由站得住:菜单栏里的东西**要先点一下才存在** —— 一个"我得先知道
它在哪儿才找得到"的界面,不算露在外面;而这个程序的用法是"先单独接一下表看看数",那一步
不该有任何前置动作。

于是当天拆掉,改成现在的样子:**整块框搬回右栏原位,去掉编辑门控**。
拆掉的痕迹只剩两处,都是有意留着的 —— `buildMeterPanel()` 顶上那段注释(记着"搬出去过、
当天又搬回来"),以及本小节。**`meterwindow.h/.cpp` 已经删掉**,`MeterCurve`(那条折线)
单独留在 `metercurve.h/.cpp`,`main.cpp` 里那几条菜单样式一并删掉(留着就是死样式)。

代价是明摆着的,也是当初选它时就知道的:右栏因此变长,要滚动才看得全(参数栏那一列本来
就在 `QScrollArea` 里,见 §16);折线的宽度只有那一列的宽度(最窄 320 px,拉宽了也就 400
上下),所以 `MeterCurve` 的左边距是按**这一列的宽度**算的,不是写死的。

### 23.2 两条硬约束,它们决定了整个形状

| 约束 | 出处 | 推出什么 |
|---|---|---|
| **同一时刻只允许一个未决请求**(违约不报错,只会静默分错数) | `powermeter.h:28-31` | 仲裁点只能有**一处**。原来是两个发起方(`ScanController` / 「读一次」),这一轮加到**三个**,仍然只能是 `ScanWindow::refresh()` 那一处 |
| **Ophir 表头是独占的**(第二个实例打开同一个头会 `0x80040201`) | `ophirmeter.cpp:308`、`ophircom.cpp:134` | 真机那一路**只能有一个 `OphirMeter`**(`ScanWindow::m_ophir`),界面不管长成什么样都只是它的界面 |

第二条是本节的枢纽:它把"功率计那一块该不该有自己的源"这个问题直接封死了 —— 不能有。
第一版那个独立窗口也**没有**自己的 `PowerMeter` 指针的所有权(它拿到的是一份指针副本,
与主窗口 `m_cbMeter` 指向同一批对象),换成现在的右栏面板之后这一点更是天然成立:
**换源仍然只有一个写入口**(`ScanWindow::onMeterChanged`)。

### 23.3 三个请求发起方与仲裁表(唯一写点:`ScanWindow::refresh()`)

| 发起方 | 什么时候能发 | 什么时候让位 |
|---|---|---|
| ① `ScanController`(扫描) | `armRun()` 过闸(照旧) | 另两个在场时它也在跑,不存在让位 —— 它是最高优先级 |
| ② 参数栏那个「读一次」按钮 | `m_meter->isOpen()` **且** `!m_ctl->running()` **且** `!m_readPending` **且** `!m_mlog->running()` | 每一条都拼在按钮的 tooltip 里(`refreshMeterPanel()` 里那个 `why` 链),不是只说"现在读不了" |
| ③ `MeterLog`(连续读数) | `running() && !held()` | `held = m_ctl->running() || m_readPending` |

三条要在意的地方:

- **让位不是"拒绝",是"不开新的口"**。每个发起方本来就是一问一答,已经发出去的那一个照旧
  回收 —— 各家的 `sender()` / `pending` 比对会把不属于自己的那份丢掉。所以 `refresh()`
  每拍只做一件事:把两个开关摆成此刻该有的样子,**不记"谁先按的"**。
  那一框里的按钮同理:**每拍照设,不缓存**(全在 `refreshMeterPanel()` 一处算,由
  `refreshEditability()` 转调)—— 记住"上次是什么样"正是漏掉一次同步的来源;
- **`hold` 期间 `running()` 照旧是 true**。它是"还在采,只是不发"。判断"能不能启扫"要看的是
  另一个:`MeterLog::issuing()`(= `running() && !held()`)。这两个名字很像,是刻意的:
  界面上显示"采集停着"和"这个源被占着"本来就是两件事;
- **扫描跑的时候那一块不抢源,改画扫描采的那些点**。这就是"跟随":`ScanController::pointLogged`
  带上 `(ix, iy, ok)`,配 `cellValue()` / `cellHasValue()` 取值。这一路是**白捡的** ——
  那个信号在 `m_watts[cell]` 写完之后才 `emit`(`scancontroller.cpp:666`),所以槽里读到的
  一定已经是这一点的值,不需要任何额外的同步。跟随时**不写 CSV**:那些点本来就写在扫描
  那份 CSV 里,再写一份就是同一趟数据两个文件,迟早对不上。

### 23.4 顺手关掉的一个既有隐患

`m_btnStart`(开始扫描)原来的可用条件里**没有 `!m_readPending`**。于是这样一条路子是通的:
点「读一次」→ 那个请求还在飞 → 点「开始扫描」→ `armRun()` 过闸(它只看扫不扫得动)→
扫描的第一个点又向同一个源要一次数。**两个未决请求压在一个源上**,而那正是
`powermeter.h` 说的"静默分错数"。

这一轮把 `!m_readPending` 加进了按钮条件,`onStartClicked()` 头一行也补了同样的守卫
(按钮灰掉不是保证,键盘/快捷键/程序内部调用都绕得过去),并在 `m_ctl->start()` **前一行**
`m_mlog->setHold(true)` 兜住"这一拍 `refresh()` 已经跑过去了"的缝。

### 23.5 改到的东西

| 文件 | 改什么 |
|---|---|
| `scan/meterlog.h` / `.cpp` | **新**。连续采样的**全部逻辑**(不含界面,不碰 SOEM),进 `SCAN_COMMON_SRC` |
| `scan/metercurve.h` / `.cpp` | **新**。那条时间-功率折线,手画(`MapCanvas` 是范例)。**只要 Qt6::Widgets**,所以只进 `scan` 目标 |
| `scan/axisutil.h` | **新**。`niceStep()` 从 `mapcanvas.cpp:716` 的文件内 static **抽出来** —— 两张图都要用,抽出来才只有一份实现 |
| `scan/mapcanvas.cpp` | 改成 `#include "axisutil.h"`,删掉本地那份 `niceStep` |
| `scan/scanwindow.h` / `.cpp` | 那一框**整块扩回原样**(取样源 / 各源参数 / 真机三项 / 读一次 / 连续读数 / 曲线 / CSV 导出),外加 `m_mlog` / `m_curve`;`refresh()` 里的三方仲裁;`pointLogged` 里喂跟随数据;新增 `refreshMeterPanel()`(那一框**全部**可用性与露不露,每拍重算)与 `refreshMeterReadout()`(那一框的字);`GI_METER` **不进 `GateIdx`**(没有"参数"可保存) |
| `scan/scanprefs.h` / `.cpp` | 两个键:`meter/interval_ms`(缺省 200)、`meter/csv`(缺省空 → 开始时按时间戳起名),`saveSettings()` 里跟着落盘(那一框没有「保存」可点,所以是"改了就在这儿被记下") |
| `scan/selftest.cpp` | `FakeMeter` 多两个计数(`requests()` / `overlaps()`,后者**正常必须恒为 0**)+ `MeterRig` 台架 + 九组 `meterlog` 断言 |
| `scan/CMakeLists.txt` | `meterlog` / `axisutil.h` 进 `SCAN_COMMON_SRC`,`metercurve` 进 `scan` 目标 |
| `scan/main.cpp` | 那几条 `QMenuBar` / `QMenu` 样式随独立窗口一起**删掉**(加了又删,不留死样式) |

「读一次」的结果(含往返 ms)**就近显示在按钮下面那一行** `m_lReadout` 里 —— 曲线、统计、
那行读数在同一框里,读的人和按的人看的是同一块地方。

顺带一处形状上的调整:那个 `m_readTimer`(「读一次」的兜底超时)留着没删。它是"回话永远不来"
时唯一的出路,删了的话一个卡住的 `m_readPending` 会**永久**压死「开始」。

### 23.6 一个不显然的决定:超时就**停在那儿等**

`MeterLog` 给每个请求配了 3000 ms 的看门狗(比 Ophir 自己 1800 ms 的 stale 超时宽,让它先
开口)。到点的时候有两条路,这里选了不常见的那一条:

| 做法 | 后果 |
|---|---|
| 记一笔 `ok=false`,**重新发一个**(第一直觉) | 那个超时的请求**并没有被撤回**,源迟早还会把它那一份回话投递回来。那时 `m_pending` 又已经是 true,那份迟到的回话会**冒充**新请求的答案 —— 一个几百毫秒前的旧数配上新时刻,**一个字节的错都不报** |
| 记一笔 `ok=false`,**停在那儿等那一个回话**(这里的做法) | 计数不再涨;回话一到就照常记下来(用"我们知道它的时刻")并续采。源彻底卡死就一直停着,`timedOut()` 为 true,那一框里那行字变成「**卡住了**」(黄字加粗),靠「停止」/「开始」或换源出来 |

换句话说:**这一条是把 `powermeter.h` 那句约定在本类里变成字面为真**,代价是遇到卡死的源
会停住而不自动绕过。停住这件事被做成了**看得见的**(那一框里那行「卡住了」+ 一次 `failed`),
不是悄悄绕过去的 —— 一个自己不会说"我卡住了"的采集器,比一个卡住的采集器坏得多。

### 23.7 几处与初版计划的偏离(以及为什么)

| 计划里写的 | 实际做的 | 为什么 |
|---|---|---|
| 控制器加 `setMeterBusy()`,`armRun()` 里拒 | **没做** | 界面在 `m_ctl->start()` **前一行**、同一个栈上把 `MeterLog` 置 hold,中间没有事件循环 —— 控制器那一侧的守卫是**到不了的死代码**。加一句永远为假的判据,比少一句更坏(与"注释不许撒谎"是同一条) |
| 换源时"缓冲留着" | **缓冲一起清掉** | 一条曲线顶上只写得出一个源的名字。一半的点来自另一个源而看不出来,那就是一条会骗人的线。宁可少一段可见的历史(要紧的那份在 CSV 里) |
| `beginRecord` 截断写 | **接着写(追加)** | 一次连续记录停了再按「开始」,不该把前面那些数抹掉 —— 抹掉没法撤销,而"两个文件"在文件夹里一眼能看出来。空文件才写表头;顺手补一个"上次被强杀留下的残行少个换行"的补丁 |
| 扫描起来时"拒绝"连续读数 | **自动进 hold** | 拒绝会让人以为坏了(按了开始,什么都没发生)。进 hold 是"还在采,只是不发,改跟扫描" —— 曲线上照样长 |
| 删掉 `m_readTimer` | **留着** | 它是"手动读的回话永远不来"时唯一的兜底。删了的话一个卡住的 `m_readPending` 会**永久**灰掉「开始」(仲裁表里"手动读在飞"那一格) |
| 跟随模式也开一个 CSV | **不开文件** | 写出来只有一行表头。一个"看起来在记录"的空文件比不记录更坏(与扫描那边"记录开不了就不采"同一个道理) |
| 看门狗"记一笔并续采" | **记一笔并停着等**(见 §23.6) | 上面那张表 |
| 一个**独立窗口**装这些东西,菜单栏当入口(第一版真做出来了) | **拆掉,搬回右栏** | 见 §23.1.1 —— "先点一下菜单才看得见"就不叫外露 |

### 23.8 验证到什么程度

**验了**(不需要设备):

- `scan` 与 `scan_selftest` 全量重建,**0 warning 0 error**;
- `scan_selftest` **695 passed / 0 failed / 0 skipped**(上一轮是 597)。新增九组都用手拨的钟
  跑完,不需要硬件也不需要界面:
  1. **到点才发** —— 两笔之间的间隔落在 `[间隔, 间隔 + 往返 + 一拍钟]`,既不提前也不合并;
  2. **未决期间再拨多少拍也不发第二个** —— 特意让往返(500 ms)远大于间隔(20 ms),
     固定节拍的做法会在这里堆出一串请求,这里 `overlaps()` **必须是 0**;
  3. **换源** —— 缓冲清空、未决请求作废;旧源**迟到的**那份回话被丢掉(先断言旧源确实回了一份,
     否则这一条是空的);新源照常喂数;
  4. **环形缓冲** —— 采 `容量+50` 个,留在最前面的正是**第 51 个**(丢的只能是最旧的);
  5. **统计** —— 只数 `ok` 的;标准差是样本标准差;空缓冲的 `sd` 是 **0 不是 NaN**(NaN 会一路
     糊到界面上);
  6. **看门狗** —— 记一笔 `ok=false`、只喊一次、`requests()` 停在 1(不重发)、迟到的那份回来
     后收下并重新走起来;「停止」是另一条出路;
  7. **`setHold`** —— 期间一个请求都不发、`running()` 照旧 true、放开之后**从当时重排**而不补采
     欠下的那些(补出来的是编的);
  8. **CSV** —— 表头逐字固定、`watts` 空/`ok=0` 的口径、行数与样本数一致、追加不覆盖;
  9. **源没打开 / 没给文件名** —— 都是 `false` + 原因,不留半开的状态。

**没验**(需要设备或需要人):

- 真机上连续读数能不能跟上真机的往返节奏(这一条只验了"不会堆请求",没验"够不够快");
- 真机在流中途被改波长/量程/模式(走 `setWavelengthIndex` 那套 stop→set→start)时,
  `MeterLog` 的节奏会不会被那几秒打断(它的看门狗是 3000 ms,**Ophir 改配置那一下可能更长**,
  真机上是"每个配置回合记一笔 `ok=false`"还是"干脆卡住",要实测才知道);
- 关窗/退出时那个未决请求的回话落在**已经析构的对象**上的情形(靠 `m_mlog` 是 `ScanWindow`
  的子对象、`closeEvent` 里先 `stop()` 兜着,但真机上没人走过这条路);
- 不连总线跑 `scan.exe` 的那几条人工检查(那一框在不开总线时能不能选真机、切到「随机(噪声)」
  点「开始」看得见曲线往上走、导出的 CSV 对得上、跑一趟扫描时它切「跟随扫描」)—— 这几条要
  **人**在界面上看,自检里没有界面。**只做了能自动做的那一半**:不连总线启动 `scan.exe`,
  进程活着、`stdout` 里枚举出 9 块网卡、不崩。

---

## 24. 长度单位改称 mm(2026-09-22)

**现场要求**:"区域的单位是 mm 改一下描述"。

界面上原来把它叫"单位"(画布左下角那两个字甚至专门写着"说不清是毫米还是脉冲"),而现场
那台机器的丝杠**一圈走 1 mm**,`pulses_per_unit` 的缺省 50000 就是这么标定的 ——
所以那个量一直就是 mm,只是一直没写出来。这一轮把**给人看的字**全改成 `mm`。

### 24.1 改了的字(全部)

| 在哪 | 原来是 | 现在是 |
|---|---|---|
| 「区域 X / 区域 Y / 分辨率」三个输入框的后缀 | ` 单位` | ` mm` |
| 「1 单位 =」那一行的行标 (它在参数框里的名字) | `1 单位 =` | `1 mm =` |
| 「1 单位 =」的 tooltip | "1 单位 = 多少脉冲。默认 50000 = 2400h 实测的一圈" | 多两句: 缺省对应"丝杠一圈走 1 mm"、导程不同的机器要自己改 |
| 参数栏那行网格统计 | `±%4 单位` | `±%4 mm` |
| 画布左下角标尺交会处 | `单位` | `mm` |
| 画布视野说明 | `视野 ±%1 单位 (固定)` | `视野 ±%1 mm (固定)` |
| 画布左上角第三行 | `区域 %1 × %2 单位` | `区域 %1 × %2 mm` |
| 画布下沿悬停读数 | `(%1, %2) 单位 = (%3, %4) pul` | `(%1, %2) mm = (%3, %4) pul` |
| 选中那一格的坐标行 | `[%1, %2]  (%3, %4) 单位` | `[%1, %2]  (%3, %4) mm` |
| 「恢复默认」的横幅 | `区域 … 单位, 分辨率 …` / `1 单位 = …` | 同样换成 `mm` |
| 「开始扫描」的横幅 | `区域 %1 × %2 单位` | `区域 %1 × %2 mm` |
| 参数校验的报错 | `「1 单位 = ? 脉冲」必须是正数` | `「1 mm = ? 脉冲」必须是正数` |
| 续扫几何不一致的那句诊断 | `区域X(单位)` / `分辨率(单位)` / `每单位脉冲数` | `区域X(mm)` / `分辨率(mm)` / `每 mm 脉冲数` |

### 24.2 **没**改的(有意留着的)

- **CSV 表头一个字节都没动**:`x_unit` / `y_unit`、`# area_x_unit=` / `res_unit=` /
  `pulses_per_unit=` 全是**已落在文件里的格式**,续扫靠读它们做几何校验 —— 改名会让以前
  采的那些 CSV 再也接不上。它跟 §10 那条"`pos_x_pul` 这类列一个都不能少"是同一条规矩;
- **代码里的名字**:`Params::area_x_unit` / `res_unit` / `pulses_per_unit`、`pxOf(u)`、
  `viewHalfUnits()`、`kViewHalfUnits` 这些一个没动。**改字不改名**是有意的: 名字一改,
  这一轮的 diff 会淹没在重命名里,而真正要验的只是"屏幕上那两个字对不对";
- **§13 那几张当时的核对记录**(`k = 19.09 px/单位`、"`[36, 29] (4.50, 1.00) 单位`"、
  "我要的是 16 单位")—— 那是**当时屏幕上真的那么写**的记录,按本仓库"注释不许撒谎"
  的口径,历史记录不跟着现在改。§3 顶上那句话已经把新旧叫法对上。

### 24.3 验了什么

- `scan` 与 `scan_selftest` 重建,0 warning 0 error;自检 **695 passed / 0 failed / 0 skipped**
  (这一轮没动任何逻辑,自检里也没有断言钉着这些界面字符串 —— 它验的是几何与状态机);
- 剩下的是**要人看的**:那一行 `mm` 后缀、画布角上那个 `mm`、悬停读数、两处横幅。
  这些在自检里看不见。


## 25. 读数单位 / CSV 记的是"谁采的" / 缓冲满了要说(2026-09-22)

**现场要求**:"会误导人的三处(建议先改) 把他改了 / 此外界面上充分展示有接口和在驱动层的的内容"。

前一句是这一轮的主线(§25.1~§25.3),后一句把**接口层与驱动层已经有、界面上却看不见**的东西
摆出来(§25.4),并且把**这一轮仍然不做**的几样连同理由一起写清楚(§25.5) —— 免得回头说不清
是"没做"还是"没接口"。贯穿全篇的口径只有一条:**不知道就不写**。宁可屏幕上出现「单位不明」,
也不许哪一处替设备编一个单位。

### 25.1 一条读数到底是什么单位

三处里最坏的一处:`PowerMeter::readingReady(double watts)` 那个参数叫 `watts`,界面上写的是
`%1 W`,曲线纵轴写 `W`,连续读数那份 CSV 的列名就叫 `watts` —— 而**真机报的不一定是瓦**。

- Ophir 那份数组是 W 还是 J 由**探头与测量模式**定,而 COM 接口**一个字段都不给**(手册
  GetData 只说"the data array",没有单位参数)。于是只能从设备**自己的字**里判:

  | 判据(按优先级) | 结论 |
  |---|---|
  | 测量模式名里带 `Energy` | `J` |
  | 测量模式名里带 `Power` | `W` |
  | 模式名是**别的东西**(例如 `dBm`) | **不认** |
  | 模式这一项不存在,探头类型带 `pyro` | `J` |
  | 模式这一项不存在,探头类型带 `thermo` / `photodiode` | `W` |
  | 都没说清 | **不认** |

  实现只有一处:`scan/ophirmeter.cpp` 的 `unitFromDeviceInfo()`(声明在 `ophirmeter.h`,
  `scan_selftest` 直接调它验上面那六条)。

  **模式名在的时候不许探头类型替它翻案** —— 同一只热电堆探头切到 `dBm` 模式,报回来的那个数
  不是瓦(是对数),那时按探头类型说 W 就会让一个对数值顶着瓦的单位被记进 CSV。这是这一轮里
  唯一一处"越判越严"的地方:代价是以后遇到没见过的模式名会显示「单位不明」,而那正是要的 ——
  它是**看得见**的,一个编出来的 W 是看不见的。
- 三个模拟源按定义就是 W(那份 CSV 那一列本来就叫 `watts`),于是 `PowerMeter::unit()` 的缺省
  返回 `"W"`,真机把它覆写成判出来的那个(可能是空)。
- **界面上一共五处**跟着源走,一处都不许自己写单位(全部经 `powermeter.cpp` 的 `unitLabel()`
  取,没有第二份判断):

  | 在哪 | 判不出来时写什么 |
  |---|---|
  | 「读一次」那行读数 | `单位不明` |
  | 连续读数的「最近 / 最小 / 最大 / 平均 / 标准差」 | 同上 |
  | 曲线左上角 | 纵轴画 `?`,理由在 tooltip 里 |
  | 色标那一框新加的一行 | `单位不明 —— 色阶刻度的数是 W 还是 J 还没判出来 (见功率计那一框)` |
  | 真机的状态行 | 末尾缀 `单位 W` / `单位不明` |

  色标那一框原来只有「色标 (功率)」这个名字里写着一个单位,**数值上没有一个字**说明它是什么
  —— 现在那一行写着 `单位: %1 (跟着取样源)`。

### 25.2 两份 CSV:这一份是"哪个探头 / 波长 / 量程 / 模式 / 什么单位"采的

原来两份 CSV 都只记几何(扫描那份)或者什么都不记(连续读数那份),回头复核时**说不清那一列数
是什么单位**。

- 列名**一个字节都没动**(`watts` 照旧叫 `watts`,连续读数那份的表头照旧逐字是
  `unix_ms,elapsed_ms,watts,ok`)。改的是**文件头那几行 `#`**:

  ```
  # meter_source=ophir
  # meter_unit=J
  # meter_device=Juno+  meter_device_serial=…  meter_sensor_type=pyroelectric
  # meter_wavelength=1064nm  meter_range=…  meter_mode=Energy
  ```

- 同一份 builder:`powermeter.cpp` 的 `meterMetaLines()`(接口层)加 `OphirMeter::configLines()`
  (真机自己报的那些)。扫描那份经 `ScanLog::beginNew` 的 `extra_meta`,连续读数那份经
  `MeterLog::setMeta` —— **没有第二份实现**。
- 全 ASCII(判不出来时写 `meter_unit=unknown`):CSV 的 `#` 行一直全是 ASCII,中文字进去读的人
  就得先猜编码。
- **续扫的兼容性判定一个字都没变**:读回时只认那几个几何 key(`scanplan.cpp` 的 `metaGet`),
  多的行没人看。自检里有这一条 —— 带 meter 行的文件照样 `csvParseForResume` 得干干净净。
- 老文件照旧读得进来(多出来的只是它自己没有的那几行)。
- **一处已知的限制**(写进了 `meterlog.h` / `scanlog.h` 的注释):沿用同一个文件续写时**不重写**
  头那几行(与列表头同一个规矩 —— 那是"这份文件是怎么来的",不是"这一行是怎么来的"),所以
  续写前换过探头/波长/量程/模式的话,那份文件的名字就与内容不符了,该另起一个文件。
- §24.2 那条"CSV 表头一个字节都没动"照旧成立:动的是 `#` 行,不是列名与格式。

### 25.3 环形缓冲满了要说

`MeterLog::kCapacity = 20000`,满了就在丢最旧的 —— 而**屏幕上那条曲线丢了数看着和没丢一样**。
新增 `MeterLog::full()`,参数栏那行计数在满的时候多一句并用告警色:

```
(**满了** —— 最旧的正在被丢掉; 要全都留着就打开 CSV)
```

判断只写在 `refreshMeterReadout()` 一处,`full()` 自己也只是 `size() >= kCapacity`。

### 25.4 新露出来的东西(参数栏里,不进门控)

| 新控件 | 露的是接口/驱动层的什么 | 为什么值得露 |
|---|---|---|
| 「模拟延迟」ms 旋钮 | `RandomMeter::setDelayMs` / `ScriptMeter::setDelayMs` —— **装出来的往返时间** | 没有真机时也能看出「读一次」的往返时延、连续读数为什么是"回话驱动排下一次"、把看门狗(3000ms)调过去时超时那一路怎么表现。两个模拟源共用这一个数(它们本来就该一样);手动源是立刻回,不露这一项 |
| 「每次平均」旋钮(1..64) | `MeterLog::setAverage` | 与扫描参数里的「每点采样」是同一件事 —— 那边少了它噪声大的源读不稳,这边少了它同一条曲线上"扫描那趟"与"手采那趟"的噪声水平对不上。N 个读数是**连着要**的(中间不隔一个间隔),「间隔」是采样之间的间隔;N 次里有一次没读回来这一笔就作废(与扫描 `finishPoint` 同一个口径) |
| 随机源的「基值」「噪声」 | `RandomMeter::setBase` / `setNoise` | 原来只能选"随机源"却改不了它的缺省 1.0 / 0.1;现在能把量级对到真机附近 |
| 真机状态行末尾的「版本: 对象 … 驱动 …」 | 驱动层的 `getVersion` / `getDriverVersion` | 出事时能一眼说清装的是哪一版。取不到就整段不显示(它是诊断,不因为它打不开设备) |
| 取样源下拉第 3 项的两句灰化原因 | `OphirCom::isRegistered()` 与 `isAvailable()` | 原来两种坏法(没装 / 装了但 typelib 读不出来)都只会以"打开失败"的样子出现 |

### 25.5 有接口、驱动层里也有,但**这一轮仍然不做**的

| 能力 | 为什么不露 |
|---|---|
| `OphirCom::addWavelength`(往设备里加一个波长) | 那是**写设备**:写进去的东西留在表头自己的配置里,而这里没有真机可以验证它到底写成了什么、会不会与 StarLab 里的表冲突(手册也没说这个列表存在哪儿) |
| 多通道选择 | Juno+ 是**单通道**设备(手册 Conventions:"For devices with only one channel <channel> should be 0"),现在写死 `k_channel = 0` |
| `stopAllStreams` / `closeAll` | 收尾用的内部清理,`close()` 里已经在做。做成按钮只会多一条能把设备弄成半开状态的路 |
| `configureStreamMode` | **故意一个字节都不调**:现在的流参数是实测出来的(见 §23 的 stale 超时与轮询间隔),换一种流模式要先有一台真机重测 |

### 25.6 验了什么

- `scan` 与 `scan_selftest` 重建:**0 warning 0 error**(`-Wall -Wextra`);
- `./bin/scan_selftest.exe`:**752 passed / 0 failed / 0 skipped**。新增的那些是:
  `unitFromDeviceInfo` 的六条判据(含 `dBm` 那条)、`meterMetaLines` / `unitLabel`(「单位不明」
  与 `unknown` 两个写法)、平均的三条路(凑够 N 个 / 第一个子读数就失败 / 攒到一半才失败)、
  `full()`、meta 行**逐字**落在文件头而列名照旧、带 meter 行的 CSV 照样能续扫;
- 这一轮**改了一处实现**,不只是加断言:`unitFromDeviceInfo` 原来定义在 `ophirmeter.cpp` 的
  匿名空间里,与头文件那份声明形成二义(g++:`call of overloaded ... is ambiguous`)—— 已移到
  `namespace scan` 里;
- 剩下的是**要人看的**:那五处单位字符串、色标那一行、满了那句话、两个新旋钮、真机状态行末尾
  那个版本。

---

## 26. 界面文案规范(2026-09-22)

### 26.1 为什么值得单独立一条

不立规范,文案就按"谁在哪一天加了什么功能"长:每加一个功能就往 tooltip 里补一段"为什么这么
设计",几年下来参数栏里最长的一条到了 **299 字 / 五段**,一条报错到了 180 字 —— 那些理由对
操作员没用(它该在代码注释里,而注释本来就在写),而且把当下要做的那一句判断埋了。

同一轮还查实三件与规范无关、但必须一并修的:

| 硬伤 | 实情 |
|---|---|
| `**粗体**` 从来没粗过 | 这些字进的是 `QLabel::setText`(默认 `Qt::AutoText`)与 `QToolTip`,**纯文本**,只认 HTML 不认 `**` —— 屏幕上一直显示着字面的星号 |
| 笔误 | 「保持保持力矩」(三处)、「轴 %1 的 %2已发出」少一个空格、「正在连接...」用了半角省略号 |
| 同一样东西几个名字 | 「网格间距」/「分辨率」、「色阶」/「色标」、「本程序这一侧」/「上位机侧」、`->`/`→` |

范围 = **scan 的界面字 + scan 与 hmi 共用的那几个文案函数**(`hmi/ecatworker.h` 的 `ecatcmd::*`:
`mode_text` / `fault_code_text` / `fault_axis_text` / `fault_code_line` / `faulted_axes_text` /
`fault_banner_text` / `home_end_text` / `home_cause_text` / `home_lim_refusal` /
`home_lim_branch_text` / `limit_switch_text` / `limit_hit_advice` / `limit_hit_headline` /
`axis_label`)。**它们是同一份源码**,所以改这些会同时改掉 `hmi.exe` 的屏幕字 —— 这是接受的:
hmi 与 scan 说的是同一件事,不该有两套说法。`hmi/mainwindow.cpp` / `axispanel.cpp` 自己的窗口字
这一轮不动。

`scan/selftest.cpp` 那些断言说明**不重写**:它面向的是开发者,口径本来就是"哪一条断言失败了
必须看得见";只做了同一层的机械规范化(去 `**`、全角括号改半角)。(**条数故意不写在这里** ——
它每次加功能都变,写死的那个数迟早是假的;唯一那一份在 `README.md` 里,由每次改动顺手更新。)

### 26.2 规范

**标点**

- 面向人的字符串里,分隔符一律半角 `,` `:` `;`(后面跟一个空格),`( )` 也用半角且紧跟右括号不
  留空格;句末与列举用全角 `。` `、` `——` `「」` `…`。这不是新定的 —— 全仓统计下来本来就只有
  `selftest.cpp` 有 22 处全角括号,这一轮把那 22 处也改了,于是全仓一致。
- 完整句子以「。」收尾;按钮名 / 标签 / 下拉项 / 单位后缀 / 标题**不带**句号。
- 禁用:`**` 一类标记(Qt 不认)、`->`(写 `→` 或写成字)、`...`(写 `…`)、以 `!` / `?` 收尾
  (改陈述句)。
- **单位一律英文符号**,与数值之间一个空格:`200 ms`、`0.5 mm`、`20000 pul/s`、`±500000 pul`、
  `1 W`、`1 J`。数字后面不写「秒 / 毫秒 / 毫米 / 个脉冲」;「脉冲当量」这类词条照旧。
- 寄存器与位号原样:`6041h bit3`、`0xFF02`、`6098h`。

**长度**(超了就是没精简完)

| 位置 | 上限 |
|---|---|
| 普通 tooltip | 2 行 / 60 字 |
| 操作类 tooltip(找限位、回零这种要预告分支的) | 4 行 / 90 字,每行 ≤30 字 |
| 参数栏横幅(`hint()`) | 1 句 / 40 字 |
| 报错、拒绝理由 | 「现象 + 出路」/ 50 字 |
| 模态框正文 | 80 字,一条一行 |

**内容**:屏幕上只留操作员当下要做的那个判断(这是什么 / 会怎样 / 该怎么选)。**设计理由、历史、
取舍一律移进代码注释** —— 移之前先看附近有没有已经写过的,有就别重复写第二遍;注释同样不许啰嗦。

**术语表**(唯一写法)

| 概念 | 用 | 不用 |
|---|---|---|
| 那台仪器 | 面板标题「功率计」;泛指「取样源」;真机「真机」 | 单字「源」当主语 |
| 运动部件 | 机械整体「滑台」;单轴「轴X」「轴Y」;带电/力矩语境「电机」 | — |
| 本机这一侧 | 勾选框名「上位机侧」;句子里写「只作用于本程序」 | 「本程序这一侧」 |
| 网格间距 | 「分辨率」 | 「网格间距」 |
| 那张彩图 | 「色标」(控件名;标题里原来的「(功率)」已去掉 —— 单位现在跟着源走,写死一个就是错的) | 「色阶」 |
| 速度 / 位置 / 功率单位 | `pul`、`pul/s`、`s`、`ms`、`mm`、`W`、`J` | 中文单位「脉冲 / 秒 / 毫秒 / 毫米」 |
| 回零那一族 | 「回零」「找原点」「找正限位」「找负限位」(与 `home_method_short()` 逐字一致);分组标题「回零与找限位」 | 「原点模式」 |
| 允许的双名 | 扫描参数「每点采样」/ 功率计「每次平均」—— 各自语境下更清楚;**只在一处**说清二者是同一件事 | 两处都解释一遍 |

规范化之后,自检里钉着的那些词(过压 / FF02 / 轴X / bit11 / 负限位 / 释放点 / 高速 / 低速 / 拒绝 /
手册 / 分辨率 / 点数 / 量程 / 使能 / 功率计 / 回零 / 603Fh / 还没读到 / `6041h bit3`)**一个都没动**
—— 它们是这套术语的下界,自检就是它的守卫。

### 26.3 第二轮:把成因搬出界面(2026-09-23)

26.2 管的是**怎么写**,这一轮管的是**写什么**。00 年那一轮的遗留:文案规范化了,但**内容**还是
"把我知道的都告诉你" —— 一句提示里塞下三四种成因、每种成因一条排查步骤,最后补一句"先按中止"。
操作员当下要的是**现在是什么状况**,那堆可能性反而把那句话埋了。

这一轮定的四条:

| 规则 | 落地 |
|---|---|
| **提示里不写操作步骤** | 不许出现"先点某处 / 先按某个 / 用什么方法试一下"。步骤属于手册与培训,不属于状态栏 |
| **成因分析移出界面** | 一句提示里出现"多半是 / 可能是因为 / 有两种情况"就是该搬了。搬到 [`scan_messages.md`](scan_messages.md) —— **不是搬到代码注释**:注释只有开发者看得见,而那几张表是现场要查的 |
| **出路只允许「请……」** | 真的只有一条出路时才写,写成一句祈使句:「请重新连接」「请手动把滑台移离该限位」。不写"建议 / 最好 / 可以试试" |
| **界面里不留指向手册的指引** | 包括"见手册 / 详见 xxx"。`mode_text()` 与 `fault_code_text()` 里的「手册之外」是**例外且保留** —— 那句说的是"这个值不在手册那张表里"(照实报十六进制的理由),不是让人去翻手册 |

同轮统一的术语:**限位开关一律「触发 / 未触发」**(替换口语的「压着 / 松开」),含状态栏、
信号灯提示、画布标记与 `ecatcmd::limit_switch_text()`。

范围比 26.2 那一轮**大一圈**,多出来的两类:

- `hmi/ecatworker.cpp` 里那些 `note(...)` —— 它们与 `ecatworker.h` 的横幅函数说的是同一件事
  (界面上的状态行),只是走另一条路出去。**这不包括它往控制台打的 `printf`**:
  控制台不是界面,`consoleNote` 那几段(2300h 读数、同步对象、603Fh 回退耗时)照旧。
- scan 的其余界面文件(`scancontroller` / `mapcanvas` / `metercurve` / `meterlog` /
  `powermeter` / `scanlog` / `scanplan` / `ophirmeter` / `ophircom` / `scanarrive` /
  `editgate.h`)。其中 **写进 CSV 的字符串一律不动**(比如超时那条 `flags` 是数据,不是界面),
  `scan/selftest.cpp` 自己的用例名与断言文字也不在这一轮的范围内。

`scan/selftest.cpp` 的断言在这一轮**局部放宽**了(26.1 里"断言不重写"那句话只适用于 26.2 那一轮):
成因搬走之后,原先逐句钉着成因的断言会成片失败,而它们要守的东西没变。放宽只做两件事:

- **同层的机械替换**:「都没压着」→「都没触发」、「只治软件」→「只作用于本程序」、「走离」→
  「移离」、「永远开不了」→「无法启动」、「关掉」→「关闭」;
- **把"钉成因"改成"钉分工"**:比如"状态句里必须出现 2300h"改成"状态句只报状态、不许夹带出路
  (不许出现「请」)"—— 出路那一支由 `limit_hit_advice()` 的断言继续守着。

**判据本身一条都没删**:必须点名的码(`0xFF02` / `6041h bit3` / `轴X`)、必须出现的出路
(「故障复位」/「上位机侧取反」)、不许出现的猜测(「撞」/「驱动器认为」/「硬顶」)全部还在。

---

## 27. 回零超时:30 秒的常量变成一个可改的数(2026-09-22)

### 27.1 为什么动它

`hmi/ecatworker.h` 原来写死 `HMI_HOME_TMO_MS = 30000`,全仓库只有一个消费点
(`em_home(ax, &cfg, HMI_HOME_TMO_MS)`)。它只管 `em_home()` 里等 `6041h` bit12 的那个循环,
周围的参数写 / 切模式 / 使能各自另有 `EM_STEP_TMO_MS`(1 s)与 SDO `EC_TIMEOUTRXM`(700 ms)。

那个宏自己的注释写着「速度 × 这个时间 = 最远距离(缺省 50000 pul/s ⇒ 30 圈)」——
**30 秒是按缺省速度推出来的**。而回零速度下限是 100 pul/s,那个速度下 30 秒只走 **0.06 圈**。
速度可调、超时不可调,两者本来就不对称。

### 27.2 改成什么

| | 值 |
|---|---|
| 缺省 | **120 秒**(`HMI_HOME_TMO_DEF_S`) |
| 上下限 | `HMI_HOME_TMO_MIN_S` = 5 / `HMI_HOME_TMO_MAX_S` = 600 |
| ini 键 | `ui/home_tmo_s`(秒) |
| 没记过 | `-1` → 缺省;越界的**夹回**而不是拒绝 |

**单位一律秒** —— ini 键、界面、`Cmd` 字段、纯判据函数全叫 `*_s`,只在 `doHome()` 里乘一次
1000(那是全程序**唯一**一次 `* 1000`)。每一个换算点都是一次写错数的机会,单位进名字才不会传错。

`.arg(HMI_HOME_TMO_MS / 1000)` 那一处换成 `.arg(tmo_s2)` 之后,回零收尾那句话里的「上限 %4 s」
**不用改字**(它本来就是对的)。

**现存的 `bin/scan.ini` 不需要迁移**:缺这一项就是 `-1`,落到 120 s,正是要的那个缺省。
自检里那条"老 ini"用例钉着这件事。

### 27.3 那个数还是距离上限,这一点没变

**速度 × 超时 = 一次回零最多能找多远。** 缺省 120 s × 50000 pul/s = **6000000 脉冲 ≈ 120 mm**,
是缺省扫描区域(27 mm)的 **4.4 倍**。

**所以:够不着开关时该做的是先把滑台挪近,不是把超时(或速度)调大。**
真正让滑台停下来的**始终是硬件限位开关**(`2311h`/`2312h` 实测 = 2/3),
这个数只管「找不到还不肯停」。撞超时的收场是**举着 bit4 返回、随后失能、从所在处滑停**。

> **本文件任何地方都不许出现"这台机器能安全走多远"的说法** —— 行程从来没有被实测过
> (见上文 ±13.5 圈那段与 §28.4)。120 s 这个数**只**说明上位机愿意等多久。

### 27.4 顺带补上的一个窄缝

`disconnectAndStop()` 里「回零进行中先掐掉它」那一道,判的是 `telemetry().homing` ——
而 `CMD_HOME` 躺在队列里时 `homing` 还是 false,于是标志没置上,线程随后要阻塞**整个回零超时**
才轮到 `postDisconnect`。超时 30 s 时是卡 30 秒;调到 600 s 就是卡 10 分钟外加一个
"工作线程 15 s 没退出来"的模态框。

补法:在那 12 秒轮询里,一旦 `homing` 变真就补一次 `requestMotionStop()`
(它落在 `doHome` 直接写 `m_telem.homing = true` **之后**,也就是 `em_clear_stop()` 之后,
不会被清掉)。这样这条路的时长就与超时值无关了。

### 27.5 验证到什么程度

**验了**(不需要设备):

- `scan` / `scan_selftest` / `hmi` 三个目标全量重建,**0 warning 0 error**;
- **`hmi.exe` 行为不变** —— 它不调 `postHome`,这次改动对它只有"多了两个成员"这一种影响;
- `scan_selftest` **791 passed / 0 failed / 0 skipped**(上一轮 752)。新增的与本节有关的:
  1. `home_tmo_s_clamp` 的**边界关系**(MIN-1 / 0 / MAX+1 / INT32_MAX),不钉具体某个数;
  2. `clamp(MIN) == MIN` 与 `clamp(MAX) == MAX` —— 钉的是"输入框的 `setRange` 与工作线程的
     夹取读同一对宏",哪天只放宽一边就会响;
  3. `HMI_HOME_TMO_DEF_S == 120` —— 纯产品决定的钉子,唯一作用是"缺省被悄悄改掉"时当场响;
  4. `home_tmo_s_from_pref(-1 / 0 / 300 / 1 / 999999999)`;
  5. `ui/home_tmo_s` 往返(存 300 读回 300),以及**老 ini 缺项 → -1**;
  6. `MAX_S * 1000` 装得进 `int32`(`doHome()` 里那一次换算不许溢出)。

**没验**:超时真的生效这件事只有硬件能验。实跑清单:

- 把超时改成 300 再「保存」→ 看 `bin/scan.ini` 里是 `ui/home_tmo_s=300`;
- 回零速度压到 100、点回零 → **不再** 30 秒判超时;
- 回零中点「停止」→ 仍 ≤2ms 中止(这条路走的是 GUI 直呼,与超时值无关);
- 回零中点「断开」→ 应立刻开始收尾,而不是等到超时。

---

## 28. 面板全域可点:软量程的下限是画布半宽(2026-09-22)

### 28.1 症状与成因

**症状**:在画布上 Shift+左键,面板上大部分地方点不到;点到的地方也不是点的那一点。

**成因**:`scan/` 在参数一变就把软量程设成 `autoRangePul()`,原来是
`ceil((max(区域宽,高) / 2 + 1.0) * 脉冲当量)` —— 默认 27 mm 区域 → ±725000 pul = ±14.5。
而一个 **3 mm** 的区域只到 **±2.5 mm**,画布却仍然画到 ±16。
于是 Shift+左键被夹了**三道**:画布一次、`EcatThread::setTarget()` 一次、`interpolate()` 一次。
界面上完全看不出「我要的是 16,实际只走了 2.5」—— 除了橙色三角落在哪。

### 28.2 改法

**只有一份常量**:`scan::kCanvasHalfUnits = 16.0`(`scan/scanplan.h`)。
原来它是 `mapcanvas.cpp` 里私有的 `kViewHalfUnits`,删掉了 —— 两个数"必须一致"正是
`scanplan.cpp` 开头 `VEL_MIN`/`VEL_MAX` 那段注释已经在防的坑。`mapcanvas.cpp` 加一句显式
include,`viewHalfUnits()` 改读它。

```
量程脉冲 = ceil(max(区域半宽 + 1.0, kCanvasHalfUnits) * 脉冲当量)
```

**16 而不是 15**:标尺画到 ±15,多出的 1 单位是边距,而画布本来就**接受** `|x| ≤ 16`
的点击(超出才丢)。取 16 才真的没有够不到的一圈。

**点击处那道夹改成纯面板边界**(`lim = half`),不再按量程夹。留着它是因为边界该写在点击发生
的地方;而按量程夹有两个毛病:软量程的下限现在就是 `half`(所以量程这一侧本来就 ≥ half),
而且量程是**异步跟上**的(参数一变就重投,要等工作线程转一圈),在这儿再夹一遍会让面板边缘
"有时候点得到有时候点不到",界面上完全看不出原因。

真正的夹取仍在 `EcatThread::setTarget` → `interpolate`(都在 `±m_range`),画出来的橙色三角
就是夹过之后的那个值 —— 万一真有够不到的地方,看三角落在哪就知道。

### 28.3 连带面(逐个核过)

| 地方 | 变了什么 |
|---|---|
| `autoRangePul()` 的缺省结果 | 725000 → **800000** |
| `scan/selftest.cpp` 那条 `checkEq(autoRangePul(p), 725000)` | 会挂 → 改成 800000 |
| `fitsRange()` | 在界面这条路上从此**恒真**(`currentParams()` 总把 `range_pul` 设成 `autoRangePul()`),但它**留着** —— 它仍是"有人显式塞了一个 `range_pul`"时的唯一那道闸,自检里那两处正是现在唯一走得到假分支的路径 |
| `armRun()` 的 `far > t.range` 预检 | 只是变宽松,不改 —— 它防的是**陈旧的遥测量程**,仍然有用 |
| 画布那个红框「区域超出量程 —— 边缘会被静默夹掉」 | 现在要区域 > 32 mm 才亮,保留 |
| `csvMetaLines()` 的 `range_pul=` | 变成 800000,是实话 |
| 「量程从不记忆」(`scanprefs`) | 仍然对,**并且多一条理由**:它现在是 `max(区域推出来的, 画布推出来的)`,记下来就等于把画布那条下限**冻结在当时的脉冲当量上**,脉冲当量一改它就是个错的数 |
| `hmi/` | **不受影响**。`SlideCanvas` 用编译期 `HMI_RANGE`,从来不读软量程 |

### 28.4 代价(原话说清)

**软量程从此永远 ≥ ±16 mm**,而机器上到底有多少行程**没人量过** —— 这正是
「先跑一行」那段(§9 之前的实测提醒)在讲的事。安全边界从此**靠硬件限位开关**
(`2311h`/`2312h` = 2/3)。

这次只是**拆掉一句关于"点得到"的谎话,没有增加任何新的物理底气**:软限位仍然是软件自己定的,
被删掉的只是"面板上有一圈点不到、而且不说"这件事。

### 28.5 面板外的一个点

保留零点之后 `AxisTelem::pos` 可以落在画布 ±16 之外(区域 > 32 mm 时软量程就超过视野),
而 `mapcanvas.cpp` 全文没有 `setClipRect`,实心白点会画到标尺上。两件事一起做:

- 三个来自**驱动器**的标记(目标 / 点击目标 / 实测位置)在 `drawMarkers` 里夹到 `plotRect()`;
- `drawHud` 在 `|pos| > 视野` 时加一行「位置在面板外 (x, y) mm —— 白色标记被贴在边框上」。

**留着不画比画到标尺上更糟** —— 操作员会问"我的点哪去了"。

### 28.6 验证到什么程度

**验了**(不需要设备):

- `scan` / `scan_selftest` 全量重建,**0 warning 0 error**;
- `scan_selftest` **791 passed / 0 failed / 0 skipped**。与本节有关的:
  1. **一条全域不变式**,形状照 `home_vel_slow` 那个全域循环:对 6 组区域 × 5 组脉冲当量,
     `autoRangePul(p) >= ceil(kCanvasHalfUnits * 脉冲当量)` —— 它把"面板上看得见的地方必须
     点得到"这条耦合变成机械的,两处常量哪天漂了就会响;
  2. 「取两者大的那个」两个方向各一条:3 mm 区域 → 800000(画布说了算),
     40 mm 区域 → 1050000(区域说了算);
  3. `fitsRange(默认参数)` 的**真**分支(原先只有假分支)。

**没验**:点下去之后橙色三角是否真的落在那个像素上,只有实跑能看。实跑清单:

- 存一个 3 mm 区域,Shift+左键点画布四角 → 三角落在点的那一点;
- 把区域改成 40 mm → 面板外那一行字应出现,白点贴在最靠外那条边上。

---

## 29. 零点跨重连保留 —— 只对 `scan`(2026-09-22)

### 29.1 今天为什么丢

`tryInitOrigin()` 每次连接都无条件 `m_origin[i] = em_pos(m_ax[i])`,于是显示坐标
`pos - origin` 归零。而 `teardown()` 只把 `m_origin_ready = false`,**不清 `m_origin[]`**;
`EcatThread` 每个程序只 `new` 一次,断开→连接用的是**同一个对象**。

所以"位置丢了"不是对象没了,就是那一行覆盖掉的 —— **保留它在内存里是免费的**。

### 29.2 那条不变量,以及它为什么危险

- `interpolate()` 每周期把 `m_tgt[i]` 夹进 `±range`,然后写
  `em_csp_set_target(ax, m_origin[i] + m_tgt[i])`;
- `doEnable()` 的重钉写的是 `m_tgt[i] = em_pos(m_ax[i]) - m_origin[i]`,**不夹**。

保留零点之后这个差可以是任意大:滑台停在旧零点外 1200000 pul、量程 800000,
那一夹把它改成 800000,于是驱动器从 `origin + 1200000` 被命令走到 `origin + 800000` ——
**400000 脉冲,全速,没人按过按钮**。

而"重连之后滑台不在旧零点附近"**恰恰是这次要支持的正常用法**。

### 29.3 所以沿用要有一个前置条件

`ecatcmd::origin_keep_ok(pos, origin, range)`(`hmi/ecatworker.h`,inline 以便自检):
**只有每根轴都仍在当前量程内才许沿用**,量程未知(`<= 0`)一律不沿用 —— 不知道就别赌。
差值是 `int64` 算的,`int32` 相减会回绕(滑台在最远那一头反而会被判成"可以沿用",正好反过来)。

`doEnable()` **一个字节都不许动**。它是保留零点与本改动之间唯一的东西,而它"不夹取"这件事
**正是** `origin_keep_ok` 存在的原因。

### 29.4 三个分支的三种说法

`tryInitOrigin()` 现在分两支,重取那一支有**两个成因**,文案分开说:

| 分支 | 状态栏说 | 世代 |
|---|---|---|
| **沿用** | 「沿用上次的零点(连接不再重设,界面正中仍是上次那个物理位置)。滑台现在显示在 (…) pul。」 | **不加** —— 零点没搬,前面那些 CSV 的坐标仍然作数 |
| 重取,以前没定过零点(或这是 `hmi`) | 「零点是连接时读到的位置: 界面正中 = 现在这里…」 | +1 |
| 重取,有旧零点但不敢沿用 | 「重新取零点: 滑台已跑出上次零点所在的量程, 不敢沿用 / 轴数变了, 上一份零点作废」 | +1 |

第三行是**关于机器状态的一句警告** —— 屏幕上的 0 换了一个物理位置,而操作员按老习惯会以为
它还是刚才那个。

顺手去掉了原来那句里的 `50000 pul/rev` —— `scan` 的脉冲当量是可改的(100…1000000),
写死一个就是一句**今天就可能已经是假的**的话。

### 29.5 世代搬到工作线程

今天 `setConnected(true)` 每次连接都 +1,理由是「连接会把零点重设为当时所在的位置」——
而那件事正是这次要去掉的。计数改由**工作线程**维护,因为只有它知道零点真被搬过:

- `BusTelem` 加 `origin_gen`;`EcatThread::m_origin_gen` 在 `m_origin[]` 被真正写入的三处各 +1
  (doHome 第 4 步 —— **无条件**跑,成败都搬;`doZero`;**`tryInitOrigin` 只在重取那一支**);
- `publish()` 每圈拷进电文;
- 界面在 `refresh()` 里用 `ecatcmd::origin_epoch_sync()` 同步,**只许往前** ——
  倒回去等于给下一次续扫发一张**假的**"世代对不上"红横幅。

**`m_origin_gen` 必须是成员而不是只住在遥测里**:`teardown()` 结尾 `m_telem = BusTelem()`
会把整份遥测清成默认值,计数跟着归零;而界面那侧只许往前的同步就会**漏掉**下一次真正的零点
变更 —— 正是危险的那个方向。`publish()` 在没进 OP 的路上也会跑,所以断开之后它仍然新鲜。

**为什么不记进 ini**:用户明确定的是"只在本次程序运行内"。跨进程保留要把零点与当时的量程、
脉冲当量、轴数一起存下来并在连接时全部校验一遍 —— 那是一条新的、没人会去看的落盘路径,
而"重新取一个零点"只要按一下「设为区域中心」。

### 29.6 为什么不需要别的同步

起扫的前提是**两轴已使能**,而使能拒绝在零点就绪之前;起扫又要求两轴已使能。
所以「第一行 CSV 写在零点定下来之前」**不可能发生** —— 一处 CSV 的 `zero_epoch` 永远写着
它那些坐标所属的那一代。

回零与「设为区域中心」在运行中都被拒,所以跑起来的那一趟零点不会动。

### 29.7 这个开关为什么存在(对仓库默认做法的一次有意偏离)

`hmi/ecatworker.cpp` 是两个程序共享的那一份源码,「只有一份实现」的默认做法是两边一起改。
这里开了一个 `setKeepOrigin(bool)` 分叉,**默认 `false` 就是本类自己的老行为**(连接那一刻
即零点),`scan/` 在构造之后调一次 `true`。

理由是:「连接那一刻的位置就是零点」是 **hmi 的操作模型**,不是无意的副作用 ——
它那一侧的界面文案与操作习惯全是围着这个写的。这是两个程序**产品上的差别**,
不是同一件事的两种实现。

**代价(原话)**:断开期间被人手动挪过的滑台,重连之后会被**静默映射进旧坐标系** ——
它现在显示的那个数,不是它刚才停的那个数。这正是 `origin_keep_ok` 要挡住的那件事的一半
(另一半是量程外的剧烈运动);量程之内的静默位移是这条功能**故意接受**的代价。

### 29.8 会变成假话的字,改了哪些

| 位置 | 改成 |
|---|---|
| 连接按钮 tooltip | 「零点沿用本次运行里已定的那个, 断开重连不重设」 |
| 「设为区域中心」tooltip 与它按下后的提示 | 只说到"定为显示坐标 0",**不再报世代** —— 世代现在异步跟上,说一个当场就过期的数不如不说(§26 已把它从界面上拿掉) |
| 续扫拒绝语 | 「回零与『设为区域中心』会把零点搬走(**连接不会**)」 |
| `docs/scan_sweep.md` 的零点世代那一段 | 加划改:对 `hmi` 仍逐字成立,对 `scan` 不是了 |
| `docs/hmi_click_position.md` | **一字不动** —— 它讲的是 `hmi`,仍然为真 |

### 29.9 验证到什么程度

**验了**(不需要设备):

- 三个目标全量重建,**0 warning 0 error**;
- `scan_selftest` **791 passed / 0 failed / 0 skipped**。与本节有关的**只有两个纯判据**:
  1. `origin_keep_ok` —— 界内真、`|d| == range` 真(边界**含**)、`range + 1` 假、负侧对称、
     零点不在原处时看的是差值、`range <= 0` 假、以及
     `pos = INT32_MAX / origin = INT32_MIN` 必须**假**(不许回绕);
  2. `origin_epoch_sync` —— `(0,1)→1`、`(5,5)→5`、`(5,7)→7`、`(5,0)→5`(「只许往前」那一条)、
     `(5,4)→5`;外加 `BusTelem{}.origin_gen == 0`。

**没验,而且这一节**:**保留/重取那个决定、世代计数、`doEnable` 的重钉,三样都测不到** ——
`scan_selftest` 不编 `ecatworker.cpp`(`scan/CMakeLists.txt`),`FakeBus` 里也没有 origin 的概念。
**这三样只有硬件能验。** 实跑清单(按重要性排):

1. 使能 → 手动走到离零点较远处 → 断开 → 连接 → **位置接着断开前那个数,而且连接那一刻一步
   不走**(本改动唯一真正危险的地方,盯着看);提示语说「沿用」;
2. **故意把滑台挪到离零点超过量程的地方再重连** → 必须**不沿用**并重新取零点(那条警告文案出现);
3. 接着按「开始扫描」→ **不该**弹"零点世代对不上"(没漏发一代);再点一次「回零」后按开始扫描
   → **该弹**(零点真动了);
4. 断开期间手动挪一段(量程之内)再重连 → 会静默映射进旧坐标系 —— 这是**接受的代价**,
   但要亲眼看一次,确认自己接受的是这个。

### 29.10 这一轮动到的文件

`hmi/ecatworker.h` / `hmi/ecatworker.cpp`(分叉开关、世代、`tryInitOrigin` 的三条分支、
`teardown` 的"不清"注释、`doEnable` 的注释)、`scan/scanwindow.{h,cpp}`(`setKeepOrigin(true)`、
删掉三处手工 +1、`refresh()` 里的同步、两处 tooltip、`disconnectAndStop` 的补丁)、
`scan/scancontroller.{h,cpp}`(世代注释与续扫拒绝语)、`scan/selftest.cpp`(两条纯判据),
以及 `docs/scan_sweep.md` 这一节与 §3/§15/§18/§21 那几处划改、`README.md` 的对应几段。

---

## 30. 驱动器通讯报警:上位机看得见,并且把"是谁的锅"变成可测的数(2026-09-22)

现场原话:

> 隔一阵子就会驱动器通讯报警 且上位机这里故障信号灯没有更新

追问拿到的三条事实决定了整个设计:发生在**挂着没动**的时候(连上/使能之后空闲,没扫描也没
回零);上位机这边**故障灯根本不亮**而别的灯正常更新;驱动器**要断电重启或按故障复位**
才能继续(闩锁)。驱动器手册对上了:`603Fh` = `FF06` 是通讯报警,ERR 灯双闪 = 过程数据看门狗
超时 —— 也就是驱动器认为**主站有一段时间没给它喂过程数据**。

### 30.1 上位机为什么看不见(四扇窗,全是关着的)

1. **`603Fh` 只在 bit3 立起来之后才读一次**,bit3 = 0 时反而把码**清成 UNREAD**。
   ⇒ 一个不置 bit3 的驱动器报警,在这个程序里**永远不存在**。
2. **连接之后从不看 AL 状态 / 从属关系** —— `em_slave_info()` 明明给出 `state` 与
   `alstatuscode`,全仓库 `hmi/` 与 `scan/` **零调用**,`BusTelem` 里连字段都没有。
   ⇒ 「从站掉出 OP」「AL `0x001B`」没有任何显示位。
3. **`mirror_ok` 只置不清** —— 短帧那一支只 `short_frames++`,不清 0。
   ⇒ 所有 `!mirror_ok` 判据**全是死代码**(`ScanController::healthProblem()` 那句
   「轴%1 丢了过程数据帧」、界面的 `known`、`home_refusal` 那几条闸)。
   丢帧时位置冻住,界面照样说"正常"。
4. **没有任何东西量"上位机自己最长多久没发帧"**。所以"隔一阵子"**无法归因**。

### 30.2 第六扇窗是这次才发现的:那条 SDO 就是报警的一半成因

第 4 条反过来咬了自己:`serviceFaultCodeReads()` 是全程序**唯一一条在 OP 里做的 SDO**,
而 SOEM 的 SDO 事务期间**过程数据一帧都不发**(硬事实与两处源码证据写在
`docs/ykd2205pe_ci402.md`「硬事实」那一节)。所以它不是"让圈期变长",是**把圈停掉**,
超时多长就停多久(默认 `EC_TIMEOUTRXM` = 700 ms)。而"驱动器不应答 SDO"正是它刚报警时的
常态 —— 于是**上位机在驱动器最脆弱的一刻把总线静默了最多 700 ms,自己把看门狗又喂掉一次**。

**这一条是实机量出来的,不是推出来的**:改动上线后第一次实跑,界面自己弹出

> 过程数据帧连续10帧不足(工作计数器 -1/6)……本程序最长 **1638 ms** 没发出一帧
> (超过 50 ms 的有 1 次)

1638 ms ≈ **两根轴各 700 ms** —— 就是那条回退 SDO 在两根轴上先后超时。超过阈值的次数
只有 **1**,说明是**一次**停顿而不是持续漂移。(顺带排除了"接收超时":`EC_TIMEOUTRET` 只有
2 ms,几百毫秒的静默只可能是我们自己的循环体在阻塞。)

**曾经以为"调用点套 `BlockTick` 就行",那是错的**:`BlockTick` 的 `tick()` 只在 `em__cycle`
收完一帧时被调,而 SDO 事务期间**根本没有帧**,那个钩子一次都不会响 —— 套了等于没套,
却让人以为这块有保护。这一轮把它撤掉了,换成下面三条真的管事的。

### 30.3 六个改动

| # | 改动 | 落在哪 |
|---|---|---|
| 1 | **`603Fh` 追加进 `1A00h` (TxPDO0)** —— 报警码与 `6041h` bit3 **同帧**到达,一个字节的 SDO 都不发。照 `60FDh` 那套:仅 RAM、写前快照、收尾还原;**默认就是开** (它是只读监视量,再加一个默认关的开关等于把同一件事再关上一次) | `motor_api/ec_motor.{c,h}` + `ec_motor_internal.h` |
| 2 | **`mirror_ok` 降级**:连续 `EM_SHORT_FRAMES_LIMIT` (20) 帧不完整就置 0 ⇒ 上面那些死代码全活过来 | 同上 |
| 3 | **`em_al_status()`** 暴露 AL 状态/码(内部 `ecx_readstate()`,BRD 广播读、不走邮箱) | 同上 |
| 4 | **`em_set_sdo_timeout()`** —— 进 OP 那一刻把所有 SDO 超时从 700 ms 压到 **60 ms**;故障码那条回退 SDO 再加两道闸:**映射里有码就不发**、**帧不健康就不发** | `motor_api` + `hmi/ecatworker.cpp` |
| 5 | 帧间隔统计:`em_service()` 两侧各取一次表,记**最长多久没发帧**与超阈值次数(不加任何计时器开销) | `hmi/ecatworker.cpp` + `BusTelem` |
| 6 | 界面:参数栏「轴信号」加**「通讯」那盏灯**(问的是**主站自己**的帧够不够,不是驱动器报的)、一条**不自动消失**的红横幅、状态栏的 AL 码 / 「最长 N ms 没发帧」/ 连续不足帧数、`hmi` 侧 `m_lBus` 的丢帧计数 | `scan/scanwindow.{h,cpp}` + `hmi/axispanel.cpp` |

**改动 2 必须一起改的一处安全路径**(不动它这一轮就是**安全倒退**):`em_shutdown()` 有两处
拿 `mirror_ok` 当**前提** —— 写 `0x0000` 卸力那一步"链路一断连试都不试",确认循环
"链路一断就 `continue`"于是 `unconfirmed` 永远是 0、README 里那条"立即断动力电源"的模态
告警**再也不会弹**。改成用一个**只增不减的闩锁** `ever_enabled` 判:写过使能的轴,**链路断了
也要走确认循环,而且判成「未能确认」** —— 要报得更响,不是更轻。

**文案分两族,不许混**:「驱动器自报」(`603Fh` / `0xFF06` / 通讯报警 → 查通讯线、干扰源与
站号配置)与「上位机的帧不够」(WKC / 过程数据帧 / 掉出 OP → 查网线/网卡/本机负载)。
两件事处置不同,说成一句话会把人支到错的地方。

### 30.4 实机已经验到的两件(用户贴回来的界面输出)

1. **`603Fh` 真的补进 TxPDO 了**:界面逐轴打出 `轴X 603Fh = 0x0000 (无错误)` /
   `轴Y 603Fh = 0x0000 (无错误)` —— 走到"映射里读到 0000"这一支,说明 `em_err_code_offset >= 0`
   在真机上成立(`0x0000` 是**真实读数**,与"没读到"分得开,这正是要的效果)。
2. **故障横幅 + 逐轴故障码真的会出来了**(用户原报的"故障灯不亮"这一半已经修掉),
   而且两个轴同时是 `0x0000` 时界面给的那句处置是「码是好的, 只是故障位还没清 ——
   点「故障复位」」—— 与 CiA402 的语义一致(bit3 是**闩锁**的,码可以已经清掉)。

   > 这一次实跑还暴露了一个**文案缺陷**:两根轴同一个码时,那句处置**连着出现了两遍**
   > (处置是按码给的、不按轴给,而循环是逐轴的)。已改成同一个码只说一遍,轴号照旧逐轴点名,
   > 并补了自检(`s.count(one) == 1`)。

### 30.5 验证到什么程度

**验了**(不需要设备):

- 三个目标全量重建(逐个 `touch` 过,强制重编),**0 warning 0 error**;
- `scan_selftest` **843 passed / 0 failed / 0 skipped**(上一轮 791)。新增 `test_bushealth()`
  与"两根轴同一个码"那一组;与本节有关的**全是纯判据**(`scan_selftest` 既不编
  `ecatworker.cpp` 也不编 `motor_api` 的 `.c`,所以判据必须 header-only `inline` 写进
  `namespace ecatcmd` 才验得到):
  1. `comm_bad_from` —— `limit-1` / `limit`(边界**含**)/ `limit+1`,以及 `expected <= 0`
     与 `limit <= 0` 两个非法值必须为假(否则每次连接都被记成一串坏帧);
  2. `err_code_from` —— 四条来源优先级,含 **`0x0000` 是真实读数**、以及 bit3 = 0 时
     **必须作废 SDO 那条陈值**(否则"已经清掉的故障还在报");
  3. `al_code_text` —— `0x001B` 点到「看门狗」与「喂帧」、相邻码不许抄串、表外码照实写
     十六进制(不许猜成"未知故障")、`state <= 0` 是"没读到"而不是 INIT;
  4. `axis_alarm` —— **只有码、bit3 没立起来的那一种也算报警**(现场报的正是这种),
     而两个哨兵 UNREAD/FAIL 不算;
  5. 两族横幅措辞不许混,含「(B) 不许抄 (A) 那句『查站号配置』」;
  6. 同一个码的处置只说一遍,且**不同码时两句都要留下**。

**没验,而且这一轮的主要部分都在这儿** —— `em__cycle` 的短帧降级、TxPDO 追加、`em_al_status`
的取值、帧间隔统计、`em_shutdown` 那处安全修改:**`scan_selftest` 没有 `FakeBus` 能造出
"短帧"这个局面**。实跑清单(按重要性排):

1. 连上 → 控制台应打出 `1C32h:01` / `:02` / `2217h` 三个数,以及 `603Fh` 是否补进映射;
2. **空闲挂着不动,盯状态栏那句「最长 N ms 没发帧」**:几 ms ⇒ 与这台上位机无关(查线缆/
   干扰/驱动器设置);几百 ms ⇒ 是本机的网卡或**电源管理**在拖延;
3. 复现报警那一刻 → 「通讯」灯亮 + 不自动消失的红横幅 + 状态栏 AL 码;
4. **拔网线** → 「轴%1 丢了过程数据帧」这一族**真的会触发**(以前是死代码);
5. **收尾那一路必验**:连着的时候拔网线再点「断开」→ 必须报「未能确认所有轴失能」,
   而不是静默跳过(改动 2 那处安全修改,**它是这一轮唯一可能让机器更危险的地方**)。

### 30.6 不写代码的那一条:PC 侧的电源管理

"挂着没动"最容易对上的是 **Windows 电源管理**:网卡"允许计算机关闭此设备以节约电源"、
PCIe 链接状态电源管理、处理器最低状态 —— 空闲时 NIC/CPU 降频 → 发帧被推迟 → 看门狗。
第 2 条实跑量到的 `max_gap_ms` 就是判它的那把尺。**这一轮不改任何配置**,只把尺子装上。

### 30.7 风险

- **改动 2 那处安全前提没有实跑过**(上面第 5 条),它是这一轮唯一可能让机器更危险的地方;
- **`mirror_ok` 降级是行为改变**:运动中途丢帧会让 `em_home` 一类函数开始拒绝继续;
- **改动 1 是改通信配置**(虽然只在 RAM、收尾还原)。本机已经为 `60FDh` 这么干过,所以有
  先例;SM3 装不下时会退回 SDO 路,而那条路本身有上面的代价 —— 所以超时那一条**无论走哪条路
  都必须留着**;
- **`2217h` 到底管什么,只有手册一行字**,读出来的 `1C32h:01` 才作数 —— 这一轮**不猜、不改,只报**。

### 30.8 这一轮动到的文件

`motor_api/ec_motor.{c,h}`、`motor_api/ec_motor_internal.h`(603Fh 字段与绑定、短帧降级、
`em_al_status`、`em_set_sdo_timeout` + `sdo_tmo_us`、`em_shutdown` 的 `ever_enabled`)、
`hmi/ecatworker.{h,cpp}`(遥测字段与纯判据、故障码两条路的挑选、帧间隔统计、AL 码只在异常时
读一次、进 OP 压超时、故障复位那一支)、`scan/scanwindow.{h,cpp}`(第六列「通讯」灯、不自动
消失的横幅、状态栏三项)、`hmi/axispanel.cpp`(丢帧计数)、`scan/selftest.cpp`(`test_bushealth`
与去重那一组),以及 `docs/ykd2205pe_ci402.md`(603Fh 进 TxPDO、三个只读对象、SDO 那条硬事实)、
`docs/hmi_click_position.md`(2ms 循环那段补同一句事实)、`README.md`(六列灯与那盏灯的判据、
自检条数)。**不提交。**

---

## 31. 参数框:撤掉「编辑」,装回 `[保存][取消]`(2026-09-23)

这一节记的是同一天的四趟改动, **按钮那一块以 §31.4 为最终形状**: ①三个按钮全撤;
②`[保存][取消]` 装回来; ③这两个按钮从"框里的一行"挪到"框标题那一行的右端";
④界面样式表搬出源码、放进 exe 旁边的 `scan.qss` 并加了个 F5 热重载(§31.7 ——
这一趟**不碰**上面那三趟的结论, 改的是"外观怎么调"这件事本身)。

### 31.1 现场那两句话

第一句:"暂时取消其他菜单的编辑保存取消功能 **他们是用于防止滚轮误触的**"。

先分清一件事:按 §19.4 的记录,那三个按钮当初**不是**为滚轮加的 —— 它们是为了"有一份设置要
固化"(「保存」= 写 `scan.ini`)和"打错了能退回去"(「取消」= 退回上次保存值并重新下推)。
"防误触"是它**顺带**起的作用(非编辑态里控件是禁用的, 而禁用控件收不到滚轮)。所以:

- **防误触**这件事归 §16.2 那条滚轮闸管 —— 判据是"按住 Ctrl 才认", 与门控无关;
- **门控本身**在滚轮那条闸到位之后, 剩下的就只有代价: 想看一个数、改一个数都得先点一下
  「编辑」。于是第一趟全撤。

第二句(当天稍后):"**还是要保存和取消按钮的**"。

这句推翻的是"退路也不要了", 不是"编辑这道门要回来"。于是第二趟按三个决定装回来。

第三句:"**保存和取消按钮要放在每个菜单框标题的右侧 靠右对齐 和标题一行**"。

于是第三趟把那一行从"框里的一行"挪到标题上(摆法见 §31.4 最后一段)。

### 31.2 第一趟:三个按钮全撤

撤掉:

- 四块框首那一行 `[编辑][保存][取消]`(扫描参数 / 回零 / 高级选项 / 色标);
- 编辑态本身:`begin` / `save` / `cancel` / 快照(`gateSnapshot`)/ 回滚(`gateRollback`)/
  「未保存」与「已丢弃」标题标记(`gateTitle`)/ 同一时刻只许一块框编辑的静默丢弃;
- 因回滚才存在的两个函数:`gateRebase`(**程序自己**改了某个控件值时把快照跟上)与
  `gateDirty`(**逐项与快照比对**算"有没有改动")。程序自己写控件那三处
  (色标按数据定标、自动跟随每拍刷读数、CSV 缺省名)随之各删一句 `gateRebase`;
- `saveSettings()` 末尾那个"正在编辑的那一框, 快照跟上、标记清掉"的循环。

留下(**照旧一处写**):

- **运行中锁**: `GateItem.lock_running` 那一列原样保留 —— 区域 X/Y、分辨率、1 mm = N 脉冲、
  方向、方式、CSV 路径 + 「…」、恢复默认。理由不变: 跑到一半改几何, 落进 CSV 的 `(ix,iy)`
  就和滑台实际站的地方对不上了。**这也是撤门控时唯一不能顺手删的东西**;
- **`need_manual`**: 「按数据定标」仍在自动跟随时灰掉(开着时它做的事每帧都在做);
- 空下拉框 (`count() > 0`) 那条判据;
- `refreshEditability()` 仍然是这些控件 `setEnabled` 的**唯一写点**, 仍是 30Hz 的
  `refresh()` 每拍调;
- **`scan/editgate.h` 整份**, 以及 `scan_selftest` 里那 35 条 `test_editgate`。

改名的(撤掉门控之后原来的名字就不对了, 第二趟沿用):

| 原来 | 现在 | 是什么 |
|---|---|---|
| `enum GateIdx { GI_* }` | `enum PanelIdx { PI_* }` | 框号 |
| `PanelGate { Gate; box; btn*; title_base; items; snapshot }` | `PanelItems { gate; box; btnSave; btnCancel; title_base; items; baseline }` | 一块框的项表 |
| `m_gates` | `m_panels` | 见上 |
| `addGate(gi, box, items)` | `addPanel(pi, box, items)` | 登记项表 + 标题原文 + 那两个按钮 |
| `snapshot` | `baseline` | 语义变了: 不再是"进编辑态那一刻", 而是**ini 里那一份**(见 §31.4) |

`GateItem` 这个名字**没改**: 它就是"这一项什么时候不能改", 与门控在不在无关。

### 31.3 第二趟:三个决定

问用户问出来的三条(不是自己定的 —— 这属于界面取舍):

1. **按钮组合** = 只回 `[保存][取消]`, **不装「编辑」**。控件平时就能改(只有 `lock_running`
   那几项在扫描中锁住), 「保存」把这一框固化进 ini, 「取消」退回上次保存的那一份。
   防滚轮误触**不靠按钮**, 靠 §16.2 那条"按住 Ctrl 才认滚轮"。
2. **「未保存」标记** = 照旧留着。框标题在框里任何一项与基线不同时挂 `   ● 未保存`;
   **没改动时两个按钮都灰**(交付时只做到「取消」灰, 用户当场纠正 —— 见 §31.4 的"灰的规矩")。
3. **「保存」的范围** = **只写这一框**。没有编辑态之后, 几块框可以同时是"未保存"的 ——
   若按一个框的「保存」就把整份 ini 重写一遍(老 `saveSettings()` 做的事), 别块框
   **还没保存**的改动会被顺手固化, 而它们的「取消」承诺的是"退回 ini 里那一份", 那一刻
   ini 已经变成屏幕值了, 那个按钮就成了空话。所以按框拆开写。

### 31.4 最终的形状(以这一节为准)

**没有编辑态, 但状态机照旧用着。** `addPanel()` 里进门就 `editgate::begin(&p.gate)` 一次,
之后 `gate.editing` 恒真 —— 于是:

- `editgate::titleMark()` 仍是「未保存」这四个字的**唯一出处**(自检钉着它),
  `editing && dirty` 那一支恒成立, 就等价于"`dirty` 时出标记";
- `discarded` 那一支永远走不到, 所以界面上**不会**出现 `● 未保存 (已丢弃)`;
- `dirty` 由 `editgate::markDirty/undirty` 每拍按 `panelDiffers()` 重设, 不是靠信号点灯
  —— 程序自己也会改控件值(定标 / 自动跟随 / CSV 缺省名), 布尔标记分不出是谁改的, 而且改回
  原值不会自己消失。比对是幂等的, 所以**改回原值标记就自己落下去**。

**基线 = `ini` 里那一份。** 三处采:

| 何时 | 为什么 |
|---|---|
| `buildUi()` 末尾 (`for pi < PI_N`) | 六块框的控件到这里才全建齐(`loadSettings()` 在 `buildParamPanel()` 里已经跑完); 早一步采就是拿缺省值当基线, 一开门每块框都顶着「未保存」 |
| `onPanelSave(pi)` 之后 | 刚落盘的那一份就是新的基准 |
| `saveSettings()` 末尾 | 「连接」/关窗是**整份**落盘, 盘上那份跟屏幕一模一样了 —— 「取消」此刻兑现成"什么都不用退", 标记也该落下去 |

**「保存」按框写** —— `panelSavePrefs(pi)` 一个 `switch`:

| `pi` | 写进 `Prefs` 的字段 |
|---|---|
| `PI_PARAM` | `prefsMergeParams(&pf, currentParams())` + `manual_speed` |
| `PI_HOME` | `home_vel`、`home_tmo_s` |
| `PI_ADV` | `want_dig_in`、`npn_write_drive`、`npn_sw_invert` |
| `PI_SHADE` | `shade_auto` |
| 别的 | `default: return;`(不落盘) |

写完 `prefsSave(prefsPath(), pf)`, 然后 `panelCapture(pi)` 把基线挪到当前值。
**「功率计」那一框没有这两个按钮** —— 它不在 `m_panels` 里(§21 起它就不在门控里),
它的两格(`meter_interval_ms`、`meter_csv`)只在「连接」/关窗那两次整份落盘。

**「取消」= `panelRevert(pi)`**: 逐项 `gateSetValue(w, baseline[i])`。
控件 ← 基线**刻意不挂 `QSignalBlocker`** —— "改动当场生效"就是靠那些 `valueChanged` /
`currentIndexChanged` 重新下推给控制器与画布的, 拦了就成了"界面回到旧值、控制器还拿着新值"。
(`QLineEdit` 的 CSV 路径不影响: 它没有信号连接, 是扫描开始时才读的, 见 `m_edCsv`。)

**灰的规矩**(都在 `refreshEditability()` 里每拍重算, 别处不写 `setEnabled`):

- **两个都看 `dirty`**: 这一框没改动时**都灰**。那时候「保存」写下去的就是盘上已经有的那一份,
  「取消」要退的就是此刻屏幕上这一份 —— 两个都是空动作, 亮着只会让人以为还有事没做完。
  (一开始写成"「保存」不灰", 用户当场纠正。)
- 「取消」另有一条 `!any_locked`。`any_locked` = 运行中且这一框有 `lock_running` 项 ——
  那种时候按下去会把几何退回基线, 正好把上面那条运行中锁撬开, 所以按不动。
  **「保存」不看这一条**: 它写的是"当前值", 没有锁的问题(运行中本来就改不动那几项)。

**`m_panelRevert`**: 回灌途中为真。色标那一对 `[最小][最大]` 是**推着走**的(改一头越过另一头
就把另一头一起推过去), 而回灌是**先写 `lo`, 此时 `hi` 还是操作员敲的那个新值** —— 那一瞬间
两个数看着是反的, `onShadeLoChanged` / `onShadeHiChanged` 里那条"最小值超过最大值"的横幅会
白弹一次。所以那两个槽进门先看 `m_panelRevert` 让开, 两个都退完之后由 `panelRevert()`
补一次 `m_canvas->setShadeRange(lo, hi)` 把画布推到最终那对值。

**两个按钮不进 `items`**: `gateValue()` 对按钮返回无效 `QVariant`, 于是它们永远不会让一框
变「未保存」。

**`[保存][取消]` 那一行摆在框标题的右端, 与标题同一行。** 这是当天第三趟(用户要的):
原先那一行占框里的一行(参数框/色标用 `QFormLayout::insertRow(0, …)`、回零框用网格的
`(0,3,2,2)` 格), 后来改成贴着标题摆。要点:

- **框标题不在任何布局里** —— 它是样式画出来的, 所以那一行**不能靠布局摆**, 只能自己算
  坐标: 那一行是框的一个孩子(**不进框的布局**), 位置由 `placePanelBar()` 定。
  为此回零框的网格空出了 3~4 列(速度与超时改成占满 1~4 列), 另三块框的各一行也撤了。
- **那一行的下沿 = `contentsRect().top()`**。样式按标题字高在框顶上留出这一条, 框里的布局
  从它下面开始 —— 所以不用去问样式要几何, 也不用改框的边距。
  (本程序带了样式表, 量出来是 **18px**, 与标题字齐平。)
- **y 取 0, 高度取那一行自己的 `sizeHint`** —— 不从外面塞一个数字进来。它的自然高度 =
  "字高 + 上下边框", 字体调大时自己跟着长, 框的标题那一行也按字高一起变高, 两边同步,
  不会把字挤掉。
- **宽度取 `sizeHint()`, 不拉满**: 拉满的话左侧那块空白会盖住标题那一带的命中区。
- **框一变宽就得重摆**: `ScanWindow::eventFilter()` 盯着四块框的 `QEvent::Resize`。
  挂在这个事件上而不是 `ScanWindow::resizeEvent` 上, 是因为**参数栏出滚动条**时框宽也会变,
  而那次不会给主窗口发 `resizeEvent`(分隔条拖动、窗口缩放倒是会)。
  `buildUi()` 末尾另调一次 `placePanelBars()` 兜底(那时框才拿到真实宽度)。

**这两条要一起看: 按钮高度和样式表里那条 `padding`。** `scan/main.cpp` 那份样式表的
`QPushButton { padding:5px 12px; }` 是按"框里的大按钮"定的 —— 照它算出来的自然高度是 **28px**,
而标题那一行只有 18px。第一版是硬把按钮压成 18px 高, 结果**字被挤成一条缝**(按钮的框在,
里面只剩一条横线, 用户当场报的)。所以:

- 样式表里给这两个按钮单开一条 `QPushButton#panelbar { padding:0 9px; }`
  (`panelBar()` 里给它们挂上 `panelbar` 这个 objectName) —— 上下内边距压成 0, 自然高度就
  成了"字高 + 边框" = 18px, 正好放进那一行; 左右留 9px, 不然两个字的按钮太挤;
- **不设 `setFixedHeight`**: 高度由那条规则算出来, 想改尺寸只改样式表这一处, 不会出现
  "代码里写死 18、样式表说 28"两边打架。

量过四种情形(把框渲染成图看过): 没改动时两个都灰、改过时两个都亮、最长的标题
(「色标 (功率)」)不撞、**字号调到 12pt 也不挤**(按钮和标题一起变大)。

### 31.5 动到的文件

`scan/scanwindow.{h,cpp}`: `PanelItems` 与 `m_panels`、`gateValue`/`gateSetValue`、
`addPanel`/`panelBar`/`panelCapture`/`panelDiffers`/`panelRevert`/`panelSavePrefs`/
`onPanelSave`/`onPanelCancel`、`refreshEditability()`(每拍算 `dirty` / 挂标记 /
定「取消」的灰)、四块框的 `panelBar(...)` + `addPanel(...)` 调用点、
`buildUi()` 末尾的基线采集、`saveSettings()` 的基线跟随、色标两个槽的 `m_panelRevert` 让位;
第二趟另加 `placePanelBar`/`placePanelBars`、
`ScanWindow::eventFilter()`(框的 `QEvent::Resize`), 以及回零框网格里
速度/超时由「占 1~2 列」改成「占 1~4 列」。

**`scan/main.cpp` 也动了**(这一轮唯一一次): 样式表里加了一条
`QPushButton#panelbar { padding:0 9px; }` —— 理由见 §31.4 最后一段。
(那条规则连同整个样式表当天稍后又搬出了 `main.cpp`, 见 §31.7 —— 现在它在
`scan/scanstyle.cpp` 的 `kDefaultSheet` 里; `main.cpp` 只剩
`app.setStyleSheet(styleLoad());` 这一行。)

文档: 本文 §16.2 / §18.7 / §19.4~19.6 的**现状标注**、`README.md`(参数那一条、
滚轮那一条、回零框那一格)。

**`scan/editgate.h` 与 `scan/selftest.cpp` 一个字没动** —— 那 35 条 `test_editgate` 两趟
跑下来都原样通过, 因为它们钉的是**纯逻辑**(状态机与 `titleMark` 的字), 而界面这一层只是把
`editing` 钉成真、把快照换成 ini 里那一份。

### 31.6 验证到什么程度

**验了**(不需要设备):

- 全量重建 **0 warning 0 error**;
- `scan_selftest` **833 passed / 0 failed / 1 skipped**(跳过的那条是"本机没装 Ophir StarLab
  的 COM 对象"那一个用例)。顺手把 `README.md` 里那个 **843** 改成了实测值: 它是 §30 那一轮
  写的, 之后 §26.3 放宽自检时条数变了, 而 README 没跟。

**没验**: 界面本身。`scan/CMakeLists.txt` 里 `scanwindow.cpp` 不在 `SCAN_COMMON_SRC` 那一组
(§11: 自检只链 `Qt6::Core`, 碰不到控件), 所以这一节改的东西**一行都进不了自检** ——
下面这些只能有人在屏幕前点一遍:

| 该看的 | 期望 |
|---|---|
| 四块框(扫描参数 / 回零 / 高级选项 / 色标)框首框尾 | 只有 `[保存][取消]`, **没有**「编辑」 |
| 同上, 看位置 | 两个按钮在**框标题那一行的右端**, 与标题齐平; 框里少了一整行 |
| 同上, 看**字** | 「保存」「取消」四个字**完整可读**, 不是被压扁的一条缝 |
| 同上, 看有没有压到东西 | 按钮压不到左边标题字; 也没盖住框里第一个控件 |
| 拖动中间的分隔条 / 缩窗口 / 把参数栏滚出滚动条 | 两个按钮**一直贴着框的右边界**, 不会被裁掉也不会停在老位置 |
| 「回零」框 | 速度、超时两个框**占满整行宽**(右边不再空一块) |
| 刚启动, 什么都没碰 | 四块框标题都没有 `● 未保存`, **「保存」「取消」两个都是灰的** |
| 改一个数 | 那一框标题挂上 `   ● 未保存`, **两个按钮都变亮**;**别的框的标题不受影响** |
| 把那个数改回原值 | 标记自己落下去, 两个按钮又变灰 |
| 改 A 框一个数 -> 按 B 框的「保存」 | A 框的标记**还在**, A 框的「取消」仍然亮着(证明只写了 B 那一框) |
| 改 A 框一个数 -> 按 A 框「保存」 | 标记落下; 关窗重开, 那个值还在 |
| 改一个数 -> 按「取消」 | 控件回到上次保存值; 那个改动真的下推给了控制器(不是只改界面) |
| 色标: 把最小值改到超过最大值 | 横幅弹出、最大值跟着挪(跨度不变) |
| 色标: 按「取消」 | 两个数一起退回上次保存值, 画布色阶跟着回去, 而且**没有**多弹一条"最小值超过最大值" |
| 扫描跑起来 | `lock_running` 那几项灰掉; 「扫描参数」那一框的「取消」同时灰掉(撬不开锁), 「保存」仍可按 |
| 扫描跑起来 -> 「回零」「高级选项」「色标」 | 这三框里没有 `lock_running` 项, 它们的「取消」照旧可用 |
| 「功率计」那一框 | 框里**没有**保存/取消, 与 §21 一样 |
| 按 **F5** | 状态栏/横幅报出刚读的那个 `scan.qss` 路径, 界面外观照文件里写的变 |

### 31.7 样式表外置 + F5 热重载(2026-09-23, 紧接上一节)

**起因是上一节逼出来的一个不方便**: §31.4 里那两次"按钮被挤成一条缝"、以及标题那一行
到底多高, 只能靠"改一个数 -> 编 -> 看"来试。样式表本来在 `scan/main.cpp` 里写死, 而
*外观这件事恰恰只能靠眼睛调* —— 颜色深一点浅一点、内边距多一点少一点, 编一次看一次的
周期太长。于是把它搬到 **exe 旁边那个文件**里, 改完在界面上按一下键就换过来。

**做法**(新增 `scan/scanstyle.{h,cpp}`, 只依赖 `Qt6::Core`):

| 函数 | 干什么 |
|---|---|
| `stylePath()` | exe 旁边那个 `scan.qss` —— 取 `applicationDirPath()`, 与 `prefsPath()` 同一个目录, 从哪儿启动都找得到 |
| `styleDefault()` | **出厂那一份** (源码里的 `kDefaultSheet`) |
| `styleLoad()` | 读文件; 没有 / 只有空白 -> `styleDefault()` |

四条规矩, 每一条都是有意选的:

- **程序里那份是"缺省", 不是"备份"。** 只有 exe 旁边**没有** `scan.qss` 时才把它写出去,
  文件在的时候**一个字节都不碰** —— 否则"改文件"这件事会在下一次启动被悄悄撤销。
  想回出厂样子就**删掉文件**, 与 `bin/scan.ini` 同一条规矩。
- **文件是空的 -> 当成没有。** 空样式表会把整个界面打回系统默认样子 (浅色、方角),
  那不是谁要的效果, 多半是存盘存坏了。这一条只在**返回值**上兜底, **不去改盘上那个文件**。
- **读不到不是错误。** 只读目录 / 没权限写不出文件也照样返回出厂那份, 界面正常开机。
  样式表是外观, 不该成为起不来的理由。
- **写到盘上的是 CRLF**(写的时候带了 `QIODevice::Text`)。读回来也带 `Text`, Qt 会把
  CRLF 折回 LF, 所以**往返无损**: 没改过的文件读回来与 `styleDefault()` 逐字节相等
  (探针验过, 见下)。Windows 文本编辑器打开它就是正常样子。

**F5**: `buildUi()` 末尾挂一个 `QShortcut(Qt::Key_F5)` (`WindowShortcut`, 焦点在哪个
输入框里都能按) 连到 `ScanWindow::reloadStyle()`。那个函数做三件事 ——
`qApp->setStyleSheet(styleLoad())`、立刻 `placePanelBars()`、再用 `QTimer::singleShot(0, …)`
补摆一次。

**为什么换完样式要重摆 [保存][取消] 那一行**: 那一行的高度、以及框标题那一行的下沿,
**都是样式算出来的** (§31.4: 标题那一行的下沿 = `contentsRect().top()`, 而它由
`QGroupBox` 的 `margin-top` 与 `::title` 的 `padding` 决定)。改了 `padding` 或字号它们全变。
补摆的那一次是等 `polish` 走完; 框自己因此收到的 `Resize` 也会走 `eventFilter()`,
三条路随便哪条先到都不吃亏。

**界面上不写"按 F5"这句话**: 它是一次性的调试入口, 不是操作流程 —— 与"按住 Ctrl 才改值"
那条闸同一个待遇(那条写成 tooltip, 因为它是**用起来会撞上**的)。知道这条键的途径是
`scan.qss` 自己的文件头注释、`scan/scanstyle.h` 顶部、以及 `README`。按下去会有一句回话
(横幅/状态栏报出刚读的路径) —— 那个键既不弹窗也没有菜单项, 不报一声就分不出
"读到了"与"没反应"。

**文件头注释里写了两处最容易改坏的地方**, 连同各自的症状: 标题那一行按钮的
`padding` (删掉那两个字会被挤成一条缝, 就是 §31.4 那个 bug) 与 `QGroupBox` 的
`margin-top` (改到比标题字还矮, 标题会被切掉一截)。文件里还写明它能改的是"长什么样",
**"谁摆在哪儿"改不了** —— 位置、行顺序、拉伸比例都在 `scan/scanwindow.cpp` 里。

**动到的文件**: 新增 `scan/scanstyle.{h,cpp}`; `scan/main.cpp`(样式表整段搬走,
只剩 `app.setStyleSheet(styleLoad());` + 一句 include); `scan/scanwindow.{h,cpp}`
(`reloadStyle()` 声明与实现、`buildUi()` 末尾那个 `QShortcut`、两个 include);
`scan/CMakeLists.txt`(**只进 GUI 目标**, 不进 `SCAN_COMMON_SRC` —— 自检那边没有界面,
放进去只是多一次磁盘访问); `.gitignore`(加 `/bin/scan.qss`, 与 `/bin/scan.ini` 并排);
`README.md`(`scan` 那一节加一段"exe 旁边那两个文件")。

**验到什么程度**:

- 全量重建 **0 warning 0 error**;`scan_selftest` **833 passed / 0 failed / 1 skipped**
  (与本节之前逐条相同)。
- **`styleLoad()` 的三条分支用一个临时探针真跑过** (探针建在 `out/styleprobe/`, 验完已删):
  文件不在 -> 返回出厂那份**并且**写出去一份; 文件在(手改过) -> 原样读回、**没被出厂那份
  盖掉**; 只有空白 -> 退回出厂**且没去动盘上那个文件**; 加上"盘上每一行都是 CRLF"与
  "二次读回与 `styleDefault()` 逐字节相等"两条。十条全过。
  (第一遍跑时"写出去的内容 == 返回的内容"是**红的** —— 原因就是上面那条 CRLF, 是探针
  测得比契约更严, 不是代码错; 把那条改成"往返无损"之后才对上。)
- **真机路径走了一遍**: 起一次 `bin/scan.exe`, `bin/scan.qss` 出现在 exe 旁边、
  2780 字节、头一行就是那句文件说明、`file` 认它是 UTF-8 + CRLF; 进程杀掉后干净退出。
  `git check-ignore` 确认它被 `.gitignore` 挡住了。
- **没验**: F5 那一下本身 —— 需要一个真按键盘的人。现在能保证的是"这个键建出来了、
  连到了 `reloadStyle`、那个函数编译通过、它调的那两条路 (`styleLoad` 读盘、
  `setStyleSheet`) 都验过"。剩下的就是按下 F5 看外观变不变。

