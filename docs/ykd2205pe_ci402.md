# YKD2205PE EtherCAT 驱动器速查

研控 (www.yankong.com) EtherCAT 总线型步进驱动器。CoE + CiA 402。
开环步进(适配 42 两相开环电机)；电源 DC 24~50 V；**最大输出电流 2 A**；100BASE-TX。
通信模式: **SM 同步** 与 **DC 同步** (同步周期 250µs~4000µs) 都支持。
SM0/SM1 = 邮箱收发、SM2 = RxPDO、SM3 = TxPDO。站号由拨码 SW1~6 设定 (0~63)，
驱动器上电时写入 ESC 别名寄存器 0012h。

**注意**: 拨码站号对 **pysoem** 不可见 —— CoE 0012h 对象实测缺失 (SDO
"object does not exist")，pysoem 封装的旧版 SOEM 也不暴露 ESC 别名寄存器。
因此本框架(python 链) **按 EtherCAT 总线线序 (从站 0/1) 定位轴**。

> 若要真正按手册读取该 0012h 别名寄存器，须用原生 SOEM：见下文
> 「读取拨码站号」，工具 = [`aliasinfo/`](../aliasinfo/)。
> (手册 V2.4 另有 **2215h 从站节点来源** / **2216h 从站节点号** 可以改这个别名，
> 但那是写操作，本仓库不用。)

## 0. 本文档的依据 (`YKD/`)

| 文件 | 文档 | 对本文档的意义 |
|---|---|---|
| `YKD/… V1.0.pdf` | **YKD2205PE 用户手册** (第一版) | 唯一逐条针对 YKD2205PE 的旧版手册 |
| `YKD/… V1.1.pdf` | **YKD2205PE 使用手册** V1.0 2021.12.30 / V1.1 2022.9.9 | **硬件/接线/指示灯/适配电机**，型号最准的一份。V1.1 更正了 V1.0 的电流指标 |
| `YKD/… V2.3.pdf` | EtherCAT 系列 总线型驱动器功能手册 V2.3.0 | 系列级功能手册 (含探针、STO、报警)；§3「运动控制 (CiA 402)」(p8~p17) 已摘入本文档 |
| `YKD/… V2.4.pdf` | 同上 V2.4 (修订历史末条「新增 6091h 对象字典描述」2025/03/05) | **最新**；附录 3 = YKD 系列对象字典 (含 YKD2205PE) —— 本表 6xxx/2xxx 的主依据 |
| `YAKO_MS_ECAT_V2.5.xml` | 厂家 ESI (2025) | SM/PDO 声明、CoE InitCmd、对象字典默认值；PDO 部分与手册**不一致**(见「PDO 映射」) |

下文每条事实后标注来源：`(V2.4 p84)` = V2.4 第 84 页；`[实测]` = 本仓库现场实测记录
(见 [slide_motion_verify.md](slide_motion_verify.md) §6 与 [baseline_ykd2205pe.ini](../baseline_ykd2205pe.ini))。

> **重要**: V2.4 的附录里有多份对象字典表，**只有「附录 3：YKD 系列」那张适用于本机**
> (支持型号: YKD2105PE / YKD2205PE / YKD2405PE / YKD2608PE / YKD3722PE)。
> 「附录 2：闭环系列」(MS/SSD/ESD) 的表里端子数、电流项、6502h 都与本机不同，别混用。

## 读取拨码站号 (原生 SOEM)

拨码站号存于每台从站 ESC 的 *Configured Station Alias* 寄存器 **0012h-0013h**
(由 SW1~6 在驱动器上电时写入)，普通 EtherCAT 主站启动时就靠读它识别/寻址。
python 链 (pysoem) 读不到这个寄存器，所以仓库里放了一个原生 SOEM 读取器工具
[aliasinfo](../aliasinfo/aliasinfo.c) (自有顶层工程，引用下载的 SOEM)：SOEM 在
`ecx_config_init()` 里已对每台从站 FPRD 0x0012，结果在 `ctx.slavelist[i].aliasadr`，
该工具只把它打印出来 (只读、不进 OP)。

构建并运行 (需要 VS/CMake；在仓库根配置顶层工程，`SOEM` 作为子工程):

```bash
cmake -S . -B build                 # 首次/改 CMakeLists 后重跑一次
cmake --build build --config Release --target aliasinfo
build/aliasinfo/Release/aliasinfo.exe '\Device\NPF_{GUID}'
```

输出形如 (位置 = 0-based 总线线序，与 python 链 `--pos` 一致):

```
2 台从站。位置为 0-based 总线线序 (主站起数)。
  位置    序号   配置地址     别名0012h      厂商ID      产品码  名称
  -----  -----  ------  ------------  ----------  ----------  ----
    0       1   0x0001        5 (0x0005)  0x00000994  0x00003000  YKD2205PE
    1       2   0x0002       17 (0x0011)  0x00000994  0x00003000  YKD2205PE
```

注意:
- 别名 = 0 表示拨码=0 或未启用，同型号多台仍按总线位置区分。
- 拨码站号是驱动器**上电时**读的，改动拨码须断电重启才生效。
- 该工具用 Npcap 独占网卡，运行期间勿同时跑 python 链的 pysoem 脚本。

## 识别信息 (1018h)

| 项 | 值 |
|---|---|
| Vendor ID (1018h:01) | 0x0994 |
| Product Code (1018h:02) | **0x3000** [实测] |
| Revision (1018h:03) | 0x0001 |
| 设备名 (1008h) | MS-ECAT |

> **手册里的 Product Code 是「系列占位值」，不是本机值**：V1.0 的表写 `0x2000`；
> V2.4 的 YKD 附录写 `0x2000/0x2100/0x2200` (V2.4 p77)。而 ESI (V2.5) 的
> `<Type ProductCode="#x3000">` 与 SII、CoE 1018h:02 的**实测值 0x3000** 三者一致。
> 代码识别集合 `YKD_PRODUCT_CODES = {0x2000, 0x3000}` 两者都接受，
> `master.is_ykd_slave()` 用于判断。

## CiA 402 对象 (6000h~6FFFh)

类型/范围/默认值取自 V2.4 附录 3 (YKD 系列, p85~p88)；`[实测]` 为现场读数。

| 对象 | 类型 | 说明 | 默认 |
|---|---|---|---|
| 603Fh | U16 RO | 驱动器故障码 (同 1003h 低 16 位)。0000h 无错误；FF01h 过流；FF02h 过压；FF03h 欠压；FF04h 动力线报警；FF06h 通讯报警；FF08h 传感器告警 | 0 |
| 6040h | U16 RW | 控制字 | 0 |
| 6041h | U16 RO | 状态字 | 0 |
| 605Ah | I16 RW | **Quick stop 方式**: 0 释放 / 1 停止 / 2 急停 | **1 (停止)** |
| 605Bh | I16 RW | **Shutdown 方式** | **0 (释放)** |
| 605Ch | I16 RW | **Disable operation 方式** | **1 (停止)** |
| 605Dh | I16 RW | **Halt 停止方式** | **1 (停止)** |
| 605Eh | I16 RW | **Fault reaction 方式** | **2 (急停)** |
| 6060h | I8 RW | 运行模式控制: 0 未定义 / 1 PP / 3 PV / 6 HM / 8 CSP | 0 |
| 6061h | I8 RO | 运行模式状态显示 (同上取值) | 0 |
| 6064h | I32 RO | 实际位置 (pul) | 0 |
| 606Ch | I32 RO | 实际速度 (pul/s) | 0 |
| 607Ah | 32 位 | 目标位置 / 手册称「总脉冲数」(PP 三段总步数)。手册两处类型不一致: §3.4/3.5 记 INT32 (-2^31~2^31-1)，附录表记 U32 | 0 |
| 607Ch | I32 RW | 回零偏移 / 原点补偿 (pul) | 0 |
| 607Dh:01 / :02 | I32 RW | 负向 / 正向软限位 | ∓2000000000 [实测 ±2000000000] |
| 607Eh | U8 RW | 极性 (电机方向): 0x00 默认 / 0x80 反转 | 0 |
| 6081h | U32 RW | 最大速度 (PP 速度, pul/s) | 0 |
| 6083h / 6084h | U32 RW | 加速度 / 减速度 (pul/s²) | 0 [实测 500000] |
| 6085h | U32 RW | 急停减速度 (pul/s²) | 0 |
| 6091h:01 / :02 | U32 RW | 齿轮比 分子=电机分辨率 / 分母=轴分辨率 | 手册自相矛盾，见下 |
| 6098h | I8 RW | 回零方式: **1~14、17~30、33~34、35** | **17** |
| 6099h:01 / :02 | U32 RW | 回零速度 (找原点) / 回零查询速度 (找到原点后返回) (pul/s) | 0 [实测 50000 / 10000] |
| 609Ah | U32 RW | 回零加减速度 (pul/s²) | 0 [实测 500000] |
| 60B8h | U16 RW | 探针控制 | 0 |
| 60B9h | U16 RO | 探针状态 | 0 |
| 60BAh~60BDh | I32 RO | 探针 1/2 上升沿/下降沿**锁存位置** | 0 |
| 60D5h~60D8h | U16 RO | 探针 1/2 上升沿/下降沿锁存**计数** | 0 |
| 60F4h | I32 RO | 位置误差 (1 编码器单位) | 0 |
| 60FDh | U32 RO | 数字输入状态 (见下) | 0 |
| 60FEh:01 / :02 | U32 RW | 物理输出开启 / 物理输出使能 (位在 Bit16~Bit31) | 0 |
| 60FFh | I32 RW | 最大速度 (PV 目标速度, pul/s) | 0 |
| 6502h | U32 RO | 支持操作模式位图 | 手册 **0x00A5** / ESI 0x01DD，见下 |

> **6502h 两处口径冲突**: 手册给 **0x00A5** = bit0(PP)+bit2(PV)+bit5(HM)+bit7(CSP)
> —— 正好是本机 6060h 支持的 1/3/6/8 四种模式，自洽；
> 而 ESI 声明是 **0x01DD** = bit0+bit2+bit3+bit4+bit6+bit7+bit8 (含 IP/CSP/CSV/TQ，
> 却**不含 bit5 HM**)，与 6060h 能跑回零矛盾。

> **6091h 默认值手册三处不一致**: §4.1 (V2.4 p18) 记 分子 10000 / 分母 4000；
> YKD 附录 (p86) 记 分子 **4000** / 分母 **10000** (正好互换)；V1.0 记
> 2409h 分子 4000 / 2408h 分母 1000。

## 控制字 6040h

| 位 | 名称 | 说明 |
|---|---|---|
| 0 | Switch on | 状态机 (见下) |
| 1 | Enable voltage | 状态机 |
| 2 | Quick stop | 状态机 (0 = 触发快速停止，按 605Ah 动作) |
| 3 | Enable operation | 状态机 |
| 4 | 按模式而定 | PP: New set-point (0→1 上升沿触发新目标)；HM: Homing operation start (0→1 启动回零)；CSP/PV: 保留 |
| 5 | 按模式而定 | PP: Change set immediately (1 = 立即更新)；CSP/PV/HM: 保留 |
| 6 | 按模式而定 | PP: abs/rel (0 绝对 / 1 相对)；其余模式保留 |
| 7 | Fault reset | 0→1 复位故障 |
| 8 | Halt | 0 = 按命令运动 / 1 = 停止 (暂停) |
| 9~11 | 保留 | 无功能 |
| 12~15 | 制造商指定 | 手册未定义功能 |

**(V2.4 p10)** CSP 模式下「控制字无(模式相关)功能」—— 只靠 bit0~3/bit7 的状态机
(即：CSP 只要使能 + 给 607Ah 就能动)。

### 模式相关位 (V2.3 §3.3.1, p10)

`Bit0`~`Bit3`、`Bit7`：用于 CiA402 状态机控制。`Bit9`~`Bit11`：保留位，暂无功能。
`Bit12`~`Bit15`：制造商指定，各厂家可根据需要自行定义功能，这里暂无功能。
`Bit4`~`Bit6`、`Bit8`：这些位在不同工作模式下功能略有不同：

| Bit | csp | pp | pv | home |
|---|---|---|---|---|
| 8 | Halt 暂停 | Halt 暂停 | Halt 暂停 | Halt 暂停 |
| 6 | reserved 保留 | abs/rel 决定/相对 | reserved 保留 | reserved 保留 |
| 5 | reserved 保留 | Change set immediately 立即执行 | reserved 保留 | reserved 保留 |
| 4 | reserved 保留 | New set-point 新目标设置 | reserved 保留 | Homing operation start 回零启动 |

(§3.3.1 的这张表把 `Bit8` 列为 csp 也有定义；§3.4 又写「控制字在该模式下无功能」
—— V2.3 p10 / p12。)

### 状态迁移 (V2.4 p9 / V1.0 p26)

| 命令 | Bit7 | Bit3 | Bit2 | Bit1 | Bit0 | 控制字值 | CiA402 迁移 |
|---|---|---|---|---|---|---|---|
| Shutdown | 0 | X | 1 | 1 | 0 | **0x06** | 2, 6, 8 |
| Switch on | 0 | 0 | 1 | 1 | 1 | **0x07** | 3 |
| Switch on + Enable operation | 0 | 1 | 1 | 1 | 1 | **0x0F** | 3 + 4 |
| Disable voltage | 0 | X | X | 0 | X | **0x00** | 7, 9, 10, 12 |
| Quick stop | 0 | X | 0 | 1 | X | 0x02 | 7, 10, 11 |
| Disable operation | 0 | 0 | 1 | 1 | 1 | 0x07 | 5 |
| Enable operation | 0 | 1 | 1 | 1 | 1 | 0x0F | 4, 16 |
| Fault reset | 0→1 | X | X | X | X | **0x80** | 15 |

### 手册的初始化步骤表 (V1.0 p26)

| 写 6040h | 手册称 | 6041h 低 4 位 | 电机 |
|---|---|---|---|
| **0x00** | 初始化步骤 0 | **0000** | 释放 |
| **0x06** | 初始化步骤 1 | **0001** | 释放 |
| **0x07** | 初始化步骤 2 | **0011** | 使能 |
| **0x0F** | 初始化步骤 3 | **0111** | 使能 |

## 状态字 6041h

| 位 | 名称 | 说明 |
|---|---|---|
| 0 | Ready to switch on | 状态机 |
| 1 | Switched on | 状态机 |
| 2 | Operation enabled | 状态机 (**bit2 = 1 就是「电机带电」**) |
| 3 | Fault | 状态机 (1 = 故障) |
| 4 | Voltage enabled | **上电后该位置 1** |
| 5 | Quick stop | 该位为 **0** 表示驱动器正在快速停止 |
| 6 | Switch on disabled | 状态机 |
| 7 | Warning | 1 = 驱动器内部存在警告 |
| 8 | 制造商指定 | 手册未定义功能 |
| 9 | **Remote** | **该位为 0 时无法对控制字进行操作** |
| 10 | 按模式而定 | PP/PV/HM: Target reached；CSP: 保留 |
| 11 | Internal limit active | 硬件限位信号有效时置 1 |
| 12 | 按模式而定 | PP: Set-point acknowledge；PV: Speed (速度为 0)；HM: Homing attained；CSP: 位置跟随 |
| 13 | 按模式而定 | PP: Following error；PV: Max slippage error；HM: Homing error；CSP: 保留 |
| 14~15 | 制造商指定 | 手册未定义功能 |

### 模式相关位 (V2.3 §3.3.2, p11)

`Bit0`~`Bit3`、`Bit5`~`Bit6`：用于 CiA402 状态机反馈。`Bit4`：上电有效位，上电后
该位置 1。`Bit5`：快速停止位，该位为 0 时表示驱动器正在快速停止。`Bit7`：警告位，
该位为 1 时表示驱动器内部存在警告信息。`Bit8`：制造商指定位。`Bit9`：远程位，该位
为 0 时无法对控制字进行操作。`Bit11`：硬件限位有效位，当限位信号有效时，该位置 1。
`Bit14`~`Bit15`：制造商指定。`Bit10`、`Bit12`、`Bit13` 随工作模式定义不同：

| Bit | csp | pp | pv | home |
|---|---|---|---|---|
| 13 | reserved 保留 | Following error 跟随错误 | Max slippage error 跟随速度错误 | Homing error 回原点错误 |
| 12 | Driver follow the command value 位置跟随 | Set-point acknowledge 位置应答 | Speed 速度为 0 | Homing attained 回零完成 |
| 10 | reserved 保留 | Target reached 目标位置到达 | Target reached 目标速度到达 | Target reached 位置到达 |

### 状态迁移 (V2.4 p9 / V1.0 p26)

| Bit6 | Bit5 | Bit3 | Bit2 | Bit1 | Bit0 | 状态 |
|---|---|---|---|---|---|---|
| 0 | X | 0 | 0 | 0 | 0 | **Not ready to switch on** |
| 1 | X | 0 | 0 | 0 | 0 | **Switch on disabled** |
| 0 | 1 | 0 | 0 | 0 | 1 | **Ready to switch on** |
| 0 | 1 | 0 | 0 | 1 | 1 | **Switched on** |
| 0 | 1 | 0 | 1 | 1 | 1 | **Operation enabled** |
| 0 | 0 | 0 | 1 | 1 | 1 | **Quick stop active** |
| 0 | X | 1 | 1 | 1 | 1 | **Fault reaction active** |
| 0 | X | 1 | 0 | 0 | 0 | **Fault** |

低 4 位因此就是 `0000 / 0001 / 0011 / 0111`，与上面「初始化步骤表」逐行对应。

### 状态机各状态的含义 (V2.3 p8)

CiA402 协议中定义了运动控制设备的标准状态机，同时还定义了各种运行模式，以及它们在
对象字典中的定义。标准状态机 (State machine) 描述了设备的状态和驱动可能的控制序列。
每一步状态表示了一个特定的内部或者外部行为，设备的状态也决定了哪些命令可以被接收。

| 状态 | 手册说明 |
|---|---|
| Not Ready to Switch on | 设备电源已接通，驱动器已经初始化，并执行内部自检，制动器也被激活 |
| Switch on Disabled | CANopen 通信已开始，可以利用 SDO 通信服务对驱动器进行参数设置 |
| Ready to Switch on | 驱动器继续进行参数设置，电机没有被励磁 |
| Switched on | 驱动器电机准备好状态，输出级电压将在该状态中最后接通，但不能执行驱动功能 |
| Operation Enable | 驱动器电机使能，驱动器正常运行状态，按照控制模式控制电机 |
| Quick Stop Active | 快速停止功能被激活，驱动功能已启动，同时启动了电机 |
| Fault Reaction Active | 驱动器检测到报警发生，按照设定方式停机，电机仍使能 |
| Fault | 错误出现，允许更改驱动器参数 |

> **`[实测]`**：两台上电后第一次读到的 6041h 是
> **0x0210** (bit4 Voltage enabled + bit9 Remote，低 4 位 = 0000 = 手册「步骤 0」态)。
> 随后程序写 6040h = **0x0000**，再读到的是 **0x0231** (bit0+bit4+bit5+bit9，
> 低 4 位 = **0001** = 手册「步骤 1」态，即 **Ready to switch on / 电机释放**)。
> 按手册，**0000 和 0001 都是「电机释放」**(步骤 0 与步骤 1)，都不是故障。
> 要判「未使能且无故障」，判据是 **`(6041h & 0x000C) == 0`**
> (bit2 = 未使能、bit3 = 无故障)。0x0210 / 0x0231 的 **bit9** 都是 1 (Remote 有效)。

## 四种工作模式 (V2.3 §3.2, p9~p10；§3.4~§3.7, p11~p17)

CANopen 通过对象 `6060h`(Mode of operation)对驱动器工作模式进行设置，并通过对象
`6061h`(Mode of operation display)反映驱动器当前的工作模式状态。驱动器目前支持
4 种工作模式：循环同步位置模式 (CSP)、位置模式 (PP)、速度模式 (PV)、回原点模式
(home) (V2.3 p9)。

| `6060h` | 工作模式 |
|---|---|
| 0 | 未定义 |
| 1 | 位置模式 (pp) |
| 3 | 速度模式 (pv) |
| 6 | 回原点模式 (home) |
| 8 | 循环同步位置模式 (csp) |

### 循环同步位置模式 csp (V2.3 §3.4, p11~p12)

循环同步位置模式下，控制器完成位置指令规划并输出规划好的目标位置 `607Ah`，同时
以周期同步的方式将目标位置发送给驱动器，驱动器内部完成位置、速度控制。需将工作
模式对象 `6060h` 值设置为 8，当工作模式状态对象 `6061h` 寄存器读取为 8 时可进行
CSP 工作模式的相关操作。

通信对象：控制字 `6040h`、状态字 `6041h`、工作模式 `6060h`、模式显示 `6061h`、
目标位置 `607Ah`、实际位置 `6064h`。

**控制字在该模式下无功能** (V2.3 p12)。

| `6041h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit12 | Drive follow the command value | 0 | 从站未跟随指令 |
| Bit12 | Drive follow the command value | 1 | 从站跟随指令 |
| Bit13 | Following error | 0 | 没有位置偏差过大故障 |
| Bit13 | Following error | 1 | 发生位置偏差过大故障 |

| 对象 | 名称 | 数据类型 | 访问 | 单位 | 范围 |
|---|---|---|---|---|---|
| `607Ah`:00 | 目标位置 | INT32 | RW | p | -2^31~2^31-1 |
| `6064h`:00 | 实际位置 | INT32 | RO | p | -2^31~2^31-1 |

### 位置模式 pp (V2.3 §3.5, p12~p14)

此模式主要用于点位定位运动，该模式下，控制器给定目标位置 `607Ah`，位置曲线速度
`6081h`，加速度 `6083h`，减速度 `6084h` 参数，急停减速 `6085h` 驱动器内部完成轨迹
规划。需将工作模式对象 `6060h` 值设置为 1，当工作模式状态对象 `6061h` 寄存器读取为
1 时可进行 PP 工作模式的相关操作。

通信对象：控制字 `6040h`、工作模式 `6060h`、目标位置 `607Ah`、轮廓速度 `6081h`、
加速度 `6083h`、减速度 `6084h`、急停减速度 `6085h`、状态字 `6041h`、模式显示
`6061h`、实际位置 `6064h`。

| `6040h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit4 | New set-point | 0 | 没有目标位置 |
| Bit4 | New set-point | 1 | 目标位置更新，该位从 0 到 1 的上升沿表示预触发新的目标位置 `607A` |
| Bit5 | Change set immediately | 0 | 完成当前目标位置后更新 |
| Bit5 | Change set immediately | 1 | 立即更新 |
| Bit6 | abs/rel | 0 | 目标位置是一个绝对值 |
| Bit6 | abs/rel | 1 | 目标位置是一个相对值 |
| Bit8 | Halt | 0 | 按 Bit4~Bit6 命令运动 |
| Bit8 | Halt | 1 | 停止 |

| `6041h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit10 | Target reached | 0 | Halt=0：目标速度没有到达；Halt=1：轴减速 |
| Bit10 | Target reached | 1 | Halt=0：目标速度到达；Halt=1：轴速度为 0 |
| Bit12 | Drive follow the command value | 0 | 可更新目标位置 |
| Bit12 | Drive follow the command value | 1 | 不可更新目标位置 |
| Bit13 | Following error | 0 | 没有位置偏差过大故障 |
| Bit13 | Following error | 1 | 发生位置偏差过大故障 |

在位置模式下，通过向控制字 `6040h` 的 Bit4 位依次写入 0 和 1 产生上升沿以完成运动
触发。一旦驱动器收到触发命令，状态字的 Bit12 被置为 1，运动开始，同时通过控制字的
bit6 控制该运动属于绝对定位运动还是相对定位运动。

一旦到达目标位置，状态字的 Bit10 被置为 1，如果没有到达目标位置，或者出现位置误差
偏差过大故障，则该位保持 0 状态。

当新目标位置更新时会有两种情况出现：当前目标位置到达和当前目标位置未到达。如果在
当前目标位置未运动到达的情况下出现新的目标位置和新的触发命令，应根据控制字 `6040h`
的 Bit5 位给定情况做出相应反应 (V2.3 p13~p14)：

- `6040h` 的 Bit5 位为 0 时，表示必须完成当前运动后再响应新的目标位置和触发命令
  (时序图：**新目标位置出现后不立即更新**)。
- `6040h` 的 Bit5 位为 1 时，表示打断当前运动马上响应给定的新的目标位置和触发命令
  (时序图：**新目标位置出现后立即更新**)。

### 速度模式 pv (V2.3 §3.6, p15)

此模式下，控制器将目标速度 `60FFh`，加速度 `6083h`、减速度 `6084h` 发送给驱动器，
由驱动器内部完成速度曲线规划。需将工作模式对象 `6060h` 值设置为 3，当工作模式状态
对象 `6061h` 寄存器读取为 3 时可进行 PV 工作模式的相关操作。

通信对象：控制字 `6040h`、工作模式 `6060h`、轮廓速度 `60FFh`、加速度 `6083h`、
减速度 `6084h`、状态字 `6041h`、模式显示 `6061h`、实际速度 `606Ch`。

| `6040h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit8 | Halt | 0 | 按 `60FFh` 给定目标速度运动 |
| Bit8 | Halt | 1 | 停止 |

| `6041h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit10 | Target reached | 0 | Halt=0：目标速度没有到达；Halt=1：轴减速 |
| Bit10 | Target reached | 1 | Halt=0：目标速度到达；Halt=1：轴速度为 0 |
| Bit12 | Speed | 0 | 速度不为 0 |
| Bit12 | Speed | 1 | 速度为 0 |

### 原点模式 home (V2.3 §3.7, p16~p17)

回原点模式下需将原点信号接入驱动器输入端子，驱动器内部完成对机械原点的查找和定位。
需将工作模式对象 `6060h` 值设置为 6，当工作模式状态对象 `6061h` 寄存器读取为 6 时
可进行 HM 工作模式的相关操作。

通信对象：控制字 `6040h`、工作模式 `6060h`、回原点模式 `6098h`、回原点速度
`6099h`、加减速度 `609Ah`、原点偏移量 `607Ch`、状态字 `6041h`、模式显示 `6061h`。

| `6040h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit4 | New set-point | 0 | 回原点未启动 |
| Bit4 | New set-point | 0->1 | 回原点动作启动 |
| Bit4 | New set-point | 1 | 回原点启动 |
| Bit4 | New set-point | 1->0 | 回原点动作中断 |
| Bit8 | Halt | 0 | 按 Bit~Bit6 命令运动 (原文如此) |
| Bit8 | Halt | 1 | 停止 |

| `6041h` | 名称 | 值 | 描述 |
|---|---|---|---|
| Bit10 | Target reached | 0 | Halt=0：原点位置没有到达；Halt=1：轴减速 |
| Bit10 | Target reached | 1 | Halt=0：原点位置到达；Halt=1：轴速度为 0 |
| Bit12 | Drive follow the command value | 0 | 回原点模式未完成 |
| Bit12 | Drive follow the command value | 1 | 回原点模式完成 |
| Bit13 | Following error | 0 | 没有回原点故障 |
| Bit13 | Following error | 1 | 回原点动作失败 |

(`6041h` Bit12/Bit13 在这一节里用的是 §3.3.2 表里 CSP 那两位的名称，描述是回零的
含义；§3.3.2 表里这两位的名称是 Homing attained / Homing error —— V2.3 p11 / p16。)

驱动器支持 1~14、17~30、33、34、35 回原点方法，其中，1~14、33、34 回零模式需配套
使用带 Z 信号的闭环步进电机，具体描述可见附录 1 (V2.3 p17)。各方法取值与轨迹见上文
「回零 (6098h)」。

## PDO 映射

| 来源 | 生效的 RxPDO | 生效的 TxPDO | SM2 / SM3 长度 |
|---|---|---|---|
| 手册 V2.4 附录 3 (p78~79, YKD) | 只映射 **6040h**，其余 11 项未映射 | **全部未映射** | 手册未给 |
| 手册 V1.0 (p38~40) | 同上 | **全部未映射** | 手册未给 |
| ESI V2.5 `<RxPdo>/<TxPdo>` | 5 项: 6040h:16 / 607Ah:32 / 60FFh:32 / 60B8h:16 / 60FEh:01:32 = **16 B** | 12 项: 603Fh:16 / 6041h:16 / 6064h:32 / 60FDh:32 / 606Ch:32 / 60F4h:32 / 60B9h:16 / 60BAh~60BDh:32×4 / 6061h:8 = **39 B** | Outputs 16 / Inputs 39 |
| ESI V2.5 `<Dictionary>` 默认值 | 2 项: `60400010h` + `60600008h` = **3 B** | 2 项: `60410010h` + `60610008h` = **3 B** | (同上) |
| **[实测] 2026-09-15 `motor_test` 只读运行** | **1C12h = { 1601h }**，1601h 共 7 项 = **23 B** (见下) | **1C13h = { 1A00h }**，1A00h 共 3 项 = **10 B** | IOmap **输出 23 + 输入 10** |

三份资料说的是「1C12h = { 1600h }」，而 **[实测] 这台设备的 1C12h 指的是 `1601h`**，
`1600h` 是同一台驱动器里另一张**没有生效**的表。所以**写死的必须是分配对象里读出来的
那个索引**：照着 `1600h` 去读偏移、去补缺项不会报任何错，而那张表里的字段齐不齐都与
过程数据无关。

1C13h = { 1A00h }（与三份资料一致）；1C00h:01~04 = SM0~SM3 通讯类型 1/2/3/4。

**[实测] 2026-09-15 的完整实读值** (只读运行，一个字节都没写)：

```text
1C12h (分配): 1 项  0x1601
1601h (映射): 7 项
  [1] 6040h:00  16 bit  @ +0    控制字
  [2] 6060h:00   8 bit  @ +2    运行模式
  [3] 607Ah:00  32 bit  @ +3    目标位置 (CSP)
  [4] 60FFh:00  32 bit  @ +7    目标速度 (PV)
  [5] 6081h:00  32 bit  @ +11   轮廓速度 (PP)
  [6] 6083h:00  32 bit  @ +15   轮廓加速度
  [7] 6084h:00  32 bit  @ +19   轮廓减速度
  合计 184 bit = 23 字节      <-- 那 23 字节就是它, 已解释
1C13h (分配): 1 项  0x1A00
1A00h (映射): 3 项
  [1] 6041h:00  16 bit  @ +0    状态字
  [2] 6064h:00  32 bit  @ +2    实际位置
  [3] 606Ch:00  32 bit  @ +6    实际速度
  合计 80 bit = 10 字节
```

**读法**:
- **不要相信任何一份「出厂映射」**。同一个厂家在手册、ESI 声明、ESI 字典默认值里
  给了三种答案，真机上量到的又是第四种 —— 驱动器当前的映射很可能被厂家上位机
  改过。**每次运行前读一遍 `1C12h` / `1C13h`，按它们指到的索引再读那张映射表，
  再定偏移。**
- 三份资料唯一的共识是: **6040h 在 RxPDO 的第 0 项**，所以控制字在输出镜像的 `+0`
  字节 —— 这一条 [实测] 也成立。但 6041h 在 ESI 声明里位于 **输入镜像 +2**，
  [实测] 1A00h 里却在 **+0**：偏移必须从实读的映射表推，不能照抄任何文档。
- 实测输出 23 字节**不是** `1600h`，而是 `1601h`：6040h(2) + 6060h(1) + 607Ah(4) +
  60FFh(4) + 6081h(4) + 6083h(4) + 6084h(4) = 23。
- 副作用：**当前生效的映射里 `6040h` / `607Ah` / `60FFh` 已经齐全**，所以做
  CSP / PV / HM **不需要改写任何 PDO 映射**。
- **生效 RxPDO 里的每一项都是"主站拥有"的 —— 不驱动它，就是在每周期下发 0。**

  主站每周期把整块输出镜像原样发出去，驱动器控制环连续采样自己的 SM2 缓冲。
  所以表里每一项的主站侧都是**由我们决定**的：写了就是那个值，没写就是 **0**，
  而且不会有任何报错。`1601h` 有 7 项，`motor_api` 曾经只驱动其中 4 项。

  > **通用规则（与具体驱动器无关）：一个对象只要在生效的 RxPDO 里，就必须经过程
  > 数据驱动它；SDO 写会被下一帧覆盖。** 反过来，不在 RxPDO 里的对象才轮到 SDO。
  > 判断"在不在"只能实读映射表 —— 所以 `motor_api` 在 setup 时逐项实读偏移。
  >
  > 另一个容易踩的变体：**驱动器可能只接受在"未使能"状态下改运行模式**（CiA402 的
  > 要求）。经过程数据改也一样 —— 要先失能，再改 `6060h`，再使能。

  两个实测到的例子，都是"没写的那一项"：

  **① `6060h`（运行模式）在 `+2`。** [实测] 2026-09-15 的 S6：SDO 写 `6060h = 8`
  （CSP），回读 `6060h` 对象**确实是 8**，但 `6061h`（实际模式）一直读回 **0** ——
  因为主站每周期都在下发 `6060h = 0`，驱动器认的是过程数据那一侧。

  **② `6083h` / `6084h`（轮廓加减速度）在 `+15` / `+19`。** [实测] 2026-09-15 的
  PV 运行**全阶段 PASS、退出码 0，而电机一步没走**：

  ```text
  S4/S5/S6 → 停后:  6064h = 15270510   (全程一个计数都没变)
                    606Ch = 0          (全程)
  使能时 6041h = 0x1637,  跑的时候 = 0x0237   (bit12 "Speed=0" 被清零)
  ```

  `6041h` bit12 在 PV 下是 `Speed`（1 = 速度为 0），它**跟着我们下发的 `60FFh`
  变了** —— 说明驱动器确实收到了速度指令、也承认目标速度非零。但 `6083h = 0`
  意味着**斜坡永远起不来**：驱动器认为该转，实际减速器上不来，`606Ch` 停在 0，
  一步不走。驱动器自己的 `6083h/6084h` 是 **500000**（见 `baseline_ykd2205pe.ini`），
  被主站每周期下的 0 盖掉了。判据只能是 `6064h`（实际位置）有没有变、
  `606Ch` 有没有非零。

- **`6081h` 是这条规则下唯一"有意不驱动"的一项**：它只在 PP 下当轮廓速度用，而
  PP 是 `slide_motion` 的活。主站下发 0 与驱动器基线（`6081h = 0`，表示"从未设过"）
  一致。判据是 `em_dump_pdo()` 里那张「本接口对生效 RxPDO 每一项的处置」表：7 项
  逐项写清偏移与"驱动 / 不驱动"。加字段时先看这张表还差谁。
- 顺带一个容易忽略的后果：**进 OP 的"只读"运行同样在覆盖这些项。**
  带 `--allow-pdo` 就会进 OP 并开始发帧，于是连"只读观测"那次也把
  `6081h`/`6083h`/`6084h` 推成了 0。所以 `motor_api` 的 pin 动作放在
  `em_setup()` 里、**进 OP 之前**。
- 这正是 `test2.c` / `sm_pdo.c` / `motor_api` 里「**偏移证不出就拒绝 (退出码 4)**」
  那条护栏：`6040h`/`6041h` 在过程数据镜像里的字节偏移必须能从映射表严格推出。

> **对「空 TxPDO 挡 SAFE_OP」这条根因**：
> 手册确实说 1A00h 出厂未映射，`[实测] 2026-09-11` 也确实拿到
> **AL 状态码 0x001E (Invalid input configuration)**、从站拒绝 PRE_OP→SAFE_OP；
> 但 `[实测] 2026-09-15` 的 test2 运行里 **1A00h 已含 6041h、SM3 = 10 字节，
> SAFE_OP 与 OP 都到达了**。所以「出厂空 TxPDO」是**手册口径**，
> 报 0x001E 时要先去读 1A00h/1C13h 确认，不要默认它就是空的。

## 数字输入 / 输出与限位

本机硬件 (V1.1 §1.2/§4.2.4): **4 路光电隔离输入 X0~X3** (5~24 V，共 XCOM，
**电平脉冲宽度需 > 10 ms**)、**2 路光耦隔离输出 Y0/Y1**。

- **2004h** (U16 RO): 输入端子电平 **Bit0~Bit3 = X0~X3** (物理状态，常开/常闭由 2300h 决定)。
- **2005h** (U16 RO): 输出端子状态 **Bit0~Bit1 = Y0~Y1**。
- **2300h / 2301h** (U16 RW, 默认 0): 输入/输出端子**有效电平逻辑**，对应位 0 = 常开、1 = 常闭。
  > **本机实测 (2026-09-18) 是 `0`（常开 = 有效电平为高），而现场 X0~X3 接的是 NPN
  > 传感器（**高电平 = 未触发**）—— 两者正好反着**，于是"没触发"被读成"触发"，
  > `60FDh` 的 bit1/bit2 常年同时置起、`6041h` bit11 恒为 1，扫描一步都开不了。
  > 现场症状是拒绝启扫的话里说"正限位与负限位**同时**压着"—— 那个组合物理上不成立
  > （见 `scan_sweep.md` §13）。
  > 修法两条，层次不同：改这里的 bit0~bit2 置 1（`0x0007`）是**修根**（驱动器的限位
  > 保护、`60FDh`、bit11 一次全对），也是程序现在的默认动作（`em_di_set_logic`，
  > 连接时写、收尾还原，从不写 2102h）；上位机的「上位机侧取反」**只治软件那一侧**，
  > 驱动器自己的 bit11 与限位保护不受它影响。两条一起上 = 双反相 → 扫描永远开不了。
  > 手册没写 `2300h` 会不会被驱动器自己落 EEPROM。
- **2310h~2313h / 2320h~2321h / 2330h~2333h**: 端子功能与滤波，见下表。
- **60FDh** (U32 RO) 是**经过 2300h 电平反转与 2310h 功能映射之后**的结果：

| 位 | 含义 |
|---|---|
| Bit0 | 负限位 |
| Bit1 | 正限位 |
| Bit2 | 原点开关 |
| Bit3~Bit15 | 保留 |
| Bit16 / Bit17 | 探针 1 / 探针 2 |
| Bit18~Bit22 | 用户自定义 0~4 |
| (Bit31) | Z 信号 —— 该位只在 V2.4 的 **MS-Mini3E/S3E 附录**里写明；YKD 表未列 |

**这三位是 `2300h` 反转与 `2310h`~`2312h` 功能映射之后的结果**，调用方**不再取反**。
反过来说：**`2300h` 配反了三个灯一起反相**（`sm_baseline.c` 把 `2300h` 记为 WARN 级
正是这个原因）。

### 本仓库怎么用它 (`motor_api` / `scan`，2026-09-17)

- 位定义在 [ec_motor.h](../motor_api/ec_motor.h) 的 `EM_DI_NEG_LIMIT` /
  `EM_DI_POS_LIMIT` / `EM_DI_HOME`（`EM_DI_X3` 也定义了，本机未接线）。
- `em_setup` **只在 `60FDh` 已经在生效 TxPDO 里时**绑它（`em__find_field`，**只绑不补**）。
  绑上/绑不上都会打一行 `printf` 如实报告。
- 访问器 `em_dig_in_known()` / `em_di_home()` / `em_di_poslim()` / `em_di_neglim()` /
  `em_dig_in_raw()` / `em_dig_in_offset()`。**一律先问 `em_dig_in_known()`**：
  它 = `off_dig_in >= 0 && mirror_ok`，因为「读不到」和 `0x00000000`
  在数值上长得一模一样，而 0 在"开关压着没有"这个问题上是个**看起来完全正常**的答案。
- 要让 `60FDh` 进 TxPDO 有两条路：**用厂家上位机改一次 `1A00h`（推荐，零代码、断电也在）**，
  或者 `em_require_dig_in()` 在连接时补一次（仅写 RAM，收尾还原；崩在收尾前会留在驱动器里）。
  `scan` 界面「高级选项」里的「让 60FDh 进 TxPDO」就是后者，**默认开**。

### 端子功能配置 (V2.4 p84)

| 对象 | 默认 | 现场实测 |
|---|---|---|
| 2310h 输入 X0 功能选择 | 1 (原点) | 1 |
| 2311h 输入 X1 功能选择 | 2 (正限位) | 2 |
| 2312h 输入 X2 功能选择 | 3 (负限位) | 3 |
| 2313h 输入 X3 功能选择 | 0 (未定义) | 0 |
| 2320h 输出 Y0 功能选择 | 0 | 0 |
| 2321h 输出 Y1 功能选择 | 0 | 0 |
| 2330h~2333h 输入 X0~X3 滤波时间 (0~65535, 单位见手册) | 2 | 2 |

**输入功能码 (0~13)**: 0 未定义 / 1 原点 / 2 正限位 / 3 负限位 / 4 停止 / 5 急停 /
**6 无** / 7 探针 1 / 8 探针 2 / 9~13 用户自定义 0~4。
**输出功能码 (0~11)**: 0 未定义 / 1 报警信号 / 2 到位信号 / **3 无** / 4 刹车信号 /
5 Z 信号 / 9~11 用户自定义 0~2。

> **60FEh 不是「写得进 Y0/Y1 的位图」**：它是分子索引对象，
> `60FEh:01 物理输出开启`、`60FEh:02 物理输出使能`，**位定义在 Bit16~Bit31**。
> (V2.4 p87~p88)

### 回零 (6098h)

手册支持 **1~14、17~30、33~34、35**，默认 **17**；其中 **1~14、33、34 需配带 Z 信号的
闭环电机**，本机是开环机型，实际可用的是 17~30、35。完整轨迹图见 V2.4 附录 1。

| 值 | 原点 / 减速点 | 动作 (手册正文) |
|---|---|---|
| 17 | 原点 = 负限位，减速点 = 负限位 | a) 减速点无效时: **反向高速** → 遇减速点上升沿减速停止 → **正向低速** → 遇减速点下降沿停机；b) 减速点有效时: **正向低速**运行 → 遇减速点下降沿停机 |
| 18 | 原点 = 正限位，减速点 = 正限位 | a) 减速点无效时: **正向高速** → 遇减速点上升沿减速停止 → **反向低速** → 遇减速点下降沿停机；b) 减速点有效时: **反向低速**运行 → 遇减速点下降沿停机 |
| 24 | 原点 = 原点信号，减速点 = 原点信号 | 正向高速找原点 → 若先撞正限位则反向继续找 → 二次低速定位后停机 |
| 29 | 原点 = 原点信号，减速点 = 原点信号 | 反向高速找原点 → 若先撞负限位则正向继续找 → 二次低速定位后停机 |
| 35 | — | **以当前位置为机械原点** |

- 17/18/19/20/21… 的「原点是限位还是原点开关」「减速点是什么」在手册里逐条给出，
  上面只摘了本仓库在用的四个；**注意第二段的方向**：17 的第二段是**正向**、
  18 的第二段是**反向**。
- 只有 17/18 这两个「以限位为原点」的方式，手册才分 a)/b) 两条分支 —— 分岔的判据是
  **回零启动那一刻减速点（= 那一侧的限位开关）信号有效还是无效**。两条分支的终点都是
  **开关的释放点**（刚退开一点的那个位置）。(V2.4 p46~p48)
- 因此 17 与 18 的**首段方向是相反的**，而同一个方式的 **a) 与 b) 之间又各反一次**：
  17 a) 首段反向 / 17 b) 首段正向；18 a) 首段正向 / 18 b) 首段反向。
- `6099h:01` = 找原点速度，`6099h:02` = 找到原点后返回速度 (越小精度越高)；
  `609Ah` = 回零加减速度；`607Ch` = 原点偏移。
- `22 14h 回零辅助设置` (0~11, 默认 0) 决定**回零完成后 6064h 显示什么**
  (是否叠加/取反 607Ch、显示 0 还是实际值)。
- `221Eh 回零超时报警` (ms，默认 0 = 不开启)。

#### 本仓库的上位机用哪几个方式

**`scan` 用 24 / 29 / 18 / 17 这四个**，分两排按钮：

| 按钮 | 方式号 | 基准 | 算方式号的函数 |
|---|---|---|---|
| 「X/Y 正向回零」「X/Y 反向回零」 | 24 / 29 | **原点开关 X0** | `ecatcmd::home_method_for` |
| 「X/Y 找正限位」「X/Y 找负限位」 | 18 / 17 | **正 / 负限位开关** | `ecatcmd::home_lim_method_for` |

- 两排的差别只有**基准**：24/29 找原点开关，18/17 把那一侧的限位开关当机械基准
  （手册把后者叫「找限位」）。四个方式都走同一条 `em_home()`，它里面没有任何按方式
  分支的逻辑 —— 朝哪走、什么时候停全由驱动器按 6098h 自己决定。
- 24/29 是**唯一一对"朝一个方向高速去找，找不到就换另一边继续找"**的通用方式
  （见表里那两句："若先撞正/负限位则反向继续找"）。
- **35**：以当前位置为机械原点，**它不去找** —— 按一下就把当前位置定义成原点。
- **1~14、33、34 不可用**：需配带 Z 信号的闭环电机，本机是开环机型。
- 18/17 的 a)/b) 分支由**驱动器**按启动那一刻开关的信号自己选，上位机只负责在发起前
  把它读出来（60FDh 的 bit1/bit0）并预告这一趟走哪条；读不到就没法预告，所以那是一种
  拒绝发起的条件（见 `docs/scan_sweep.md` 的「找限位」一节）。

**「正向 / 反向」说的是电机轴的正反向**，与画布上的哪一边、与用户面对的哪一边都没有
必然关系。

**速度 × 超时 = 能找多远。** `6099h:01` 是找原点速度，而 `em_home()` 等 `6041h` bit12
有个上限（`scan` 用 30 秒），所以**它能找的距离是有限的**：按驱动器自己那个
`6099h:01 = 50000` 算是 1500000 脉冲 = 30 圈，而缺省扫描区域是 27 圈；速度调低时能找
的范围跟着缩小（2000 pul/s 只有 1.2 圈）。

**`609Ah` 与 `6099h:01` 的关系**：`609Ah = 6099h:01 × 10` 就是**斜坡时间 0.1 秒**，
驱动器自己那对实测值（`6099h:01 = 50000` / `609Ah = 500000`）相除正好也是 0.1 秒。
`scan` 不写死加速度，而是按斜坡时间反推（`ecatcmd::home_accel_for`），并在 `500000`
（器件自己那个数，`6083h`/`6084h` 也是它）封顶。

**回零之后 `6064h` 读作什么由 `2214h` 决定。** `em_home()` 对这个值不做任何断言，
`scan` 的做法是**从 `6064h` 重新锚定显示零点**（`m_origin[axis] = em_pos(ax)`），
而不是假设它等于 0 —— 见 [scan_sweep.md](scan_sweep.md) §18.3 与 §18.5。

> **回零用的是驱动器自己读到的开关电平**（`2310h~2312h` 映射到哪一路、`2300h`
> 的极性怎么算，都是驱动器的事）。所以上位机的「上位机侧取反」那个勾选框
> **对回零一点用都没有** —— 它只改上位机这一侧的中止判据。极性配反时
> （2026-09-18 实测 `2300h = 0` + NPN 传感器）**必须先改驱动器的 `2300h`**
> （「高级选项」里的「写驱动器 2300h」默认开着，做的就是这件事）。

## 驱动器参数 (2000h~2FFFh)

默认值/范围取自 V2.4 附录 3 (YKD 系列, p81~p85)，`[实测]` 取自
[baseline_ykd2205pe.ini](../baseline_ykd2205pe.ini)。

| 对象 | 说明 | 默认 | 实测 |
|---|---|---|---|
| 2004h / 2005h | 输入/输出信号状态 | 0 | — |
| 2102h | EEPROM 操作: **0 无效 / 1 参数恢复出厂 / 2 参数保存到 EEPROM**。触发方式: **先写 0，再写 1** | 0 | — |
| 2201h | **电子齿轮比与细分切换: 0 细分有效 / 1 电子齿轮比有效** | **0 (细分有效)** | 值未读 (对象可读) |
| 2203h | 使能信号清除故障选择: 0 清除 / 1 不清除 | 1 | — |
| 2204h | 超程停车方式: 0 停止 / 1 急停 / 2 无效 | 0 (停止) | 0 |
| 2208h / 2209h | 脉冲指令滤波使能 / 系数 | 0 / 5 | — |
| 2214h | 回零辅助设置 (0~11) | 0 | — |
| 2215h / 2216h | 从站节点来源 (0 拨码 / 1 对象) / 从站节点号 (写入 ESC 0012h，重新上电生效) | 0 / 1 | — |
| 2217h | 同步帧阈值 (同步帧报警阈值) | 20 | — |
| 2300h / 2301h | 输入/输出端子有效电平逻辑 | 0 / 0 | 0 / 0 |
| 2310h~2313h | 输入 X0~X3 功能选择 | 1/2/3/0 | 1/2/3/0 |
| 2320h~2321h | 输出 Y0~Y1 功能选择 | 0 / 0 | 0 / 0 |
| 2330h~2333h | 输入 X0~X3 滤波时间 | 2 | 2 |
| 2400h | **细分** (电机转一圈脉冲数) 400~51200 | **50000** | 50000 |
| 2401h | 最大有效电流 (mA) 0~5500 | **2000/4000/5500 (随型号)** | **2000** |
| 2402h | 基础电流百分比 (运行时基础电流) 0~100 | 40 | 40 |
| 2404h | 开环最大电流百分比 0~100 | 100 | 100 |
| 2405h | 锁机电流百分比 0~100 | 40 | 40 |
| 2406h | 锁机时间 (ms) 0~5000 | 100 | 100 |
| 240Dh | 脉冲指令均值滤波 1~1024 | 512 | — |
| 2500h~2503h | 厂家参数: 电流环 Kp 增益倍数 / Kp / Ki / Kc | 手册不给 (-) | 10260 / 2367 / 3276 / 112 |
| 2408h / 2409h | **电子齿轮比 分母 / 分子** (1~51200) | 1000 / 4000 (仅 V1.0 有) | 1000 / 4000 |

> **2408h / 2409h 只存在于 V1.0 手册**：V1.0 定义
> 「2408h = 电子齿轮比分母，默认 1000；2409h = 电子齿轮比分子，默认 4000」，
> 而 V2.3 / V2.4 的 YKD 附录**都不再列这两个对象** (V2.4 改用 6091h 描述齿轮比)。
> 驱动器仍然实现它们 —— 现场可正常读写。
>
> **它们是否生效取决于 2201h**：手册 V1.0 §8 与 V2.4 §4.1 都写明
> 「默认使用**细分**方式，切换为**电子齿轮比**时才通过 2408h/2409h 定义齿轮比」，
> 2201h 默认 = 0 = 细分有效 ⇒ 默认当量对象是 **2400h (=50000 脉冲/圈)**，
> 不是 2408h/2409h (=1000 脉冲/圈) —— **两者差 50 倍**。
> `2201h` 与 `2400h`、`2408h`/`2409h` 在 `sm_spec_table[]` 里同为一组，四项都是
> `dangerous` + `SM_CMP_EXACT`，漂移即拒绝 S3/S4 —— 见
> [slide_motion_verify.md](slide_motion_verify.md) §3.3。

> **2401h 「默认 5000 mA」是错的**：那个数字来自 V1.0 (以及 V1.0 里
> 「工作电流可调，最大支持 5A」「开环工作电流范围 2A~5A」等从 5A 机型抄来的说法)。
> V1.1 使用手册已更正为 **YKD2205PE 最大输出电流 2 A**，并注明适配 42 两相**开环**
> 电机。V2.4 的 YKD 附录也把范围改成 0~5500 并给出型号相关默认值
> 2000/4000/5500。**现场实测的 2000 mA 与 2 A 上限一致**，是正常出厂值。

## 报警与指示灯

- **603Fh 驱动器故障码** (U16 RO，同 1003h 低 16 位): 0000h 无错误 / FF01h 过流 /
  FF02h 过压 / FF03h 欠压 / FF04h 动力线报警 / FF06h 通讯报警 / FF08h 传感器告警。
- 从站上电后 `ecx_readstate()` 看到的 **AL 状态码**，
  以及 221Ah / 221Bh (停止/急停信号、限位信号的报警方式) 的配置。
- **LED (V1.1 §4.2.2)**: RUN 绿 = EtherCAT 状态 (关 = INIT/掉电，慢闪 = PRE_OP，
  单闪 = SAFE_OP，快闪 = Bootstrap，常亮 = OP)；ERR 红 (关 = 无错误，
  快闪 = EEPROM 载入错误，单闪 = AL 状态寄存器错误，**双闪 = 过程数据看门狗超时**，
  常亮 = PDI 看门狗超时)；ALM 红按闪烁次数报故障 (1 过流 / 2 过压 / 3 欠压 /
  6 通讯 / 8 传感器)。L/A 绿 = 物理链路 (关 = 未建立，快闪 = 有数据)。
  *(V1.1 该表为多列并排，PDF 抽取易错位，判灯前对照 PDF 原件。)*

## 设备导入验证工具 slide_verify (原生 SOEM)

滑台设备接入/上电 EtherCAT 总线时的**导入验证**:扫描总线 → 逐台读取身份与关键
CiA402 对象 → 判定是否为受支持的 YKD2205PE 滑台驱动器,逐项给出 PASS / WARN /
FAIL,最后汇总并返回退出码。**只读工具**:不进 OP、不写 6040h 控制字、不做使能
序列、不动电机。验证深度:PRE_OP 读对象(主),随后可选尝试 SAFE_OP(失败记
WARN 不致命)。

**源码位置与工程结构**(本仓库自有代码,独立于下载的 SOEM):

```text
EC_CPP/
├─ CMakeLists.txt        # 顶层工程: add_subdirectory(SOEM) + add_subdirectory(slide_verify)
├─ slide_verify/         # 本工具 (slide_verify.c + CMakeLists.txt)
└─ SOEM/                 # 下载的第三方库, 未改动
```

构建(在仓库根,顶层工程会引用并编译 SOEM 库,但不构建 SOEM 自带 samples):

```bash
cmake -S . -B build                  # 首次/改 CMakeLists 后重跑一次
cmake --build build --config Release --target slide_verify
build/slide_verify/Release/slide_verify.exe '\Device\NPF_{GUID}'
```

不带 `ifname` 时仅列出可用网卡。

**识别集合**(与代码 `sv_is_ykd_slave()` 一致):厂商 `0x0994`,产品码
{`0x2000`, `0x3000`} 双收。位置 = 0-based 总线线序(与 python 链 `--pos`、aliasinfo
一致)。

**输出流程**:
1. 身份表(位置 / 序号 / 配置地址 / 别名0012h / 厂商ID / 产品码 / 修订 / 序列号 / 状态 / 名称);
2. 逐台身份判定(是否 YKD、是否达 PRE_OP);
3. 每台 YKD 的 PRE_OP **对象级验证**(逐对象 PASS/WARN/FAIL + 读数:状态字低4位/
   Fault位 bit3/到位 bit10、操作模式名 1/3/6/8、实际位置/速度等);
4. 可选 **SAFE_OP 探针**(进不去记 WARN 不致命);
5. 汇总表 + 退出码。

**对象校验矩阵**(表驱动 `sv_objs[]`,加对象只改表不动码):

| 对象 | 角色 | 判定 |
|---|---|---|
| 6041h 状态字 / 6060h / 6061h / 6064h / 606Ch | **必需** | 缺失或超时 → FAIL;Fault 位=1 / 未知模式 / PRE_OP 下仍有速度 → WARN |
| 607Ah / 6081h / 6083h / 6084h / 60FDh / 60FEh / 6502h | **可选** | 缺失 → WARN;读出仅显示 |

**退出码**:

| 码 | 含义 |
|---|---|
| 0 | 通过:至少 1 台 YKD 且全部 PASS |
| 1 | 用法错误 / 初始化失败(网卡打不开、Npcap 被独占、需管理员) |
| 2 | 空总线(config_init 未发现从站) |
| 3 | 存在 YKD FAIL(必需对象缺失/超时、YKD 未达 PRE_OP) |
| 4 | 无 FAIL 但含 WARN(可选对象缺失、未达 SAFE_OP、未知模式、混挂非 YKD) |
| 5 | 有从站但一台 YKD 都没有(环境不匹配) |

> 退出码 0 的条件:总线上只挂 YKD2205PE 且必需对象全部可读。别名=0 不判 FAIL,
> 同型号多台按总线位置区分。

**注意**:与 aliasinfo / python 链 pysoem 一样独占 Npcap 网卡,勿同时运行。

## 带动作验收验证工具 slide_motion (原生 SOEM)

`slide_verify` 证明的是「驱动器能被识别、能读」：它不进 OP、不写 6040h、
不动电机。

`slide_motion` 补的是:参数基线与漂移比对 → CiA402 使能状态机 →
相对微动并校验编码器反馈的行程/方向/闭环归位。

**默认不动**:不给 `--allow-motion` 时全程只读, 一个字节都不写; 移动还额外需要
`--allow-jog` 与一次交互确认。它走 **SDO + PRE_OP/SAFE_OP**, 不开 OP、不改 PDO
映射 —— 因为 PP 模式的速度规划器本来就在驱动器内部, 不需要周期数据。

阶段:`S0` 身份门(只读) → `S1` 参数基线(只读) → `S2` OP/DC(**本版未实现**)
→ `S3` 使能状态机 → `S4` 微动与反馈闭环 → `S5` 收尾(无条件执行)。

> 退出码与 `slide_verify` 不同, 且多一个 **10**: 收尾后回读 `6041h` 仍报
> Operation enabled, 即**电机可能仍带电** —— 它覆盖其它所有码。

**完整设计、安全须知、微动判据、退出码与真机实测记录**:
[docs/slide_motion_verify.md](slide_motion_verify.md)。

示例基线:[baseline_ykd2205pe.ini](../baseline_ykd2205pe.ini)。

> **关于 2401h 最大电流**:基线里记的 2000 mA **就是 YKD2205PE 的正常出厂值**
> (机型上限 2 A；手册默认列写的是型号相关的 2000/4000/5500)。基线文件固定的是
> **这台设备的总线位置与整定值**, 不只是手册默认值。
>
> **关于 2201h**:基线里**已补**(2026-09-17), 它决定行程当量来自 2400h (细分)
> 还是 2408h/2409h (电子齿轮) —— 不固定住这个, 「1 脉冲 = 多少位移」就没有任何
> 断言兜底。写法是 `  2201h = 0` (**没有 `!`**), 因为那个值是**按手册默认填的假设、
> 不是本机实测**。量到之后把它改成 `! 2201h = <实测值>`。

## 状态机切换专项验证小程序 sm_state / sm_pdo (原生 SOEM)

`slide_motion` 的 S3 用的是 **SDO + PRE_OP/SAFE_OP**, 实测在出厂配置下到不了 ——
TiPDO 未映射时从站会以 AL `0x001E` 拒绝 `PRE_OP → SAFE_OP`, 而 PRE_OP 下 CiA402
状态机根本不响应 `6040h` (论证与实测见
[slide_motion_verify.md](slide_motion_verify.md) §6)。这两个小程序专门把这件事单独
拎出来验:

| | `sm_state` | `sm_pdo` |
|---|---|---|
| 控制字怎么发 | **SDO** 写 `6040h` | **RxPDO 过程数据**里的 `6040h` |
| 状态字怎么读 | SDO 读 `6041h` | **TxPDO 过程数据**里的 `6041h` (+ 步末 SDO 交叉核对) |
| AL 状态 | PRE_OP / SAFE_OP | SAFE_OP / **OP** |
| 写 PDO 映射 | 不写 | **写** `1A00h`/`1C13h` (仅 RAM, 默认还原) |
| 需要授权 | 无 (只写 `6040h`) | `--allow-pdo` |

两者的切换序列完全同构 —— 上行 `0x0000 → 0x0006 → 0x0007 → 0x000F`,
`--hold` 观察, 下行 `0x0007 → 0x0006 → 0x0000`。断言掩码都是 **`0x000F`**
(只取低 4 位)。**两者都不发运动指令**, 不写 `607Ah`/`6060h`/软限位;
但第 3 步之后电机通电有保持力矩。

> **掩码 `0x000F` 的现状 (据手册更正)**：手册**定义了** bit5/bit6, 低 4 位就是
> `0000/0001/0011/0111` 四态。掩码取低 4 位本身没问题, 但**第 0 步「期望 0x0000」
> 是错的**: 上电自检完成后驱动器处于 `0001` (Ready to switch on, **电机释放**),
> 而手册的「步骤 0 = 0000」只在刚上电那一瞬成立。
> 要判「未使能且无故障」请用 **`(sw & 0x000C) == 0`** (bit2 未使能 / bit3 无故障)。

### sm_pdo 的 PDO 映射

手册口径的出厂 `1A00h` (TxPDO0) 是**未映射**的 → SM3 长度为 0 → 从站拒绝 SAFE_OP
(AL `0x001E`)。边界收在:

- 只写 **RAM**, **从不写 `2102h`** (EEPROM) —— 掉电即回出厂映射。
- 默认跑完**还原**成运行前快照; `--keep-mapping` 才保留; `--no-map` 整个跳过
  (此时若映射不合期望, 直接以退出码 4 拒绝)。
- 默认只往 TxPDO **追加 `6041h`** 一项, 已有映射项原样保留。
- **偏移证不出就拒绝** (退出码 4): `6040h`/`6041h` 在过程数据镜像里的字节偏移
  必须能从映射表严格推出; 不猜。写错字段可能凭空下位指令。
- `6040h` **不经 SDO 写** —— 它就是被测变量。唯一的 SDO 写入口只用于映射对象与
  `--reset-fault` 的故障复位。

```
sm_pdo [网卡] [选项]
  --allow-pdo           允许写 PDO 映射并进 OP。不加则只读快照 (退出码 0)。
  --axis N              选哪根轴 (总线位置, 0 起)。默认第一台 YKD2205PE。
  --no-map              不写 PDO 映射 (要求映射已合法, 否则退出码 4)。
  --keep-mapping        跑完保留改过的映射 (默认还原)。
  --state op|safe-op    跑到哪一级 AL 状态, 默认 op。
  --dc                  配置分布式时钟 (自由运行进不去 OP 时试它)。
  --cycle US            DC 同步周期, 默认 1000 (范围 250~4000)。
  --reset-fault         允许清故障 (进 OP 后经 PDO 推 0x0080)。
  --allow-other-slaves  总线上还有别的从站时也继续 (它们留在 PRE_OP)。
  --hold MS             停在 Operation enabled 的观察时长, 默认 500。
  --tmo MS              单步等待超时, 默认 1000。
  --ifname=GUID         覆盖写死的网卡。
```

退出码: **0** 全 PASS / **1** 用法或初始化失败 / **2** 无从站或无目标轴 /
**3** 有 FAIL / **4** 安全护栏拒绝 (未写任何东西) / **5** 进不去 SAFE_OP 或 OP /
**6** 过程数据未落地 (WKC 持续偏短) / **10** 收尾后 `6041h` 仍报 Operation enabled
(**电机可能仍带电**, 覆盖其它所有码)。

`--state safe-op` 只验证"补完映射后 AL `0x001E` 是否消失", 不进 OP、不写控制字。

> 无授权直接跑 `sm_pdo` 是**纯只读**的: 它会打印 `1A00h` 计数、`SM3` 长度与
> `6041h` 当前值。**期望值取决于这台设备的当前配置**:
> 若 `1A00h` = 0 项 / `SM3` 长度 = 0 (手册口径的出厂态), 就复现了那条根因;
> 若像 2026-09-15 实测那样 `1A00h` 已含 `6041h`、`SM3` = 10 字节,
> 说明配置已被改过。

## 多轴运动接口 motor_api (原生 SOEM)

`slide_motion` 走的是 **SDO + PP (6060h=1)**, 一次一条指令、目标值不必每周期刷新。
`motor_api` 是并列的第二个运动层, 用**过程数据**做需要每周期下发目标值的三种模式:

| 模式 | 6060h | 目标值走哪 | 用它的接口 |
|---|---|---|---|
| 位置同步 CSP | **8** | RxPDO `607Ah`, **每周期刷新** | `em_csp_move_abs/rel`, `em_csp_move_multi` |
| 速度 PV | **3** | RxPDO `60FFh`, 到位后写 0 | `em_pv_run_for` |
| 回零 HM | **6** | `6098h` 方式 + `6040h` bit4 上升沿 | `em_home` |

**本驱动器没有 CSV(6060h=9)** —— `6502h` 实测 `0xA5` = PP + PV + HM + CSP。
所以"速度模式"只能是 PV。

**为什么不能在 SDO 上做**: SOEM 的 `EC_TIMEOUTRXM` 是 **700 ms**, 而同步周期是 **ms 级**;
CSP/PV 的目标值必须每周期刷新, SDO 的往返时间根本追不上。

### CSP 的加减速形状由主站决定 —— 手册口径

手册对 CSP 的原话:

> 循环同步位置模式下，**控制器完成位置指令规划**并输出规划好的目标位置 `607Ah`，
> 同时以周期同步的方式将目标位置发送给驱动器，**驱动器内部完成位置、速度控制**。

- **形状 = 主站下发的 `607Ah` 流。** 驱动器只跟踪, 不生成斜坡。所以仓库里两个 CSP
  发送方的形状**就是**电机的形状, 而**两者都不是 S 型**:

  | 发送方 | 加速段 | 减速段 |
  |---|---|---|
  | `em_csp_move_multi()` | **速度阶跃** | **速度阶跃**(全程匀速) |
  | `hmi` 的 `interpolate()` | **速度阶跃** | 恒定减速度(线性) |

- **`6081h`/`6083h`/`6084h` 不构成 CSP 的形状来源。** 它们是 PP / PV / HM 的轮廓参数。
  (它们在生效 RxPDO `1601h` 里、每周期被下发, 那是映射的既成事实, 不是"CSP 要用它们"。)

分层（与 `slide_motion` 的 `sm_bus` / `sm_guard` / `sm_motion` 同构）:

```text
motor_api/
├─ ec_motor.h           对外接口 (em_bus_t / em_axis_t 不透明)
├─ ec_motor_internal.h  两层之间的私有定义与写原语声明
├─ ec_motor.c           底座 —— **本目录唯一出现 ecx_SDOwrite 的文件**
├─ ec_motor_motion.c    运动层 —— 使能阶梯 / CSP 插值 / PV / 回零
└─ motor_test.c         验收程序 (S0~S8)
```

阶段: S0 开总线 / S1 选轴 / S2 实读映射与只读参数（**到此退出 0**）/
S3 补映射 + 建过程数据 + 证明偏移 / S4 进 OP / S5 使能 /
**S6 CSP 或 PV（由 `--mode` 选）** / S7 回零（方式 24, 或 `--home-lim` 给的方式 18/17）
/ S8 收尾（无条件执行）。

不变量: `grep -rn ecx_SDOwrite motor_api/` **只应有 `ec_motor.c` 一处**。

### 多轴怎么"同时"

`em_csp_move_multi()` 在一个循环里**一帧喂所有轴**: 逐轴按实测 `dt` 推进插值目标、
逐轴写各自的输出镜像 `6040h`/`607Ah`, 然后**才**调一次 `em_service()`。
各轴**各自独立**判到位（`|6064h - 终点| <= 容差` 且插值目标已到终点), 没有耦合、
没有插补、没有龙门同起同停。

### CSP 的目标位置以什么为基准

`em_csp_move_multi()` 收的是**绝对**目标位置, 接口不替调用方决定基准。
`em_csp_move_rel_multi()` 收的是**相对位移**, 基准由**接口内部**取:

> 每根轴的 `607Ah = 调用这一刻该轴的实际位置（6064h）+ delta[i]`。

函数先把**所有**轴的起点读完, 再算目标, 再一次性下发。任何一根轴取不到实际位置就
**整体拒绝**, 一个字节都不下发。日志里会把每根轴的 `起点 -> 终点 (位移)` 打出来。
`em_csp_move_rel()` 就是它的 n=1 薄封装。

### 偏移从哪来

**必须先读 `1C12h` / `1C13h` 问"哪个 PDO 生效", 再按它指到的索引读映射表。**
这台设备的 `1C12h` 指的是 **`1601h`** —— 见上文「PDO 映射」。写死 `1600h` 的后果不是
报错, 而是**悄悄改错表**。

实测结论: 生效的 `1601h` 里 **`6040h` / `607Ah` / `60FFh` 已经齐全**
（在 +0 / +3 / +7 字节）, `1A00h` 里 `6041h` / `6064h` / `606Ch` 也齐全
（在 +0 / +2 / +6）。所以在本机上做 CSP / PV / HM **不需要改写任何 PDO 映射**。
不改映射 ≠ 不写输出镜像: 输出镜像的每一个字节都是主站每周期**必须**给出的,
镜像里属于本次运动的那些项（`6040h`/`6060h`/`607Ah`/`60FFh`/`6083h`/`6084h`）
**一项都不能漏**, 漏掉的那项就是每周期下发 0。判据是 `em_dump_pdo()` 里那张 7 项的
处置表。

```
motor_test [网卡] [选项]
  --axis A,B            选轴 (总线位置, 0 起)。默认**全部**从站。
  --allow-pdo           允许补 PDO 映射 (仅 RAM) 并建过程数据。不加则跑到 S2 就退出 0。
  --allow-motion        允许使能/运动 (需 --allow-pdo)。
  --home                允许回零 (需 --allow-motion)。默认方式 24。
  --home-lim pos|neg    S7 改成找限位: pos -> 方式 18 (以正限位为原点),
                        neg -> 方式 17 (以负限位为原点)。需同时给 --home。
                        值只收这两个词, 不收数字 (收数字等于"随便给个方式号都能
                        发起回零")。发起前读 60FDh 判 a)/b) 分支并打印。
  --mode csp|pv         S6 跑哪种模式, 默认 csp。
                          csp = 位置同步 (6060h=8), 相对各自起点走 --dist 再走回
                          pv  = 速度模式 (6060h=3), 跑 --pv-hold 后停并断言 bit12
  --dist PUL            CSP 位移, 默认 5000, 上限 50000 (放宽要 --force-caps)。
  --vel PUL/S           CSP 速度, 默认 5000, 上限 50000。
  --pv-vel PUL/S        PV 速度, 默认 1000, 上限 50000。
  --ramp-acc PUL/S²    6083h 轮廓加速度, 默认 500000, 上限 5000000。
                        驱动器在 PV 下靠它爬坡。**能传 0, 但 0 意味着「斜坡永远
                        起不来」** —— 现象是报"速度指令已被接受"却一步不走。
                        默认值取自驱动器 6083h 的实读值 (读不到才用这个常数)。
  --ramp-dec PUL/S²    6084h 轮廓减速度, 同上。
  --pv-hold MS          PV 保持时长, 默认 1000, 上限 3000。
  --pv-opposite         PV 时让第 2 轴反向跑 (默认两轴同向)。
  --tmo MS              单个运动超时, 默认 10000。
  --keep-mapping        跑完保留改过的映射 (默认还原)。
  --dc [周期us]         配置分布式时钟 (默认不用 DC)。
  --force-caps          放宽上面那些上限 (应当是有意的动作)。
```

参数**只支持空格分隔**（`--dist 5000`），没有 `--dist=5000` 这种写法; 传了会被当成
未知参数退出 1。数字用 `atoi` 解析, 不做尾部校验。

> 这里**没有** `--home-mode 24|29` / `--home-vel-*` 这类选项: 回零方式只由
> `--home` 与 `--home-lim` 两句话决定 (24 / 18 / 17 三个, 没有 29 —— 反向找原点
> 在 CLI 这一侧没有入口), 找原点速度由 `--vel` 收紧且不超过 2000 pul/s、
> 返回速度取它的 1/4。

退出码: **0** 全 PASS / **1** 用法或初始化 / **2** 无可用轴 / **3** 有 FAIL /
**4** 安全护栏拒绝 / **5** 进不去 OP / **6** 过程数据未落地 / **7** 回零未达位 /
**10** 收尾未能确认失能 (**电机可能仍带电**, 覆盖其它所有码)。

> **先只跑 S0~S2**（不带任何 `--allow` 参数): 它一个字节都不写, 打印出的
> `1C12h`/`1C13h` 分配、实际生效的那张映射表、推导出的每个字段偏移, 以及
> `2201h`/`2400h`/`607Dh`/`6502h` 等只读参数。

### PV 怎么判"到底转没转"

`em_pv_run_multi()` 会逐轴打印 `6064h 起点 -> 终点 (走了 N pul)` 与运行期间
`606Ch` 的峰值, **位置一个计数都没变即判 FAIL**（退出码 3）。

2026-09-15 那次 PV 全阶段 PASS、退出码 0, 而电机一步没走
—— 因为所有检查都在问「驱动器收下指令了吗」（`6041h` bit12 该清的清、该置的置、
无故障无限位, 全对), **没有一条在问「电机动了吗」**。判据落在这两项上:

| 观测 | 含义 |
|---|---|
| `6064h` 变没变 | **唯一的硬判据** —— 走没走, 位置说了算 |
| `606Ch` 峰值 | 辅助证据。有些驱动器会直接回报指令值, 所以它非零**不能**反推真的在转 |

CSP 一侧本来就有到位断言（`|6064h - 终点| <= 容差`)。

### 安全边界 (与 `slide_motion` 同一套口径)

- 默认**一个字节都不写**; 写 PDO 映射要 `--allow-pdo`, 使能/运动要 `--allow-motion`,
  回零要 `--home`（它会撞限位、会找原点开关）; `--home-lim` 与 `--home` 是**同一道授权**
  —— 不给 `--home` 就相当于用 `--home-lim` 绕开它, 程序直接拒绝 (退出码 4)。
- **`--allow-motion` 本身就是那句确认 —— 运行时不问 y/N。** 别在脚本里顺手加上它:
  加上就等于确认过了。这一点与 `slide_motion`（它有 `--allow-jog` + 交互确认）
  **不同**。
- 上限**只能收紧, 放宽要 `--force-caps`**。按 `2400h = 50000 pul/圈` 算,
  默认 `--dist 5000` ≈ **0.1 圈**。
- `6040h` 推送值只有 **`0x0000` / `0x0006` / `0x0007` / `0x000F`**, 回零另加 `0x001F`,
  故障复位另加 `0x0080`。**不试探厂商私有控制字。**
- **从不写 `2102h`** (EEPROM), 不改软限位 `607Dh`, 不改 `2400h`/`2408h`/`2409h`/`2201h`
  任何当量参数。默认跑完**还原** PDO 映射 (`--keep-mapping` 才留), 且只写 RAM。
- Ctrl-C 的处理器**只置一个 `volatile sig_atomic_t`**, 不在信号上下文里发 SDO;
  收尾路径**无条件执行**, 收尾后 `6041h` 仍报 Operation enabled → **退出码 10**。
- 首次回零**必须**把速度收到很低、人在物理急停旁 —— 方式 24 的物理正确性取决于现场
  接线 (`2310h`~`2312h` 实测 = 1/2/3, 即 X0 = 原点、X1 = 正限位、X2 = 负限位),
  不对就换 29 或 35。
- **找限位 (`--home-lim`) 的前置条件更硬**: 它的 a)/b) 两条分支靠读 60FDh 才分得出来,
  而 60FDh 那两位是 2300h (输入有效电平逻辑) 与接线是否一致的结果。本机实测
  `2300h = 0x0000` 而 X1/X2 空载物理为高 → 正/负限位**同时**报有效, 那种读数下
  程序会拒绝发起 (退出码 4, 一个字节都不写)。先让「高级选项」那个默认动作把它写成
  `0x0007` (或 `--allow-param --npn`) 再试。
- 网卡是**单进程独占** (Npcap): 与仓库的 python 链 / 其它工具**不能同时跑**。
