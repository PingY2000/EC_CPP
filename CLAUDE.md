# CLAUDE.md —— 在这个仓库里干活前先读这几条

面向 YKD2205PE 驱动器 (CiA 402 / EtherCAT) 的两个 Qt 上位机: `scan` (产品, 蛇形扫描采集)
与 `hmi` (手动调试台)。共用 `hmi/ecatworker.cpp` 这个总线工作线程。

**规矩的正文在两份文档里, 不在这个文件里**; 这里只压出"最容易犯、犯了最显眼"的那几条:

| 想知道 | 读 |
|---|---|
| 界面该**怎么写** (标点 / 长度 / 术语表) | [docs/scan_sweep.md](docs/scan_sweep.md) §26.2 |
| 界面该**写什么** (不写步骤 / 成因搬走 / 出路只许「请……」) | 同上 §26.3 |
| 某句提示**背后的成因与排查** | [docs/scan_messages.md](docs/scan_messages.md) |
| 改提示语之前逐条对的清单 | 同上 §11 |
| 每个程序的设计与验收状态 | [docs/scan_sweep.md](docs/scan_sweep.md) / [docs/hmi_click_position.md](docs/hmi_click_position.md) |

---

## 1. 界面文案: 六条硬规矩

**这一节是重复犯过的错, 不是新定的。** 加一条新提示、改一句老提示之前逐条过一遍。

1. **字符串里不许出现 `**`。** Qt 的这些字走 `QLabel::setText` (默认 `Qt::AutoText`) 与
   `QToolTip`, 都是**纯文本**, 不认 Markdown —— 屏幕上显示的就是字面的两个星号。
   **这是本仓库复发次数最多的一条。**
   *它为什么会复发*: 本仓库的**注释**大量用 `**……**` 表示强调 (那是仓库风格, 保留),
   写顺手了就会把那两个星号一起带进 `QStringLiteral(...)` 里。**注释里的 `**` 合法,
   字符串里的 `**` 是 bug** —— 落笔时看一眼引号内侧。
2. **标点**: 分隔符一律半角 `,` `:` `;` (后跟一个空格), 括号用半角 `( )`; 句末与列举用全角
   `。` `、` `——` `「」` `…`。禁用 `->` (写 `→` 或写成字)、`...` (写 `…`)、
   以 `!` / `?` 收尾 (改陈述句)。
3. **单位用英文符号**, 与数值之间一个空格: `200 ms`、`0.5 mm`、`20000 pul/s`。
   寄存器与位号原样: `6041h bit3`、`0xFF02`。
4. **长度上限** (超了就是没精简完): 普通 tooltip 2 行 / 60 字; 操作类 tooltip 4 行 / 90 字;
   横幅 (`hint()`) 1 句 / 40 字; 报错与拒绝理由「现象 + 出路」/ 50 字; 模态框正文 80 字。
5. **不写操作步骤。** 不许出现"先点某处 / 先按某个 / 用什么方法试一下"。步骤属于手册与培训。
   **出路只允许一句「请……」** (真的只有一条出路时才写)。不许出现"多半是 / 可能是因为 /
   有两种情况"—— 那是**成因分析**, 搬进 [docs/scan_messages.md](docs/scan_messages.md)。
6. **界面里不留指向手册的指引** —— 包括"见手册 / 详见 xxx"。唯一的例外是
   `mode_text()` 与 `fault_code_text()` 里的「手册之外」: 那句说的是"这个值不在手册那张表里",
   不是让人去翻手册。

**术语**: 限位开关一律「触发 / 未触发」(不是「压着 / 松开」); 「分辨率」不写「网格间距」;
「色标」不写「色阶」; 回零那一族是「回零」「找原点」「找正限位」「找负限位」。完整表在 §26.2。

**范围**: `scan` 与 `hmi` **说的是同一件事, 不许有两套说法**。`hmi/ecatworker.h` 里那些
`ecatcmd::*` 文案函数是两个程序共用的同一份源码, 改一次两边都变 —— 这是**故意的**。

## 2. 改文案时必须一起动的两处

- **`scan/selftest.cpp` 钉着那些词。** 断言允许在**同一层**上机械调整 (「都没压着」→
  「都没触发」), 但**钉住的判据本身一条都不许删**: 必须点名的码 (`6041h bit3` / `轴X` /
  `0xFF02`)、必须出现的出路 (「故障复位」/「上位机侧取反」)、不许出现的猜测 (「撞」/
  「驱动器认为」)。改完必须跑自检。
- **`docs/scan_messages.md` 里引用着界面的原话。** 原话改了, 那边的引用跟着改, 否则文档与
  屏幕对不上。

## 3. 其它常驻规矩

- **界面入口必须外露**: 不塞进菜单栏、不另开窗口 (仓库既有决定)。
- **屏幕字用中文全角标点, 代码注释也用中文** —— 但注释里的 `**` 是强调, 是合法的。
- 设计理由、历史、取舍写进**代码注释与文档**, 不写进界面的字里。

## 4. 构建与验证

```bash
export PATH="/c/msys64/ucrt64/bin:/c/Qt/Tools/Ninja:/c/Qt/Tools/CMake_64/bin:$PATH"
cmake --build out/build/hmi-qt-ucrt64            # 两个界面 + 自检, 期望 0 warning
```

```bash
export PATH="/c/msys64/ucrt64/bin:/c/Qt/6.10.1/mingw_64/bin:$PATH"
./bin/scan_selftest.exe                          # 不需要设备也不需要界面
# ... 924 passed, 0 failed, 1 skipped
```

**任何改动之后都要跑一次自检。** 它是这个仓库唯一能自动验证判据的东西 (只链 `Qt6::Core`)。
`scanwindow.cpp` 这类界面文件**不在自检里** (`scan/CMakeLists.txt` 的 `SCAN_COMMON_SRC` 不含它),
所以界面上的改动**只能靠人在屏幕前看** —— 报告时必须说清"哪些验了、哪些没验", 不许拿
"编译过了"当"验过了"。

`hmi/*.ui` 那个文件名**不是** `*.ui`, 那个星号是 Unicode 私用区的 U+F02A (Windows 把非法
字符映射到那儿), 已从仓库删掉。查这类怪名字用 `git ls-files -z` 或 `os.listdir` + `repr`,
别用 `ls` / `grep` —— 它们会把 U+F02A 显示成普通的 `*`。
